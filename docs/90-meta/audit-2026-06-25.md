# Auditoría 2026-06-25 — Gap doc↔código y preparación para producción (módulo por módulo)

> **Objeto:** medir, empíricamente y módulo por módulo (superficie **admin** y **cliente**), el gap entre la documentación y el código real, y determinar **qué falta realmente para poner el proyecto en LIVE** (exponerlo a clientes reales).
>
> **Metodología:** auditoría multi-agente bajo orquestación determinista. Baseline medido en vivo → **17 agentes de módulo** (cada uno coteja contract.md + features + ADRs contra el código real, evaluando admin y cliente por separado) + **8 dimensiones transversales** (seguridad, deploy/infra, dinero, datos, eventos, DS/UX, tests, coherencia doc) → **verificación adversarial** de cada hallazgo `critical`/`high` (un segundo agente intenta refutarlo con su propia evidencia, escéptico por defecto). Las severidades de abajo **ya reflejan el ajuste post-verificación**: muchos hallazgos bajaron de severidad al confirmarse que estaban mitigados o conscientemente diferidos. Coste: 2 corridas, ~186 sub-agentes, ~9,6 M tokens, ~30 min.
>
> **Estado de partida:** rama `sprint-cuenta-perfil` (Sprint Cuenta código-completo sin mergear), sobre `master` `497127f`. Es el árbol **near-live**.
>
> **Sustituye como "estado vigente" a** [`audit-2026-06-21.md`](audit-2026-06-21.md) (sus 3 HIGH están cerrados y re-verificados aquí; queda como artefacto histórico).

---

## 0. Registro de cierres post-auditoría (vivo · actualizado 2026-06-26)

> Las §1–§6 son la **medición original** (snapshot 2026-06-25, pristina — NO se altera). Esta sección
> registra qué GLs se han **cerrado en código** desde entonces, contra la checklist de §6. Detalle por
> hallazgo en la memoria del agente (`~/.claude/.../project-state.md`) y en los mensajes de commit.

| GL | Qué | Estado | Ref |
|----|-----|--------|-----|
| GL-3 | Fuga de notas internas por WS (SUPP-INV-3) | ✅ cerrado | H1 · PR #122 (`7c12361`) |
| GL-4 | Seed superadmin con password por defecto en prod | ✅ cerrado (fail-fast) | H1 · PR #122 |
| GL-8 | Borrar BillingProfile rompe integridad fiscal | ✅ cerrado (soft-archive + FK `Restrict`) | H1 · PR #122 |
| GL-9 | Kill-switch `email_enabled_globally` no honrado | ✅ cerrado | H1 · PR #122 |
| GL-2 | Cancelación admin no destruía el recurso + aviso previo | ✅ cerrado | H2 PR #123 + H2.3 PR #125 |
| GL-5 | RGPD: retención `audit_change_log` + portal transparencia | ✅ cerrado | H3a #126 + H3b #127/#129 |
| GL-6 | RC `.es/.eu` cobra y fallaría (contacto regulado) | ✅ parte codeable cerrada | H4 PR #128 · _smoke OT&E real = gate Yasmin_ |
| **GL-17** | **`service.*` lifecycle fuera de Outbox (R8 / MEDIUM-1)** | **✅ cerrado** | **PR #130** (`d7727c4`) — mergeado a master |
| **GL-25** | **Support: HTML inline → inyección + violación D12** | **✅ cerrado** | **PR #130** (`d7727c4`) — mergeado a master |
| **GL-21** | **Sin gestión de cuentas staff (alta/baja/rol → solo BD)** | **✅ cerrado** | **PR #131** — CRUD staff `/admin/users/staff/*` + UI "Equipo" + audit R3 + invariantes |
| **GL-22** | **Overview role-aware no se mostraba al staff (`AdminStats`/`AgentStats` muertos)** | **✅ cerrado** | **PR #131** — `/admin` consume `/dashboard/overview` role-aware + accesos rápidos `canAccess` |
| **GL-26** | **Specs de seguridad faltantes (auth-login, anti-IDOR) + frontend sin red en-PR + comercio/cuenta sin E2E** | **✅ cerrado** | `ClientsBillingService` anti-IDOR ✅ (H1 #122) · `auth-login.service.spec` ✅ (PR #131) · **harness Jest+RTL frontend + E2E Sprint Cuenta + E2E comercio de dominios** (mock RC como 4º webServer Playwright) — **PR #133** (`0a70834`). Runtime E2E validado por CI (label `ready-for-e2e`) |
| **GL-23** | **Support-inside: oferta doc≠seed + upgrade 400 + canales sin dispatcher** | **✅ cerrado** | **PR #131** — (A) canales alineados doc↔seed↔entrega · (B) upgrade de plan con prorrateo ADR-029 A1 + UI "Cambiar de plan". Decisión Yasmin: precios/slots intactos. Cierra DC.18 |
| **GL-28** | **Drift documental (`_events.md`, contadores ADR, contracts domains/account, smoke real)** | **✅ cerrado** | **PR #134** (`8cbd1b8`) — reconcilia `_events.md` + contadores + `contract.md` de domains/account + aclara "smoke real" Enhance |
| **GL-27** | **DS sin enforcement mecánico (R15/R16/D1)** | **♻️ absorbido por el Rediseño UI** (no es sprint suelto) | Decisión 2026-06-26: su mitad útil (**partir `api.ts`**) entra en **F0**; los inline styles los borra el **reskin F4** (no pre-pase = no trabajo doble); el lint R15/R16/D1 se enciende como **ratchet progresivo/final**. Ver [`ui-migration-backlog-2026-06-26.md`](ui-migration-backlog-2026-06-26.md) |

**Mergeado a master:** GL-17 + GL-25 (**PR #130**, `d7727c4`) · GL-21/22/23 + GL-26-auth-login (**PR #131**, `8a47116`) · GL-26 red de tests frontend + E2E (**PR #133**, `0a70834`) · GL-28 drift doc (**PR #134**, `8cbd1b8`).
**Track activo (decisión 2026-06-26):** **Rediseño UI completo F0→F4 ANTES del go-live** → ver [`ui-migration-plan-2026-06-26.md`](ui-migration-plan-2026-06-26.md) / [`ui-migration-backlog-2026-06-26.md`](ui-migration-backlog-2026-06-26.md). **GL-1 (Stripe)** deja de ser sprint aislado: pasa a ser la vertical **F3·E6** del rediseño (sigue siendo el bloqueante real del cobro).
**Requiere decisión de negocio (Yasmin):** GL-24 (extras/categorías: v1 o recorte).
**Gate Yasmin / no-codeable:** GL-1 (Stripe, POSPUESTO a sprint dedicado) · GL-6 smoke OT&E real · GL-7 cluster Enhance live · GL-10..16/19/20 (deploy/infra, gate ADR-069).

---

## 1. Baseline empírico (medido 2026-06-25 @ `sprint-cuenta-perfil`)

| Medición | Resultado |
|---|---|
| Backend typecheck (`tsc --noEmit`) / lint (`eslint`) | exit **0 / 0** |
| Backend test (`jest`) | **90 suites · 1258 passed + 12 skipped = 1270** · 0 fallos · 21,8 s |
| Frontend typecheck / lint (`--max-warnings=0`) | exit **0 / 0** · 0 warnings |
| Tests de integración (Postgres real) | 10 specs `backend/test/integration/*.e2e-spec.ts` (concurrencia + dinero registrar) |
| Superficie | **19** módulos backend + 11 core · **33** controllers · **27** páginas admin + **20** cliente · **4** plugins provisioner · **35** migraciones · schema 1396 LOC · **85** ADRs (adr-001..085) |

> El verde es **real**, no de superficie: respaldado por harness de integración contra Postgres real y por la verificación adversarial de esta auditoría. Los logs `ERROR`/`WARN` de la suite son intencionados (simulan APIs caídas).

---

## 2. Veredicto ejecutivo

El proyecto está **sólido en su sustancia**. La arquitectura modular es genuina, las invariantes de dinero (BILL-INV, DOM-INV-1..6) están implementadas **y** testeadas, la criptografía (AES-256-GCM, bcrypt cost 12, JWT con `jti`) es correcta, el Design System está maduro (Tailwind erradicado, 0 emojis), la red de tests backend es real, y **los 3 HIGH de la auditoría 2026-06-21 están cerrados y re-verificados en código** (rate limiting global con storage Redis, IDOR pause/resume resuelto, spec de `BillingInvoiceService`). El faseado de sprints es honesto y los diferimientos están declarados, no ocultos.

**Tras la verificación adversarial queda UN solo hallazgo `critical` real, y es el que define el go-live:**

> 🔴 **No existe cobro real.** El único `PaymentProviderInterface` implementado es `ManualPaymentProvider` (stub que devuelve `success` sin cobrar). No hay plugin Stripe, ni `backend/src/plugins/payment/`, ni endpoint webhook, ni captura de método de pago, ni superficie de pago en el portal cliente. Una factura solo llega a `paid` cuando un **admin la marca a mano**. **Un SaaS de hosting+dominios no puede operar con clientes reales sin esto.** Contamina además todo el ciclo de impago→reintento (queda inerte) y support-inside (el cliente no puede autoactivar plan).

Por debajo del pago, lo que falta para live se agrupa en cuatro bloques, ninguno sorpresa pero todos **necesarios antes de clientes**: (A) **seguridad/datos/legal** —fuga de notas internas por WS, seed de superadmin con password por defecto en prod, retención RGPD del `change_log`, integridad fiscal al borrar perfiles—; (B) **infra/deploy** —no hay stack de producción, ni backups, ni SMTP real, ni observabilidad encendida, y los ~10 crons no tienen *leader election* (duplicarían facturas/suspensiones al escalar)—; (C) **dependencias externas sin verificar** —ResellerClub y Enhance están validados **solo contra mock**, nunca contra el proveedor real—; (D) **funcional/UX** —sin gestión de cuentas de staff, oferta de support-inside desalineada con el seed, extras de producto en código muerto.

El **gap doc↔código** es mayoritariamente *doc-overclaim/stale benigno* (la doc va por delante del código en varios sitios), pero hay drift que **engaña a quien la usa**: la guía cliente promete pago automático inexistente; la de support-inside publica precios/slots distintos a los que el sistema cobra; los docs de Enhance afirman un "smoke real verificado" que nunca se ejecutó contra un cluster live; `_events.md` (declarada "fuente única de verdad") tiene los contadores rotos.

**Conclusión:** el código está a **calidad de pre-producción alta**; el camino a live es **un sprint de pago (Stripe) + un bloque de hardening/infra acotado y conocido**, no una reescritura. No es "casi listo" (el pago es un hueco grande y real), pero tampoco hay deuda oculta que invalide la base.

---

## 3. Scorecard por módulo (superficie admin + cliente)

> Leyenda madurez: ✅ listo para live · 🟡 mostly, con gap acotado · 🟠 parcial, gap relevante · 🔴 bloqueado. "n/a" = superficie inexistente por diseño.

| Módulo | Estado | Admin | Cliente | Bloqueador principal para live |
|---|---|---|---|---|
| **auth + cuenta** | 🟡 | 🟡 (sin gestión de staff) | ✅ login/2FA/perfil e2e | No hay CRUD de cuentas staff/agentes (alta/baja/rol → solo BD) |
| **clients / CRM** | 🟡 | 🟡 (lectura/notas; sin edición perfil) | ✅ billing-profiles e2e | Borrar BillingProfile rompe integridad fiscal (FK `SET NULL`) |
| **billing** | 🔴 | ✅ flujo operativo e2e | 🟠 read-only (sin pagar) | **Pago real ausente** (ManualPaymentProvider) |
| **products / catálogo** | 🟡 | 🟡 (sin UI categorías) | ✅ tienda e2e | Extras (ADR-020) = código muerto; categorías sin UI |
| **provisioning / services** | 🟡 | ✅ e2e | ✅ e2e | Cancelación admin no llama `plugin.deprovision()` (resource leak) |
| **plugin framework** | ✅ | ✅ e2e (@rjsf) | n/a (admin-puro) | Rotación de `ENCRYPTION_KEY` sin path de recuperación |
| **domains commerce (RC)** | 🟡 | ✅ precios/restore | ✅ tienda/transfer e2e | Smoke OT&E real pendiente; `.es/.eu` cobra pero fallaría en RC |
| **plugin Enhance CP** | 🟡 | ✅ DNS e2e | ✅ DNS e2e | Cluster Enhance live no provisionado; shapes nunca validados |
| **support (chat/tickets)** | 🟡 | ✅ 3-col e2e | ✅ e2e (auth) | Notas internas se filtran por WS; WS sin Redis adapter |
| **support-inside** | 🟡 | ✅ editor e2e | ✅ comparador e2e | Sin pago → no autoactiva; upgrade rechaza 400; oferta ≠ seed |
| **tasks + notes** | 🟡 | ✅ e2e (interno) | n/a (interno) | Deuda observabilidad (sin Outbox ni audit de transiciones) |
| **notifications** | 🟡 | ✅ plantillas e2e | ✅ campana e2e | SMTP real ausente; support viola D12 (HTML inline) |
| **audit + transparencia** | 🟠 | ✅ timeline e2e | 🟠 portal ~20% ADR-010 | Portal RGPD incompleto; `audit_change_log` sin retención |
| **error-log** | 🟡 | ✅ e2e | n/a (interno) | Sentry sin DSN + Pino no cableado → ciego en prod |
| **settings** | 🟡 | ✅ e2e | n/a (admin-puro) | Kill-switch `email_enabled_globally` no honrado |
| **dashboard shell** | 🟠 | 🟠 overview staff no se muestra | ✅ overview cliente e2e | `AdminStats`/`AgentStats` son código muerto (staff aterriza en /admin estático) |
| **stubs (infra/KB/promos/partner/users)** | n/a v2 | sin superficie | sin superficie | **Ninguno** — correctamente fuera de alcance v1 (honestos) |

**Lectura:** la superficie **cliente** está, en general, más cerrada que la **admin**. Los huecos admin recurrentes son *gestión* (editar perfil de cliente, gestionar billing-profiles, CRUD de staff, UI de categorías) y *overview operativo*. Ningún módulo está vacío fingiendo estar lleno; los stubs (infrastructure/KB/promotions/partner) declaran su estado con total honestidad.

---

## 4. Scorecard por dimensión transversal

| Dimensión | Madurez | Síntesis |
|---|---|---|
| **Seguridad & auth** | 🟢 buena | 3 HIGH del audit previo cerrados y verificados; cripto correcta; R17 cumplido en el flujo real. Gaps menores (IP de audit spoofeable, sin validationSchema de env, helmet sin CSP/HSTS). |
| **Deploy / infra (P-DEPLOY)** | 🟠 parcial | **La lista más larga de bloqueadores.** Bien preparado (Sentry no-op listo, rate limiting Redis, Outbox+DLQ); falta TODA la capa productiva (stack, backups, SMTP, observabilidad encendida, leader election). |
| **Dinero & billing** | 🟠 parcial | Motor de factura **maduro** (FSM con guards, BILL-INV intacto, Outbox, prorrateo ADR-029). El **cobro NO existe** → `critical`. |
| **Datos & schema** | 🟢 buena | Schema sólido y coherente, seeds idempotentes con guard prod. Riesgos: superadmin seed con password por defecto en prod; `change_log` sin retención. Drift doc>código (≈29 tablas v2 documentadas, marcadas ⬜). |
| **Eventos & Outbox (R8)** | 🟢 buena | Dinero bien cubierto por Outbox (invoice.* + 11 domain.*). `service.*` se emite con `emit()` directo fuera de tx (MEDIUM-1 confirmado). `_events.md` desincronizado. |
| **Frontend DS/UX** | 🟢 buena | DS real y maduro; superficie cliente profesional. Sin **enforcement mecánico** (lint no aplica R15/R16/D1) → 603 inline styles, `api.ts` a 2197 LOC, 4 páginas con bypass de DS. |
| **Testing** | 🟠 parcial | Backend sólido (90 suites + 10 integración Postgres-real). Frontend **cero** tests propios (no bloquea PR). Comercio de dominios + Sprint Cuenta sin E2E; `auth-login` y `ClientsBillingService` (anti-IDOR) sin spec. |
| **Coherencia documental** | 🟢 buena (estructura) / 🟠 (contadores) | Índice ADR e infraestructura sólidos; **contadores y estados stale** (`_events.md` 38/28 vs ~52 reales; ADRs 60/84 vs 85); faltan `contract.md` de `domains` y `account`. |

---

## 5. El bloqueante crítico: pago real (detalle)

**`SEC/BILL-PAYMENT-1` — No existe procesador de pago real · `confirmed/critical` (sin refutación).**

- **Qué hay:** `PaymentProviderInterface` (createPayment/handleWebhook/refund/getStatus) bien abstraída (R4: el core no importa SDK de pago) + un único `ManualPaymentProvider` **inline** en la propia interface.
- **Qué falta:** plugin Stripe (PaymentIntent/Checkout Session + webhook firmado `/webhooks/stripe` + refund real + `getStatus`), captura de método de pago en el frontend, superficie cliente "Pagar factura", conciliación idempotente vía webhook, cifrado de claves en SecretVault (ADR-015), circuit breaker (R11).
- **Evidencia:** `ls backend/src/plugins/payment` → no existe; `payment-provider.interface.ts:82` (stub); `billing-invoice.service.ts:74` (`new ManualPaymentProvider()` hardcoded); `grep /webhooks|@Post.*webhook` → 0; `dashboard/billing/[id]/page.tsx:140-145` (solo `DownloadPdfButton`).
- **Acoplados:** (a) ciclo de reintentos automáticos **inerte** —la rama de cobro exige `provider.name !== 'manual'`, que nunca se cumple (`billing-lifecycle.worker.ts:200`)→ una factura impaga deriva en suspensión sin que el cliente haya tenido NINGUNA vía de pago; (b) `refund()` manual no devuelve dinero real; (c) copy de UI cliente promete "pago automático" inexistente (`dashboard/billing/[id]/page.tsx:128`, `CartView.tsx:95`).
- **Estimación:** **3-5 semanas** (sprint dedicado Stripe + webhook + UI + OT&E + tests).
- **Nota positiva:** la doc canónica (contract/ADR-031) es **honesta** (lo declara backlog); el overclaim está solo en el copy de la UI. La abstracción ya está lista → es un plugin nuevo, sin tocar billing core.

---

## 6. Qué falta para LIVE — lista priorizada y deduplicada

> Severidad = post-verificación adversarial. "Necesidad" distingue lo *alarmante/oculto* (severidad) de lo *obligatorio antes de clientes aunque sea trabajo esperado* (deploy). Estimaciones aproximadas.

### TIER 0 — Sin esto no hay negocio
| ID | Sev | Qué | Est. |
|----|-----|-----|------|
| **GL-1** | 🔴 critical | **Pago real (Stripe)** + superficie cliente "Pagar factura" + webhook + conciliación. Ver §5. | 3-5 sem |

### TIER 1 — Obligatorio antes de exponer clientes (seguridad · datos · legal · dinero)
| ID | Sev | Qué | Evidencia | Est. |
|----|-----|-----|-----------|------|
| **GL-2** | 🟠 high | **Resource/billing leak:** `deprovisionAsAdmin` marca `cancelled` pero **no llama `plugin.deprovision()`** → hosting/dominio sigue vivo y facturable en el proveedor. | `provisioning.service.ts:1105-1127` (DC.46) vs `enhance.plugin.ts:545` | ~1 sesión |
| **GL-3** | 🟠 high | **Support: fuga de notas internas por WebSocket.** `SupportWebsocketListener` broadcastea el payload completo (`is_internal:true`+body) a la room del cliente; solo el email filtra. Rompe SUPP-INV-3. | `support-websocket.listener.ts:48-53`; `support.gateway.ts:327-334` | 1-2 h |
| **GL-4** | 🟠 high | **Seed superadmin con password por defecto en prod.** `'AeliumDev2026!'` se siembra SIEMPRE (antes del guard `NODE_ENV=production`). | `prisma/seeds/test-accounts.ts:62-68,179-185` | S (fail-fast) |
| **GL-5** | 🟠 high | **RGPD:** portal transparencia ~20% de ADR-010 (sin export de datos, subprocesadores, consentimiento, supresión) **+** `audit_change_log` nunca se purga (PII ilimitada; el cron solo purga `access_log`). | `dashboard/transparency/page.tsx`; `audit-retention.cron.ts:31-49`; ADR-010:51,75-99 | 3-5 d |
| **GL-6** | 🟠 high | **ResellerClub: smoke OT&E real pendiente** (gate IP/WAF + NS sin registro A) — todo validado solo contra mock, shapes "conservadores". **+** `.es/.eu` pasa el gate y **cobra**, pero envía contacto WHOIS genérico sin NIF/residencia → fallaría tras el cobro. | `resellerclub.plugin.ts:413-414`; `resellerclub-customers.service.ts:306-327`; `resellerclub-ote-findings.md:14-18` | 1-3 d + 1-2 d |
| **GL-7** | 🟠 high | **Enhance: cluster live no provisionado + shapes nunca validados** contra orchd real (todo "smoke real" fue contra `MockEnhanceServer`). Sin el cluster (PowerDNS + ns1/ns2.aelium.net), los dominios a NS Aelium darían SERVFAIL. | `client.integration.spec.ts:23`; `admin-plugins-enhance.md §6/§8`; ADR-083 ctx:14 | 1-2 d + infra |
| **GL-8** | 🟡 med | **BillingProfile borrado rompe integridad fiscal:** FK `invoices.billing_profile_id` con `ON DELETE SET NULL` → borrar un perfil referenciado desvincula la factura de su snapshot fiscal (CLI-INV-5 documentada pero no implementada). | `clients-billing.service.ts:121-135`; `schema.prisma:704,730` | S |
| **GL-9** | 🟡 med | **Kill-switch de email no honrado:** `notifications.email_enabled_globally` es editable pero ningún código lo lee → control de seguridad/compliance falso. | `email.channel.ts:26-28`; `settings-catalog.ts:317-323` | 0.5-1 d |

### TIER 2 — Obligatorio para operar (infra · ops · escala)
| ID | Sev (necesidad) | Qué | Evidencia | Est. |
|----|-----|-----|-----------|------|
| **GL-10** | 🟡 med (🔴 necesidad) | **No existe stack de producción:** sin Dockerfiles, sin `docker-compose.prod`, sin Traefik/SSL, sin pipeline de deploy. P-DEPLOY.1 sin empezar. | `ls docker/` → solo `docker-compose.dev.yml`; sin Dockerfile | 2-3 sesiones |
| **GL-11** | 🟠 high | **Crons sin leader election:** ~10 `@nestjs/schedule` corren en toda réplica; los de billing/service-lifecycle **mutan dinero/estado** → facturas y suspensiones **duplicadas** al escalar. El flag `DISABLE_CRON_WORKERS` que la doc cree que los gobierna es **código muerto**. | `billing-lifecycle.worker.ts:35,57,170`; `service-lifecycle.worker.ts:47,125,174`; grep `DISABLE_CRON_WORKERS` en `src` → 0 | 1-2 sesiones |
| **GL-12** | 🟠 high (necesidad) | **SMTP real + deliverability (SPF/DKIM/DMARC).** Hoy MailPit / `MAIL_TRANSPORT=console` → **ningún email sale** (verificación, facturas, avisos de expiración, reset). | `.env.example:39`; `email.service.ts:25-49` | 0.25 d + cuenta |
| **GL-13** | 🟡 med (🔴 necesidad) | **Backups + recovery (RTO/RPO) inexistentes** (P-DEPLOY.2): sin scripts, sin cron, sin managed DB/Redis, sin runbook. | grep `backup\|pg_dump\|RTO\|RPO` → 0 | 0.5-1 d + hosting |
| **GL-14** | 🟡 med (necesidad) | **Observabilidad apagada:** Sentry sin DSN **y** Pino (Capa 1 de ADR-007) no cableado → ciego en prod entre deploy y activación. | `instrument.ts:21-23`; grep `nestjs-pino\|useLogger` → 0 | 0.1 d (Sentry) / 0.5 d (Pino) |
| **GL-15** | 🟠 high | **Support WS sin Redis adapter:** `connectedUsers` es un `Map` por proceso, adapter Socket.io in-memory → no escala (rompe R6); el chat **guest** (solo WS, sin fallback REST) se rompe en multi-instancia. | `support.gateway.ts:39`; grep `redis-adapter` → 0 | 0.5-1 d |
| **GL-16** | 🟡 med | **Secrets de prod + validación de env al boot.** Generar nuevos `JWT_SECRET`/`ENCRYPTION_KEY`/etc.; `ConfigModule` sin `validationSchema` → arranca con config rota/placeholder. | `app.module.ts:54-57`; `.env.example:20-21` | 0.25 d |
| **GL-17** | 🟡 med | **`service.*` fuera de Outbox (R8):** activated/cancelled/suspended/unsuspended/resumed con `emit()` fuera de tx → pérdida silenciosa de side-effects cross-módulo (zona DNS, cierre de tasks, notif de suspensión). | `provisioning.service.ts:1133,1381,1570`; `provisioning-orchestrator.service.ts:553` | 2-3 d |
| **GL-18** | 🟡 med | **Crons de billing sin gate de automatización:** `markAsOverdue` automático sin flag de producción → en live, datos mal generados producirían emails de impago y suspensiones reales. | `billing-lifecycle.worker.ts:35-53` | 2-3 d |
| **GL-19** | 🟡 med | **Endurecimiento de borde:** Swagger `/api/v1/docs` sin auth en prod; CORS/WAF `/admin`; cierre de aliases REST legacy (ADR-068); flag `THROTTLER_DISABLED` peligroso. | `main.ts:64`; ADR-068 §3 | 1 sesión |
| **GL-20** | 🟡 med | **Rotación de `ENCRYPTION_KEY` = brick de todos los secrets** (sin re-cifrado ni runbook). | `secret-vault.service.ts:83-90`; ADR-080:271-275 | 1-2 sesiones |

### TIER 3 — Funcional / UX para profesionalidad v1
| ID | Sev | Qué | Est. |
|----|-----|-----|------|
| **GL-21** | 🟡 med | **Sin gestión de cuentas de staff/agentes** (alta/baja/rol → solo BD). Offboarding manual = riesgo operativo y de seguridad. `users.controller.ts` solo `GET`. | 1-2 sesiones |
| **GL-22** | 🟡 med | **Overview role-aware no se muestra al staff:** `AdminStats`/`AgentStats` son código muerto (el staff aterriza en `/admin` estático; `/dashboard` los redirige). | 0.5-1 sesión |
| **GL-23** | 🟡 med | **Support-inside:** la guía (admin.md/client.md) publica precios/slots/canales **≠ seed** (49€/99€·3/10 vs 39€/79€·1/1; inventa canal "in-app") **+** upgrade de plan rechaza 400 **+** vende WhatsApp/phone sin dispatcher. *Decidir oferta comercial real y alinear.* | bajo (doc) + medio (upgrade) |
| **GL-24** | 🟡 med | **Products:** sistema de **extras (ADR-020) es código muerto** (escribible, nunca leído en checkout, sin UI) — incluido "dominio gratis con hosting anual"; **categorías** sin UI admin. *Decidir si entran en v1 o se recortan.* | M-L |
| **GL-25** | 🟡 med | **Notifications:** support viola **D12** (emails con HTML inline `${...}` sin escapar → riesgo de inyección, sin plantilla editable ni campana); canales whatsapp/push expuestos sin implementación. | 1-2 d |

### TIER 4 — Red de seguridad (tests) y fuente de verdad (doc)
| ID | Sev | Qué | Est. |
|----|-----|-----|------|
| **GL-26** | 🟡 med | **Cobertura de tests:** comercio de dominios (15D/15D.II) + Sprint Cuenta **sin E2E**; `auth-login.service` (login/lockout/2FA) y `ClientsBillingService` (anti-IDOR de billing-profiles) **sin spec**; frontend cero red en-PR (E2E solo post-merge). | 2-4 sesiones |
| **GL-27** | 🟡 med | **DS sin enforcement mecánico:** lint no aplica R15/R16/D1 → 603 inline styles, `api.ts` 2197 LOC (5.5×), 4 páginas con bypass de tabla cruda (error-log, jobs/failed, transparency, PluginOperationalOverview). | 0.5-1 d + triage |
| **GL-28** | 🟡 med | **Drift documental:** `_events.md` ("fuente única") con contadores rotos (38/28 vs ~52 reales) y 3 eventos sin catalogar; contadores ADR stale (60/84 vs 85); faltan `contract.md` de **domains** y **account**; guías que sobre-afirman (auth localStorage, services deprovision, enhance "smoke real"). | 1-2 sesiones |

---

## 7. Notas de deduplicación (hallazgos que aparecen en varios agentes)

- **Pago real** lo reportaron de forma independiente billing, support-inside, seguridad, deploy, dinero y testing → consolidado en **GL-1** (confirmado critical por 2 verificadores).
- **`service.*` fuera de Outbox** lo reportaron provisioning y eventos → **GL-17**.
- **Crons sin leader election** lo reportaron deploy y dinero → **GL-11/GL-18** (separados: leader election ≠ gate de automatización).
- **Smoke contra proveedor real** lo reportaron domains, enhance y provisioning → **GL-6/GL-7**.
- **Topbar lee `localStorage('token')`** (MEDIUM-4 del audit previo, **sigue sin corregir**): es *dead code* sin fuga real, pero R17 violado y badge de soporte roto. Lo trato como deuda menor dentro de GL-22/seguridad, no como blocker propio.

---

## 8. Camino a producción — orden recomendado

1. **Sprint Stripe (GL-1)** — el bloqueante absoluto; todo lo demás puede paralelizarse después. Cierra el ciclo de caja + reactiva el ciclo de impago + da sentido a support-inside.
2. **Bloque hardening pre-cliente (GL-2..GL-9)** — barato y de alto valor: resource leak, fuga WS, seed password, kill-switch, integridad fiscal, RGPD. ~1 sprint.
3. **Verificación de proveedores reales (GL-6, GL-7)** — gate de IP/whitelist + provisionar cluster Enhance + correr los smokes que ya están escritos. Refinar shapes divergentes. *Depende de infra/Yasmin.*
4. **Sprint deploy P-DEPLOY (GL-10..GL-16, GL-19)** — stack prod, leader election, SMTP, backups, observabilidad, secrets. Es el gate ADR-069: se activa con trigger de negocio, pero su contenido ya está mapeado aquí.
5. **Pulido funcional/UX (GL-21..GL-25)** — decidir alcance v1 de extras/categorías/oferta SI; gestión de staff; overview operativo.
6. **Red de seguridad + doc (GL-26..GL-28)** — E2E del comercio, specs de auth/IDOR, enforcement DS, reconciliar doc.

**Estimación gruesa a "live mínimo viable con clientes reales":** Stripe (3-5 sem) + hardening Tier 1 (~1 sprint) + deploy Tier 2 (~1-2 sprints) + verificación de proveedores (días, gated por infra). Tiers 3-4 pueden solaparse o seguir post-lanzamiento controlado.

---

## 9. Fortalezas confirmadas (no todo es deuda)

- **Plugin framework** (ADR-080): vault AES-256-GCM, loader DB con reload, circuit breaker tras interface, UI dinámica @rjsf — **fully-implemented**.
- **Orquestador de provisioning**: el módulo mejor construido (ciclo `invoice.paid`→BullMQ/DLQ→provision→activación, wrappers cross-cutting, punto único de transición, IDOR resuelto).
- **Plugin Enhance**: implementación que **supera** su doc (6 métodos + 11 inline actions + SSL/quota-alerts no documentados) — under-documented, no over-claimed.
- **Billing core**: FSM con guards, BILL-INV (cero `invoice.delete` en todo el backend), prorrateo ADR-029 e2e con `credit_balance`, Outbox en todas las transiciones de dinero.
- **Invariantes de dinero de dominios** (DOM-INV-1/2/3): exactly-once, margin guard same-currency, renovación verificada — en código **y** test.
- **Seguridad**: los 3 HIGH previos cerrados y verificados; cripto y auth (Modelo A, R17) sólidos.
- **Tests backend**: 90 suites + 10 de integración contra Postgres real (concurrencia/dinero) — activo genuino.
- **Honestidad de stubs**: infrastructure/KB/promotions/partner declaran su no-implementación sin overclaim.

---

## 10. Nota metodológica

- **2 corridas** (la 1ª con 1 dimensión caída por corte de conexión, recuperada vía resume con caché): ~186 sub-agentes en total, ~9,6 M tokens, ~30 min de wall-clock combinado.
- **Verificación adversarial**: cada `critical`/`high` fue sometido a refutación independiente (escéptico por defecto). Resultado: **1 hallazgo se mantuvo `critical`** (pago, doble confirmación), la mayoría de los `high` se confirmaron o **bajaron a `medium`** al comprobarse mitigación o diferimiento consciente; **0 hallazgos refutados como falsos** (todos los reportados son reales; lo que cambió fue la severidad).
- Toda afirmación está respaldada por evidencia de primera mano (`ruta:línea` o salida de grep) recogida durante el run.
- **Snapshot:** medido en `sprint-cuenta-perfil`. Al mergear Sprint Cuenta a `master`, los hallazgos transfieren íntegros (Sprint Cuenta no toca pago/infra/proveedores).
