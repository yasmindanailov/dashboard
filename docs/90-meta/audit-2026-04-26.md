# Auditoría código vs documentación — 2026-04-26

> **Objetivo:** detectar drift entre lo que dice la doc (estado deseado) y lo que hay en código (estado real) **antes** de refactorizar el roadmap (F6). Sin verdad sobre el estado actual, el roadmap nuevo hereda mentiras.

> **Alcance:** 3 ejes ejecutados con sub-agentes en paralelo + verificación manual de hallazgos críticos.
> **Severidad global:** 🟡 **MODERADA** — drift puntual pero no estructural. Sprint 8 deuda confirmada. Schema drift en `conversations` y `services` heredado del monolítico.

---

## Resumen ejecutivo

| Eje | Drift | Severidad | Acción |
|-----|-------|-----------|--------|
| **Schema (Prisma vs `docs/30-data/`)** | Enum + campos divergentes en 2 tablas core | 🔴 Grave | Fix antes de F6 |
| **API endpoints (contracts vs código)** | Sin drift real (verificación manual desmiente reporte inicial) | 🟢 Cero | — |
| **Eventos** | 100% alineado (25/25) | 🟢 Cero drift | — |
| **Sprint 8 (Tasks) WIP** | Listener `task.assigned` ausente, validación `assigned_to` ausente | 🔴 Grave | Cerrar antes de avanzar |
| **Sprint 7 status declaration** | Header dice ⬜ pero ~95% pasos ✅ | 🟡 Moderada | Fix indicador en F6 |
| **Indicadores ✅/⬜ en F3** | 4 tablas marcadas ⬜ que sí existen en Prisma | 🟡 Moderada | Fix en mismo PR |
| **F0 salvaguardas (7)** | 6/7 activas, lint no bloqueante (esperado, F0.6 pendiente) | 🟡 Conocido | F0.6 según playbook |
| **Outbox Pattern (R8)** | 0/25 eventos lo usan; crítico para `invoice.*` | 🔴 Grave | Pre-producción obligatorio |

**Conclusión global:** la doc es **buena pero no perfectamente fiel al código**. Los problemas concentran en 3 áreas concretas (`conversations` enums, `services` campos, headers Sprint 7/7.5) y se arreglan en 1-2 sesiones. El sistema es robusto en arquitectura — el drift es **superficial-textual**, no estructural.

---

## Eje 1 — Schema drift: Prisma vs `docs/30-data/`

### 1.1 Modelos en Prisma (26 totales, verificado manualmente)

```
Role · User · Session · EmailVerification · PasswordReset
ClientProfile · BillingProfile
Setting
ProductCategory · Product · ProductPricing · ProductExtra · ProductChecklistItem
Service · Invoice · InvoiceItem
Conversation · ClientNote · Message
Task
Notification · Server · EventOutbox · ErrorLog
AuditAccessLog · AuditChangeLog
```

### 1.2 🔴 Drift GRAVE: enums + campos de `Conversation`

**`docs/30-data/support.md`** (heredado del monolítico legacy) **diverge gravemente** de `backend/prisma/schema.prisma`:

| Campo | `support.md` dice | Prisma real | Severidad |
|-------|-------------------|-------------|-----------|
| `type` | `realtime_chat` · `async` | **`chat` · `ticket`** | 🔴 enum nombres distintos — queries del frontend romperían si confiaran en doc |
| `status` | `active`, `waiting_agent`, `waiting_client`, `resolved`, `closed` | **`open`**, `waiting_client`, `waiting_agent`, `resolved`, `closed` | 🔴 `active` no existe en código (es `open`) |
| `priority` | `low`, **`medium`**, `high`, `urgent` | `low`, **`normal`**, `high`, `urgent` | 🟡 `medium` → `normal` |
| `channel` | enum `webchat`/`whatsapp`/`email` | string libre con default `"web"` | 🟡 |
| `assigned_to` (FK) | así | **`assigned_agent_id`** | 🟡 nombre divergente |
| `parent_conversation_id` | así | **`escalated_from_id`** | 🟡 nombre divergente |
| `ai_handled` | así | **`is_ai_filtered`** | 🟡 nombre divergente |
| `source` | enum (`landing` / `dashboard` / `escalated_from_chat`) | **NO existe en Prisma** | 🟡 doc se adelanta |
| `has_support_inside` | así | **NO existe en Prisma** | 🟡 doc se adelanta |
| `ai_summary` | así | **NO existe en Prisma** | 🟡 doc se adelanta |
| `anonymized_at` | así | **NO existe en Prisma** | 🟡 RGPD pendiente — doc anticipa |
| **(Prisma extra)** | — | `sequence_number` (ticket numbering), `category`, `tags` (jsonb), `first_response_at`, `resolution_note`, `resolved_by_id`, `metadata`, `guest_session_hash` | 🟡 doc no los menciona |

**Origen del drift:** el monolítico `DATABASE_SCHEMA.md` ya tenía el drift; F3 lo copió literal sin verificar contra Prisma. **La fuente de verdad es Prisma.**

### 1.3 🔴 Drift GRAVE: campos de `Service`

| Campo | `billing.md` dice | Prisma real | Severidad |
|-------|-------------------|-------------|-----------|
| `subdomain` + `custom_domain` | dos campos separados | **un solo campo `domain`** | 🔴 doc dice 2 campos, código tiene 1 |
| `provisioner_reference` | así | **NO existe** (solo `provisioner_data` jsonb) | 🟡 doc se adelanta |
| `ssl_expires_at` | así | **NO existe** | 🟡 doc se adelanta |
| `provisioned_at` | así | **NO existe** | 🟡 |
| `next_renewal_at` | así | **`next_due_date` + `next_invoice_date`** (2 campos) | 🟡 nombres + granularidad distinta |
| `paused_until` | así | **`pause_max_date`** | 🟡 nombre |
| `failure_reason` | así | **NO existe** | 🟡 |
| `resource_config` | así (jsonb) | **NO existe** (solo `provisioner_data` jsonb) | 🟡 |
| **Prisma extra** | — | `label`, `billing_cycle`, `amount`, `currency` (NO en doc) | 🔴 doc no los menciona |
| **Service status enum** | sin `terminated` | **incluye `terminated`** | 🟡 enum value extra en código |

### 1.4 🟡 Drift de indicadores ✅/⬜ en F3

Cuatro tablas marcadas como **⬜** en `docs/30-data/` que SÍ existen en Prisma:

| Archivo F3 | Tabla | Indicador actual | Debería ser |
|------------|-------|------------------|-------------|
| `infrastructure.md` | `servers` | ⬜ | ✅ stub (existe `Server` en Prisma) |
| `system.md` | `error_log` | ⬜ | ✅ stub (existe `ErrorLog` en Prisma) |
| `audit.md` | `audit.access_log` | ⬜ | ✅ stub (existe `AuditAccessLog`) |
| `audit.md` | `audit.change_log` | ⬜ | ✅ stub (existe `AuditChangeLog`) |

### 1.5 Tablas correctamente marcadas como ⬜ (45 pendientes)

**Para que conste:** los siguientes ⬜ son correctos (no existen en Prisma todavía, encajan con plan de sprints):

`client_consents`, `client_folders`, `client_service_folders`, `client_service_tags`, `docker_templates`, `support_inside_config`, `service_checklist_items`, `subscriptions`, `provisioning_log`, `billing_credits`, `payments`, `support_inside_subscriptions`, `support_inside_slots`, `task_checklist_completions`, `maintenance_logs`, `server_pools`, `server_metrics`, `notification_templates`, `knowledge_base_articles`, `knowledge_base_tags`, `integrations_registry`, `audit.integration_log`, `audit.service_log`, todas las del módulo `partner` (9), `referrals` (3), `projects` (4), `promotions` (6).

---

## Eje 2 — API endpoints + eventos

### 2.1 🟢 Eventos: 100% sincronizados

- 25 eventos catalogados en `docs/20-modules/_events.md`.
- 25 emisores reales en código (`eventEmitter.emit`).
- 10 listeners reales coinciden con los 10 documentados con consumidor activo.
- 15 huérfanos esperados (auth.* esperan módulo `audit`; service.* esperan `provisioning`; task.* Sprint 8).
- **Cero eventos emitidos sin documentar. Cero documentados sin emisor.**

Catálogo `_events.md` es **fuente de verdad fiable**.

### 2.2 🟢 Falso positivo del agente — `support` contract completo

**Verificación manual posterior:** los 4 endpoints que el agente reportó como ausentes **SÍ están documentados** en `docs/20-modules/support/contract.md` §5:

| Endpoint en código | Línea en contract |
|--------------------|---------------------|
| `GET /support/chats` | L53 ✅ presente |
| `GET /support/tickets` | L55 ✅ presente |
| `PATCH /support/conversations/:id/assign` | L59 ✅ presente |
| `POST /support/chats/guest` | L52 ✅ presente (con nota "rate-limited 3/h por IP") |

**Lección:** los reportes de sub-agentes deben verificarse manualmente antes de actuar. Drift cero en este punto.

### 2.3 🟡 Drift moderado: `billing` transitions

- **Contract dice:** endpoints separados `PATCH /billing/invoices/:id/finalize`, `/cancel`, `/refund`.
- **Código real:** sí existen como rutas separadas (verificado por agente API). Sin drift real, solo confirmar.

### 2.4 🟢 Drift menor en otros módulos

- `auth`: contract no lista `GET /auth/me` (existe en código).
- `clients`: contract no lista `PATCH /clients/:id/billing-profiles/:profileId/default` (existe).
- `tasks`, `products`: 100% match.

---

## Eje 3 — Sprint status: declarado vs real

### 3.1 🟡 Drift en headers de sprint (declaración engañosa)

| Sprint | Header en ROADMAP.md | Realidad | Acción |
|--------|----------------------|----------|--------|
| **Sprint 7** | ⬜ "(en progreso)" | ~95% pasos ✅ (R15 cerrado, hardening cerrado, chat anónimo cerrado, notas estructuradas cerradas). Solo pendientes 7.6.x horario, 7.7 attachments (bloqueado MinIO), 7.8/7.9 IA (bloqueado Sprint 15), 7.SI (Support Inside) | Cambiar header a 🔄 con detalle de qué bloquea |
| **Sprint 7.5** | ⬜ "Design System Foundation" | Fase 1 completa: D1-D11 todos ✅. Sigue ⬜ porque Fase 2 (migración páginas) parcial | Mantener ⬜ pero clarificar Fase 1 ✅ / Fase 2 🔄 |
| **Sprint 8** | 🔄 "Tasks" | Confirmado WIP. Fase A 50% (8.1 ✅, 8.1b/c/d/14 ⬜), Fase B 30%, C/D/E 0% | Header correcto. Faltan 5 sub-pasos para Fase A. |

### 3.2 🔴 Sprint 8 deuda confirmada

| Punto | Verificado HOY |
|-------|----------------|
| Listener `task.assigned` | ❌ **AUSENTE** (grep `@OnEvent('task.assigned')` = 0 matches) |
| Validación FK `assigned_to` | ❌ **AUSENTE** (acepta sin verificar que user existe) |
| Lint `no-unsafe-enum-comparison` | ✅ **ARREGLADOS** (eran 2; hoy 0). Pequeña victoria desde la última declaración. |
| Tests E2E para tasks | ❌ Sin cobertura (0 specs específicos) |

**Conclusión:** Sprint 8 NO ha empeorado pero tampoco se ha cerrado. **Sigue bloqueando** la cadena de sprints siguientes (Sprint 9 depende de Sprint 8 cerrado para listeners).

### 3.3 🟢 Sprints 0-6: cero drift

Auditoría confirma que los 8 sprints declarados completos (0, 1, 2, 3, 3.5, 4, 5, 6) **están realmente implementados**. No hay sorpresas, no hay TODOs disfrazados.

### 3.4 9 TODOs en código (todos esperados)

- 8 en módulos stub (`audit`, `error-log`, `notifications`, `infrastructure`, `knowledge-base`, `promotions`, `provisioning`) — conforme a "8 módulos stub sin implementación" del playbook §1.
- 1 en `subscription.controller.ts` ("resolve userId from JWT token") — Sprint 8 endpoint en desarrollo.
- 1 en `dashboard.service.ts` ("settlement_schedule" hardcoded null) — feature incompleto, **no documentado como deuda**.

**Único TODO no documentado:** `dashboard.service.ts:next_settlement = null` hardcoded. Severidad: 🟡 menor.

### 3.5 F0 salvaguardas (7) — verificación

| # | Salvaguarda | Estado | Notas |
|---|-------------|--------|-------|
| 1 | Typecheck (`pnpm typecheck`) | ✅ Bloqueante en pre-push | OK |
| 2 | Build (NestJS + Next.js) | ✅ CI bloquea | OK |
| 3 | Tests unitarios (Jest) | ⚠️ Parcial | 6 specs; faltan tasks |
| 4 | Tests E2E (Playwright) | ⚠️ Parcial | 7 tests; faltan tasks + dashboard |
| 5 | Lint (ESLint + Prettier) | ⚠️ NO bloqueante en CI | Esperado por F0.6 pendiente |
| 6 | Hooks pre-commit (husky + lint-staged) | ✅ Activo | OK |
| 7 | Hooks pre-push (typecheck) | ✅ Activo | OK |

CI: 3 jobs verdes. Sistema robusto pero cobertura tasks insuficiente.

### 3.6 Outbox Pattern (R8) — deuda crítica confirmada

- 0/25 eventos usan Outbox.
- **Crítico para `invoice.*`** (4 eventos, riesgo legal/financiero).
- Sprint 9 lo planifica pero está después de Sprint 8.
- **Bloquea** despliegue real a producción (pérdida potencial de notificaciones de cobro).

---

## Acciones priorizadas

### 🔴 GRAVES (fix antes de F6 — esta misma sesión o PR aparte)

| # | Acción | Esfuerzo | Justificación |
|---|--------|----------|---------------|
| 1 | **Corregir `docs/30-data/support.md`** — alinear enums (`type`, `status`, `priority`) y campos de `Conversation` con Prisma real | 30 min | Doc engaña al lector hoy |
| 2 | **Corregir `docs/30-data/billing.md`** — alinear campos de `Service` con Prisma (un solo `domain`, eliminar `subdomain`/`custom_domain`/`ssl_expires_at`/`provisioned_at`/`failure_reason`/`resource_config`/`provisioner_reference`; añadir `label`, `billing_cycle`, `amount`, `currency`; añadir `terminated` al enum) | 30 min | Doc engaña al lector hoy |
| 3 | **Actualizar 4 indicadores ⬜→✅** en F3 (`servers`, `error_log`, `audit.access_log`, `audit.change_log`) | 5 min | Indicador erróneo |
| 4 | ~~Actualizar `docs/20-modules/support/contract.md` §5~~ — **descartado:** verificación manual confirmó que los 4 endpoints YA están documentados (falso positivo del agente) | — | N/A |
| 5 | **Cambiar header Sprint 7** a 🔄 con leyenda "core + hardening + R15 ✅; pendientes: 7.6 horario, 7.7 attachments (bloq MinIO), 7.8/7.9 IA (bloq Sprint 15), Support Inside (bloq Sprint 8)" | 5 min | Header engañoso |
| 6 | **Cerrar Sprint 8** — listener `task.assigned` + validación FK `assigned_to` + tests E2E tasks | 1-2 sesiones | Deuda activa, bloquea cadena |
| 7 | **Outbox Pattern para `invoice.*`** | 1-2 sesiones | Crítico pre-producción |

### 🟡 MODERADAS (trasladar a F6 como deuda priorizada)

| # | Acción | Esfuerzo | Justificación |
|---|--------|----------|---------------|
| 8 | **F0.6 saneamiento lint** — resolver ~344 errores `no-unsafe-*`, hacer lint bloqueante en CI | 3-4 sesiones | Salvaguarda 5 incompleta |
| 9 | **Tests E2E para Sprint 8** | 1 sesión | Cobertura crítica antes de despliegue |
| 10 | **Documentar `dashboard.service.ts:settlement_schedule`** (TODO hardcoded `null`) | añadir a roadmap | Feature incompleto sin documentar |
| 11 | **Refactorizar diff de nomenclatura** (`assigned_to` ↔ `assigned_agent_id`, `parent_conversation_id` ↔ `escalated_from_id`, `ai_handled` ↔ `is_ai_filtered`) — decidir nombre canónico y unificar doc + código | 1 sesión | Cohesión semántica largo plazo |
| 12 | **Sprint 7.5 Fase 2** — migración de páginas restantes a Design System | sprint dedicado | Empezó pero no cerró |
| 13 | **Mejorar contract de `auth`, `clients`** con endpoints menores faltantes | 15 min | Contract corto |

### 🟢 MENORES (parking lot / F7-F9)

| # | Acción | Justificación |
|---|--------|---------------|
| 14 | Considerar generar `docs/30-data/*.md` desde Prisma automáticamente con script | Eliminaría drift futuro |
| 15 | Añadir comentarios `///` en Prisma para descripciones de campos (renderiza en `prisma generate`) | Auto-doc |
| 16 | Revisar excepciones Regla 15 documentadas (permissions.ts, etc.) | Cosmético |

---

## Input para F6 (roadmap nuevo)

Hallazgos de esta auditoría que deben **reflejarse explícitamente** en el roadmap nuevo (`docs/60-roadmap/`):

### Sprint actual (estado real verificado)

- **Sprint 0–6**: ✅ cerrados (verificado).
- **Sprint 7**: 🔄 core + hardening cerrado, faltan 5 features bloqueadas por dependencias (MinIO, Sprint 15 IA, Sprint 8 Support Inside).
- **Sprint 7.5**: 🔄 Fase 1 (componentes DS) ✅; Fase 2 (migración páginas) parcial.
- **Sprint 8**: 🔄 WIP — Fase A 50%, Fases B-E pendientes. Bloquea Sprint 9 (notifications listeners) y cadena de fases.

### Backlog priorizado (output de la auditoría)

#### P0 — Bloqueantes pre-producción

1. Cerrar Sprint 8 (listener task.assigned + validación assigned_to + tests E2E).
2. Outbox para `invoice.*` (R8 crítica).
3. F0.6 saneamiento lint (Salvaguarda 5 incompleta).

#### P1 — Importantes

4. Sprint 7.5 Fase 2 (migración páginas DS).
5. Sprint 9 Audit + Notifications Full (depende de 1).
6. Sprint 14 Deploy (requiere todo P0 cerrado).

#### P2 — Funcionales

7. Sprint 10 Infrastructure → Sprint 11 Provisioning.
8. Sprint 12 Settings + KB.
9. Sprint 12.5 RGPD.

#### P3 — Crecimiento (Fase 2)

10. Sprint 15 Plugins (Stripe + provisioners).
11. Sprint 18 Landing Integration.
12. Resto según orden actual del ROADMAP.

#### P-Continua — Deuda no urgente pero importante

- Drift de nomenclatura (assigned_to vs assigned_agent_id, etc.).
- Considerar auto-gen de schema docs.
- Refactor R15 restantes documentados.

---

## Decisiones pendientes (para Yasmin)

Antes de cerrar este PR de auditoría y arrancar F6:

### 1. ¿Arreglo el drift schema en F3 (acciones #1, #2, #3) en este mismo PR?

- **A favor:** drift grave hoy, doc engaña al lector. Esfuerzo moderado (~1h). Cierra el círculo de F3.
- **En contra:** mezcla auditoría + fix en mismo PR (puede ser preferible PR aparte para revisión más limpia).

**Mi recomendación:** sí, en este PR. La auditoría sin arreglo no aporta — el agente siguiente leerá doc errónea. Es trabajo claramente delimitado.

### 2. ¿Arreglo el drift de support contract (acción #4) y header Sprint 7 (acción #5)?

- Esfuerzo: 20 min total.
- **Mi recomendación:** sí, en mismo PR.

### 3. ¿Cerramos Sprint 8 ahora o lo trasladamos al roadmap nuevo?

- Esfuerzo: 1-2 sesiones.
- **Mi recomendación:** **trasladar al roadmap nuevo** como P0 #1. Esta sesión es de doc, no de código de negocio. Sprint 8 merece su propia sesión enfocada con tests E2E.

### 4. ¿Outbox para `invoice.*` ahora o trasladar?

- Esfuerzo: 1-2 sesiones.
- **Mi recomendación:** trasladar al roadmap nuevo como **P0 #2** (después de Sprint 8, antes de Sprint 14).

---

## Cómo se hizo esta auditoría (reproducible)

Tres sub-agentes en paralelo (≈3 min total):

1. **Schema drift:** comparar `backend/prisma/schema.prisma` (26 modelos, 23 enums) vs `docs/30-data/*.md` (14 archivos). Verificación manual de 2 modelos críticos (`Conversation`, `Service`) tras reporte del agente.
2. **API/contract drift:** extraer endpoints de cada `*.controller.ts` y compararlos con tabla de endpoints en `docs/20-modules/<modulo>/contract.md`. Grep `eventEmitter.emit` y `@OnEvent` para auditar eventos.
3. **Sprint status:** leer `docs/ROADMAP.md` + `docs/90-meta/development-playbook.md`, verificar declaraciones contra git log + presencia de archivos + grep TODOs/FIXMEs.

Reproducible: cualquier futuro agente puede repetir la auditoría con los prompts en `git log` de este commit.

---

## Documentos relacionados

- [`docs/90-meta/development-playbook.md`](./development-playbook.md) — playbook (referencia de estado declarado).
- [`docs/ROADMAP.md`](../ROADMAP.md) — sprints (origen de drift de headers).
- [`docs/30-data/`](../30-data/) — schema partido por dominio (afectado por drift).
- [`docs/20-modules/_events.md`](../20-modules/_events.md) — catálogo de eventos (verificado fuente de verdad).
- [`docs/20-modules/support/contract.md`](../20-modules/support/contract.md) — contract con endpoints faltantes.
- `backend/prisma/schema.prisma` — fuente de verdad implementada del schema.
