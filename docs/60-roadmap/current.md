# Sprints en curso — Aelium Dashboard

> **Estado real verificado** contra código en auditoría 2026-04-26 + closures Sprint 8 / 9 / 9.5 / 9.6 / 11.5 (2026-04-26 → 2026-05-01) + Sprint 11 Fases A+B (2026-05-01/02). Cualquier sprint listado aquí está parcialmente avanzado (no es backlog puro — para eso ver [`backlog.md`](./backlog.md)). Los sprints ✅ que aparecen abajo son punteros a `completed/`; viven aquí solo para trazabilidad cronológica de la ola P1.1.

> **Última actualización:** 2026-05-13 — **Sprint 15C.II Hardening — Fase F.5 (`DC.44` billing-suspend-unify) cerrada y mergeada a master — PR [#72](https://github.com/yasmindanailov/dashboard/pull/72) squash-merge `72a8b0f`** (bypass policy §6, **7ª aplicación** — CI GitHub billing-bloqueada §A.9.10; las 3 condiciones cumplidas: motivo externo + `pnpm ci:check:full` verde [backend 51 suites, 691 passed + 5 skipped + `nest build`; frontend typecheck + lint + `next build`] + boot real backend verificado [`Nest application successfully started`, DI sin errores, `BillingModule`→`ProvisioningModule` sin ciclo, `ReactivateServicesOnInvoicePaidListener` registrado] + sección formal en el cuerpo del PR; rama temporal eliminada; label `ready-for-e2e`) → post-merge doc-sync (patrón #61/#64/#66/#68/#71). **Tema**: la suspensión por impago va ahora por el mismo camino canónico que la suspensión manual del admin — la BD y el proveedor no divergen para el caso impago, y se reactiva sola al pagar. Punto único de transición de estado. **Contenido**: (F.5.1) `ServiceLifecycleWorker.autoSuspendServices` (cron diario 03:00, retries agotados) ya no hace su propio `prisma.service.update(status:'suspended')` + emit directo; delega en `ProvisioningService.suspendAsAdmin(serviceId, {reason:'overdue_payment', internal_note:'Factura N', notify_client:true}, null, undefined, {actorLabel:'system:billing-overdue-cron', allowUnsupported:true})` — pasa por la inline action `suspend_service` del plugin (`patchSubscription({isSuspended:true})` en Enhance), `prisma.update`, `service.suspended` con la forma completa, audit, email; idempotente (no-op si ya suspended, 409 si cancelled — **arregla de paso** el bug del `prisma.update` crudo que revivía un servicio cancelado a suspended). (F.5.2) Convención "actor sistema" (taxonomía `system:<dominio>-<cron|job>`): `suspendAsAdmin`/`unsuspendAsAdmin` aceptan `actorUserId: string \| null` + `opts.actorLabel?`; `ExecuteActionContext` igual. Cuando `actorUserId === null`: el evento `service.suspended`/`service.unsuspended` lleva `actor` + `actor_user_id: null`; `audit_change_log.changes_after.actor` lo registra; **NO** se escribe `audit_access_log` (no hay "lectura staff" — solo cambio de estado). `notifications-on-service-suspended` simplificado: ya no hay forma legacy `{invoice_id, reason:'payment_exhausted'}` — `normalizeReason` queda como guarda defensiva. (F.5.3) Auto-reactivación al pagar: `ProvisioningService.reactivateSuspendedServiceOnPayment(serviceId)` reactiva **solo si** el servicio está `suspended` con motivo `overdue_payment` (un pago no deshace una suspensión por abuso/RGPD/mantenimiento; idempotente si ya `active`; vía `unsuspendAsAdmin` con actor `'system:billing-on-invoice-paid'`) + nuevo `ReactivateServicesOnInvoicePaidListener` (`@OnEvent('invoice.paid')` en `ProvisioningModule` — resuelve los `service_id` de la factura; degradación R7). Coexiste con `ProvisioningOrchestratorService.handleInvoicePaid` (aprovisiona servicios *nuevos* — concerns distintos, sin solapamiento práctico). **Decisión pre-código (L18 — dossier §A.11.10.2)**: `allowUnsupported: true` (un único punto de transición — `suspendAsAdmin`/`unsuspendAsAdmin` aceptan plugins `supports_suspend=false` para el caso impago: no hay inline action que invocar, solo la transición del lado de Aelium; para el caso humano se omite → 409 `SUSPEND_NOT_SUPPORTED`). **Refinamiento F.4.1** (la fase que lo necesita): `getInfoForUser` fuerza `info.status='suspended'` para **cualquier** plugin cuando `services.status` lo está (antes solo para `supports_suspend`) — un servicio `internal`/`manual` suspendido por impago el cliente lo ve suspendido; el flag `provider_state_desync` sigue gateado en `supports_suspend`. **Módulo**: `BillingModule` importa `ProvisioningModule` (sin ciclo — `ProvisioningModule` consume billing vía `@OnEvent`, no por import). **NO unificado** (deliberado — L18): `service.resumed` (flujo pause) ↔ `service.unsuspended` (conceptos distintos); `autoCancelServices` sigue con su `prisma.update` (migrarlo a `deprovisionAsAdmin` sería destructivo — DELETE en el proveedor — candidato a fase/`DC.NEW` aparte). **ADR-077 sin cambios** (no toca el contrato). Suite backend **691 passed + 5 skipped** (51 suites; +tests F.5: `provisioning.service.spec` +7 [suspend/unsuspend actor sistema sin `audit_access_log` + `metadata.actor` + `allowUnsupported` sin inline action + `reactivateSuspendedServiceOnPayment` ×4]; `service-lifecycle.worker.spec` NUEVO ×5; `reactivate-services-on-invoice-paid.listener.spec` NUEVO ×4; `notifications-on-service-suspended.spec` actualizado). Docs: `DC.44` materializado en `backlog.md` + dossier §A.11.10.2 (close-out + decisiones) + §A.11.1 (fila F.5 ✅) + nota del refinamiento F.4.1 en §A.11.10.1.1 + `_events.md` (`service.suspended`/`service.unsuspended` emisor único). Commit en la rama: `dbf5a3a`. **Próximo: F.6** — notas operativas vía `ClientNote` (`§A.11.10.3` — cancel/suspend/unsuspend manual → nota **obligatoria** en el modal → `ClientNote` `source_system='service'` + link; auto-suspend por impago → `ClientNote` auto; el motivo-enum se desacopla de la nota; supersede el `service_notes` table; migración split del `"<motivo>: <nota>"`; ADR-079 amendment). **Sprint 15D RC sigue BLOQUEADO** hasta cierre Sprint 15C.II (Fases F.6→F.12 + G). **Nota de proceso**: 7ª aplicación del bypass §6 — conviene que el desbloqueo de GitHub Actions tenga dueño/fecha (no debería volverse permanente). Earlier 2026-05-12 — **Sprint 15C.II Hardening — Fase F.4 (robustez del status de suspensión) cerrada y mergeada a master — PR [#70](https://github.com/yasmindanailov/dashboard/pull/70) squash-merge `283791c`** (bypass policy §6, **6ª aplicación** — CI GitHub billing-bloqueada §A.9.10; las 3 condiciones cumplidas: motivo externo + `pnpm ci:check:full` verde [backend prisma generate + typecheck + lint + 49 suites 674 passed + 5 skipped + `nest build`; frontend typecheck + lint + `next build`] + boot real backend verificado [ruta `POST /api/v1/admin/services/:id/resync-provider-state` mapeada, `Nest application successfully started`, DI sin errores] + sección formal en el cuerpo del PR; rama temporal eliminada; label `ready-for-e2e`) → post-merge doc-sync (patrón #61/#64/#66/#68). **Doctrina ([ADR-082 Amendment A1](../10-decisions/adr-082-modelo-domain-hosting-dns-doctrine.md#amendments) + [ADR-070 Amendment A1](../10-decisions/adr-070-service-info-sso-acciones-curadas.md#amendments); ADR-077 sin cambios)**: `services.status` es autoritativo para el lifecycle *administrativo* (suspended/cancelled — decisión de Aelium); DH-INV-6 ("el proveedor gana") aplica al estado *operacional* (plan/refs/métricas/zone) — dimensiones distintas. El reconcile cron adopta el drift operacional proveedor→Aelium; F.4 añade el complemento **read-time** + la remediación admin (realinear proveedor←Aelium, nunca al revés). **Contenido**: (a) `ProvisioningService.getInfoForUser` (capa orquestador, heredable; NO toca el contrato `ProvisionerPlugin` — el flag vive en el summary, no en `ServiceInfo`): si el plugin tiene `supports_suspend`, `services.status ∈ {active, suspended}`, el proveedor está accesible (`info.status ∉ {unknown, failed}`) e `info.status !== services.status` → marca `summary.provider_state_desync` + re-deriva `availableActions` desde el estado administrativo (vía `filterActionsByStatus`, **promovido** de `enhance.plugin.ts` privado a `core/provisioning/plugin-utils.ts` exportado); cuando Aelium lo tiene `suspended` fuerza `info.status='suspended'` (header/banner/badge coherentes, también si el proveedor reporta `cancelled`/`expired` por una desincronización — caso real: `MockEnhanceServer` in-memory reiniciado tras una suspensión deja la suscripción reportando `deleted`); cuando lo tiene `active` y el proveedor reporta algo más restrictivo, conserva el estado del proveedor (no baja severidad — el cliente no puede usarlo); proveedor caído (`unknown`/`failed`) → no afirma desync, el admin ve el `AdminDriftBanner` normal. No shortcircuita el plugin (a un servicio suspendido sí se le piden las métricas). (b) `ProvisioningService.resyncProviderStateAsAdmin` + endpoint `POST /admin/services/:id/resync-provider-state`: re-aplica la inline action canónica `suspend_service`/`unsuspend_service` para que el proveedor coincida con `services.status` — **sin transición de lifecycle** (no escribe la BD, no emite `service.suspended`/`unsuspended`, no crea notas), idempotente, audit `service_provider_state_resync_admin`. Sustituye el workaround "Reanudar y volver a suspender" (que generaba dos transiciones de lifecycle falsas → audit + notificaciones espurias). (c) Frontend: `/dashboard/services/[id]` banner de suspensión cliente (motivo cliente-seguro del enum `SuspensionReason` — nunca la nota interna — + CTA: `overdue_payment`→`/dashboard/billing`, resto→`/dashboard/support`; oculta SSO + ActionsBar + DNS mientras suspendido) + `/admin/services/[id]` `<AdminProviderStateDesyncBanner>` con botón "Realinear estado del proveedor" cuando `provider_state_desync` + `ServiceHeader` cede el `statusReason` de suspensión al banner upstream + i18n keys nuevas + `ServiceDetailResponse.service.provider_state_desync`. **4 decisiones de la valoración pre-código** (resueltas con Yasmin, L18 — registradas en dossier §A.11.10.1.1): flag en el summary (no `ServiceInfo`); aviso admin **con acción** de realineado (no solo informa — el workaround anterior ensucia el audit); doctrina como **amendment formal** (no nota inline); banner cliente **oculta** SSO/ActionsBar/DNS. **Ampliación 2026-05-12** (testing Yasmin): la condición de la reconciliación se generalizó de "proveedor reporta `active`/`suspended` y discrepa" a "proveedor accesible y `info.status !== services.status`" → cubre el caso del proveedor terminal (`cancelled`/`expired`) vs Aelium `suspended` (la página dejó de ser un Frankenstein header "Cancelado" + banner "Suspendido" + ninguna acción). Suite backend **674 passed + 5 skipped** (49 suites; +tests F.4: `provisioning.service.spec` override ×3 direcciones + provider unknown + plugin sin supports_suspend + resync ×5). `pnpm ci:check:full` verde + boot real verificado. Backlog sin cambios (`DC.44`/`DC.45` ya promovidos en F.3). Commit en la rama: `eba5cea`. **Próximo: F.5** (`DC.44` billing-suspend-unify — `ServiceLifecycleWorker.autoSuspendServices`→`suspendAsAdmin` + auto-reactivación al pagar + convención "actor sistema"; va **antes** de F.6) → F.6→F.12 + G (detalle en dossier §A.11.10). **Sprint 15D RC sigue BLOQUEADO** hasta cierre Sprint 15C.II (Fases F.5→F.12 + G). **Nota de proceso**: 6ª aplicación del bypass §6 — conviene que el desbloqueo de GitHub Actions tenga dueño/fecha (no debería volverse permanente). Earlier 2026-05-12 — **Sprint 15C.II — re-plan de la continuación de la Fase F**: el testing de F.1 destapó un agujero de robustez del status de suspensión (`getServiceInfo` deriva el status — y por tanto `availableActions` y el botón "Reanudar" — solo del proveedor, mientras `services.status` + el banner amarillo + el guard de `unsuspendAsAdmin` van solo de la BD → cuando discrepan, banner "suspendido" sin botón para deshacerlo, y el desync inverso → `409` en un botón visible) + decisión Yasmin: traer al sprint los apuntados de backlog (`DC.44` billing-suspend-unify, `DC.45` reconcile-single, deep-links curados, notas de servicio, mini-badge de salud, reenviar notificación, cross-link a billing) — cerrar el plugin Enhance + el módulo de servicios a estándar alto antes de desbloquear Sprint 15D RC. Refinado: las acciones críticas de servicio se integran con el sistema transversal `ClientNote` (no un `service_notes` propio) + una fase de layout canónico al final + "más fases, más pequeñas". **10 fases** (orden por prioridad = riesgo descendente, detalle en dossier §A.11.10): **F.4** robustez del status de suspensión (`getInfoForUser` reconcilia el status administrativo — `service.status==='suspended'` ⇒ `info.status='suspended'` override + `availableActions` re-filtrado + flag `provider_state_desync` en el summary; `services.status` autoritativo para el lifecycle administrativo, distinto de DH-INV-6 operacional; capa orquestador, NO toca el contrato — + banner de suspensión cliente con CTA por motivo + aviso de desync admin); **F.5** `DC.44` billing-suspend-unify (`autoSuspendServices`→`suspendAsAdmin` + auto-reactivación al pagar `billing-on-invoice-paid`→`unsuspendAsAdmin` + convención "actor sistema"; toca billing; va **antes** de F.6); **F.6** notas operativas vía `ClientNote` (cancel/suspend/unsuspend manual → nota **obligatoria** en el modal → `ClientNote` `source_system='service'` + link al servicio; auto-suspend por impago → `ClientNote` auto; el motivo-enum sigue como campo del banner — desacoplado de la nota; salen en `/admin/clients/[id]` → "Notas" como las de los demás módulos; staff-internal; supersede el `service_notes` table; migración split del `"<motivo>: <nota>"`; ADR-079 amendment); **F.7** SSL/TLS status read-only en `getServiceInfo.ssl?` (ADR-077 amendment + ADR-083 amendment) + card cliente/admin; **F.8** alertas de cuota (setting umbral + aviso visual `MetricsBar` + notificación sin spam vía evento `service.quota_threshold_crossed` + listener + plantilla seedeada); **F.9** reconcile per-servicio `DC.45` (`ProvisionerPlugin.reconcileOne?()` ADR-077 amendment + endpoint `POST /admin/services/:id/reconcile` + wire del CTA del `AdminDriftBanner` y del overview F.2); **F.10** deep-links curados al panel del proveedor (inline actions plugin-internas o `getSsoUrl({section})`; capability-driven); **F.11** conveniencias operativas (mini-badge salud del proveedor + reenviar notificación + cross-link billing); **F.12** layout canónico de las páginas de servicio + plugins (diseño UI_SPEC + wireframes, iterar con Yasmin, **freeze**; luego refactor pura composición — última fase de features); **G** cierre (tests críticos 8 áreas + E2E + smoke real + retrospectiva + mover dossier a `completed/` + 15D RC desbloqueado). Cada fase: 1 rama, ADR amendments dentro de la fase, valoración pre-código (L18 — mejoras como Amendment, no desvío silencioso), PR (+ bypass §6 si CI billing-bloqueada) + post-merge sync. **Sprint 15D RC sigue BLOQUEADO** hasta cierre 15C.II (F.4→F.12 + G). `DC.44`/`DC.45` promovidos del backlog. Earlier 2026-05-12 — **Sprint 15C.II Hardening — Fase F.3 (cierre Fase F — audit timeline GAP-M + GAP-N error_module + G4/G5/G8 + CTA reconcile + B.1 cooldown del force-refresh + B.2 catálogo ADR-080) cerrada y mergeada a master — PR [#67](https://github.com/yasmindanailov/dashboard/pull/67) squash-merge `2a33850`** (bypass policy §6, 5ª aplicación — CI GitHub bloqueada billing externo §A.9.10 [jobs Backend `fail 3s` / Frontend `fail 11s`]; las 3 condiciones cumplidas: motivo externo + `pnpm ci:check:full` verde [backend prisma generate + typecheck + lint + 49 suites 664 passed + 5 skipped + `nest build`; frontend typecheck + lint + `next build`] + boot real backend [rutas `GET|POST /api/v1/(admin/)services/:id/(audit|refresh)` mapeadas, DI sin errores] + sección formal en el cuerpo del PR; rama temporal eliminada; label `ready-for-e2e`) → post-merge doc-sync (patrón #61/#64/#66). **3 ADRs Amendments** (backward-compatible, NO bump): [ADR-083 Amendment A7](../10-decisions/adr-083-plugin-enhance-cp-specifics.md#amendments) (registro comprensivo de toda F.3 — A7.1 audit timeline · A7.2 error module · A7.3 testConnection · A7.4 cache TTL + timeout · A7.5 CTA · A7.6 cooldown · A7.7 catálogo · A7.8 backlog · A7.9 validación) + [ADR-077 Amendment A6](../10-decisions/adr-077-contrato-provisioner-plugin-v2.md#amendments) (`testConnection?()` 7º método opcional del contrato — obligatorio si `manifest.testConnectionMethod === 'custom'`; probe sin servicio, sin side-effects, captura sus errores — + `ProvisionerPluginError.module?` mutable, set por los wrappers, leído duck-typed por `GlobalExceptionFilter.resolveErrorModule`) + [ADR-080 Amendment C](../10-decisions/adr-080-plugin-framework.md#amendments) (`PluginManifest.serviceInfoCacheTtlSeconds?` opcional — sigue el patrón B.5 del propio ADR-080; + fila `plugin.reconcile_completed` añadida al catálogo §6). **Contenido**: GAP-15CII-M audit timeline per-servicio (`/admin|/dashboard/services/[id]/audit` — `AuditService.getServiceTimeline` `UNION ALL` `audit_change_log`+`audit_access_log` + keyset cursor `(created_at,id)` + recorte GDPR cliente [whitelist `CLIENT_VISIBLE_TIMELINE_ACTIONS` incl. `admin_sso_impersonation` **con detalle** — decisión Yasmin] + `ProvisioningService.getServiceTimelineForUser` ownership + endpoints `GET /api/v1/(admin/)services/:id/audit` + renderer reusable `ServiceAuditTimeline` + 2 páginas + i18n + `serviceDetailHref` overview F.2 repuntado a `…/audit`; **migración** índice de expresión parcial `audit_access_log ((metadata->>'resource_id')) WHERE metadata ? 'resource_id'` — no se modela en `schema.prisma`, Prisma 7 no expresa índices de expresión) + GAP-15CII-N (`ProvisionerPluginError.module?` mutable + `GlobalExceptionFilter.resolveErrorModule` recorre el error y su cadena `cause` duck-typed → `error_log.module='provisioning.<slug>'` en vez de `'http'`; el wrapper `getServiceInfoWithCache` lo setea; scope solo path HTTP) + GAP-15CII-G8 (test-connection `'custom'`: el `getStatus` de Enhance exige `provider_reference` real → un servicio sintético siempre reportaba "sin metadata"; lo correcto es un probe dedicado — `ProvisionerPlugin` gana `testConnection?()`; Enhance: `'getStatus'`→`'custom'` + probe `GET /version`+`GET /orgs/{masterOrgId}` [ADR-083 §1 dec.5]; `buildSyntheticService` `metadata:{}` defensivo; contract test `'custom'`⇒función ×N plugins) + G4 (`PluginManifest.serviceInfoCacheTtlSeconds?` precedencia manifest>setting global>60s, floor 5s en runtime; Enhance no lo declara) + G5 (`EnhanceHttpClient` default timeout 30s→15s fail-fast workers BullMQ; NO breaker anidado — anti-patrón blanket protection ADR-080 §5; mover el breaker único a envolver el client = refactor diferido v1.1) + CTA "Reconciliar contra el proveedor" en `AdminDriftBanner` cuando `info.recoveryHint === 'reconcile'` → link a `/admin/settings/plugins/[slug]` (reconcile-all + overview F.2; consistente con el patrón Fase E para `invalid_state`) + **B.1 cooldown server-side del force-refresh per-`serviceId`** (`ProvisioningCacheService.tryAcquireRefreshCooldown` `SET refresh_cooldown:<id> 1 EX 15 NX` Redis DB 2, fail-OPEN; `getInfoForUser` si `forceRevalidate` y la ventana está activa → degrada a una lectura cacheada normal [coalescing — el usuario recibe el valor actual ≤15s, sin tocar al proveedor, **sin error**; respuesta sigue siendo `ServiceDetailResponse` válido → **sin cambios en el frontend**]; cache frío → fetch igualmente; cliente y admin comparten la ventana; ambos endpoints `refresh` ya pasan por `getInfoForUser` → cubiertos sin tocar controllers) + B.2 (`plugin.reconcile_completed` en el catálogo §6 de ADR-080 — doc-only, payload cross-chequeado contra el emisor real + el listener + ADR-083 A6.1; nota: `plugin.reconcile_triggered_manually` NO va en esa tabla — no es evento del bus, es un `action` de `audit_change_log` que `AdminPluginsService.reconcileAll` escribe síncronamente). **Decisión clave B.1** (revaloración pre-código con Yasmin): cooldown per-`serviceId` en Redis (coalescing a cache), NO `@Throttle` por IP — el throttle por IP no acota lo que de verdad cuesta (1 llamada a orchd por servicio, no por IP), se rompe bajo NAT corporativo/CGNAT, y `forceRevalidate` además bypasea el cache → dos force-refresh concurrentes del mismo servicio hacen *cache stampede* sobre el write — el `SET NX` per-servicio lo resuelve gratis; en *cooldown-hit* no se devuelve `429` sino el valor cacheado: cero estado de error nuevo, cero `Retry-After` que el cliente deba reconciliar contra su cooldown de 10s, menos *information leak* — y es *menos* código total que `@Throttle` + manejo de error en el cliente. Sin spec dedicado de `ProvisioningCacheService` (no existe; el método queda cubierto vía mock en `provisioning.service.spec` — +3 tests). Backlog nuevo `DC.NEW-15CII-RECONCILE-SINGLE` = **DC.45** (endpoint `POST /admin/services/:id/reconcile` single-shot — requiere tocar el contrato `ProvisionerPlugin`, probablemente `reconcileOne(service)?`). Suite backend **664 passed + 5 skipped** (49 suites; +tests F.3: `audit.service.spec` timeline + `provisioning.service.spec` timeline ownership ×4 + TTL ×3 + cooldown ×3 + `global-exception.filter.spec` NUEVO 4 + `plugin-utils.spec` +2 + `enhance.plugin.spec` testConnection + `admin-plugins.service.spec` +4 rama `'custom'` + `plugin-contract.spec` invariantes G4/G8). `pnpm ci:check:full` verde + boot real backend verificado. **Con esto Fase F COMPLETA** (F.1+F.2+F.3 en master). Próximo: **F.4** (SSL/TLS status read-only en `getServiceInfo` + card cliente/admin + alertas de cuota disco/ancho-banda — acordado con Yasmin 2026-05-12; arranca desde master post post-merge-sync de F.3) → **Fase G** (tests críticos 8 áreas + E2E extension E+F+F.4 + retrospectiva + smoke final + mover dossier a `completed/`). **Sprint 15D RC sigue BLOQUEADO** hasta cierre Sprint 15C.II (Fases F.4 + G). Handoff/registro de cierre F.3 en dossier §A.11.9 (commit-by-commit + decisiones tomadas §A.11.9.3). **Nota de proceso**: 5ª aplicación del bypass §6 — conviene que el desbloqueo de GitHub Actions tenga dueño/fecha (no debería volverse permanente). Earlier 2026-05-12 — **Sprint 15C.II Hardening — Fase F.2 (admin overview operativo) cerrada y mergeada a master — PR [#65](https://github.com/yasmindanailov/dashboard/pull/65) squash-merge `2a3cce8`** (bypass policy §6, CI GitHub bloqueada billing externo §A.9.10 — `pnpm ci:check:full` verde + boot real backend verificado [ruta `/api/v1/admin/plugins/:slug/operational-overview` mapeada + listener `AuditOnPluginReconcileCompletedListener` registrado + cron `intervalSeconds` ok] + doc formal en el PR; rama temporal eliminada). Materializa **[ADR-083 Amendment A6](../10-decisions/adr-083-plugin-enhance-cp-specifics.md#amendments)** — A4.4 (admin overview operativo) frozen en Fase A; F.2 lo materializa con `<PluginOperationalOverview slug>` reusable en `_shared/plugins/` + evento rollup nuevo `plugin.reconcile_completed` (cron + manual → audit `reconcile_completed`, "última reconciliación" como estado observado no inferido) + endpoint `GET /admin/plugins/:slug/operational-overview` + `getOperationalOverview` (salud derivada de circuit breakers + secrets requeridos + última reconciliación) + DTO plugin-agnóstico + `ReconcileRegistryService.register(...,{intervalSeconds})` + `core/provisioning/plugin-audit-id.util.ts` extraído. Corrige 3 apuntados del dossier (§A.11.7): query drifts `action='reconciled_external_change'`, componente en `_shared/plugins/`, "última reconciliación" persistida. Suite **637 passed + 5 skipped** (48 suites, +8 tests). Handoff F.3 verbatim + estado real F.2 en dossier §A.11.7/§A.11.8. **Sprint 15D RC sigue BLOQUEADO** hasta cierre Sprint 15C.II (Fases F.3 + G). Earlier 2026-05-12 — **Fase F partida en sub-PRs F.1 / F.2 / F.3 (decisión Yasmin vía AskUserQuestion); Fase F.1 (suspend/unsuspend) cerrada y mergeada a master — PR [#63](https://github.com/yasmindanailov/dashboard/pull/63) squash-merge `b6675ed`** (rama temporal eliminada; CI GitHub bloqueada billing externo §A.9.10 → mergeada vía bypass policy §6: `pnpm ci:check:full` verde + boot real backend + doc formal en el PR — mismo patrón #57/#60). Materializa **[ADR-077 Amendment A4.5](../10-decisions/adr-077-contrato-provisioner-plugin-v2.md#a45-materialización-sprint-15cii-fase-f1-2026-05-12)** — A4 (capability `supports_suspend`) frozen en Fase A; F.1 lo materializa como **inline actions** `suspend_service`/`unsuspend_service` (NO métodos dedicados del contrato — sería breaking; reconcilia el apuntado del dossier §A.9.6.1 con A4.4 frozen) + `reason` como enum canónico `SuspensionReason` (mejora sobre el string libre del apuntado — i18n/analytics/legal limpio). **Backend:** `PluginCapabilities.supports_suspend: boolean` + `SuspensionReason` en `core/provisioning/types.ts`; plugin Enhance (`supports_suspend: true` + 2 inline actions + `actionSuspendService`/`actionUnsuspendService` vía `patchSubscription({isSuspended})` + `filterActionsByStatus` suspend⇔active/unsuspend⇔suspended + `getServiceInfo` statusReason → i18n key `plugin.enhance_cp.status_reason.suspended`); `internal`/`manual`/fallbacks (`supports_suspend: false`); orquestador `ProvisioningService.suspendAsAdmin`/`unsuspendAsAdmin` (load+guard estado idempotente — 409 `SERVICE_NOT_SUSPENDABLE`/`SERVICE_NOT_SUSPENDED`/`SUSPEND_NOT_SUPPORTED` → `executeActionWithCacheInvalidation` con el slug canónico → `prisma.service.update(status, suspended_at, suspension_reason)` combinado `"<reason>: <internal_note>"` igual que `cancellation_reason` → re-invalida cache → emite `service.suspended`/`service.unsuspended` → audit `logChange`+`logAccess`) + `getInfoForUser` summary expone `suspended_at`/`suspension_reason` + `adminServiceSummarySelect` los incluye + `executeActionForUser` rechaza los 2 slugs (`ForbiddenException USE_DEDICATED_SUSPEND_ENDPOINT` — usar endpoint dedicado); DTO `SuspendServiceDto` + enum `SuspensionReasonDto` + endpoints `POST /admin/services/:id/suspend|unsuspend`; listeners `notifications-on-service-suspended`/`-unsuspended` (NUEVOS, patrón L11+L12, R7 — el de suspended **tolera la forma legacy** del `ServiceLifecycleWorker.autoSuspendServices` de impago: deriva `user_id`, normaliza `reason` legacy → `'overdue_payment'`) + 4 plantillas seedeadas `service.suspended`/`service.unsuspended` (email + campana; el email ramifica el CTA por motivo con `{{#if}}`: regulariza pago / soporte / nada para mantenimiento; el motivo se muestra como etiqueta localizada cliente-segura, NUNCA la nota interna). **Frontend admin:** `SuspendServiceModal` (`mode: 'suspend' | 'unsuspend'` — suspend con AlertBanner warning + Select motivo + Textarea nota + toggle notificar, SIN typing-confirm porque es reversible (L17); unsuspend confirmación simple) + botones "Suspender servicio…"/"Reanudar servicio" en `AdminServiceOperationsCard` (ramifica por presencia de las inline actions en `availableActions`, NUNCA por slug) + banner amarillo "Servicio suspendido" en `/admin/services/[id]` (`isDrift` excluye `suspended`; helper `parseSuspensionReason` tolera forma canónica + legacy "Impago — Factura …") + `INTERNAL_HELPER_SLUGS += suspend_service, unsuspend_service` + i18n (`service.suspension_reason.*` ×5 + `plugin.enhance_cp.actions.suspend_service*`/`unsuspend_service*` + `plugin.enhance_cp.status_reason.suspended`) + `api.ts` tipos (`supports_suspend` + `SuspensionReason` + `suspended_at`/`suspension_reason`). **Tests:** contract spec extendido (invariantes A4 `supports_suspend` ↔ inline actions ↔ adminOnly/destructive — corre ×3 plugins) + `provisioning.service.spec` (6 tests suspend/unsuspend) + 2 listener specs nuevos (incl. forma legacy) + `enhance.plugin.spec` (suspend/unsuspend executeAction + getServiceInfo suspended) + literales `PluginCapabilities` de specs actualizados. Suite **625/630 unit verde + 5 skipped** (47 suites, +2 listener spec nuevos). **`pnpm ci:check:full` verde** (backend typecheck+lint+625/630 tests+`nest build`; frontend typecheck+lint+`next build` ✓) + **boot real backend verificado** (DI sin errores incl. 2 listeners nuevos; 2 rutas admin suspend/unsuspend mapeadas). Diferido nuevo `DC.NEW-15CII-BILLING-SUSPEND-UNIFY` (migrar `autoSuspendServices` para que llame a `suspendAsAdmin`). Handoff F.2 verbatim §A.11.3 + reconciliación §A.9.6.1↔A4.4 en §A.11.2 + lección heredable §A.11.5 ("el ADR frozen gana sobre el apuntado del dossier"). **Sprint 15D RC sigue BLOQUEADO** hasta cierre Sprint 15C.II (Fases F + G). Earlier 2026-05-11 — **Sprint 15C.II Hardening — Fase E cerrada y mergeada a master (`1250a2e`, gold standard), próxima sesión arranca Fase F (admin overview operativo + suspend/unsuspend G3 + audit timeline GAP-M + error_log módulo GAP-N + G4/G5/G8)**. Yasmin pidió "cada punto al más alto estándar, profesional, riguroso y robusto" → se subió el listón sobre el plan acotado del dossier original. **2 ADRs Amendments** (backward-compatible, NO bump `contractVersion`): [ADR-077 Amendment A5](../10-decisions/adr-077-contrato-provisioner-plugin-v2.md#amendments) (campo opcional `recoveryHint?: 'reprovision'|'reconcile'|'contact_support'` en `ServiceInfo` — el plugin clasifica su drift, la UI ramifica por contrato NUNCA por `statusReason.endsWith(...)`; cierra BUG-15CII-I por construcción; invariante test contract) + [ADR-083 Amendment A5](../10-decisions/adr-083-plugin-enhance-cp-specifics.md#amendments) (A5.1: rename slug `force_resync` → `recalculate_provider_metrics` + reubicación a `AdminServiceOperationsCard` con tooltip que la distingue de ↻ Refrescar y del cron L3 — **corrige Amendment A4.2** que decía inexactamente "Reconciliar contra Enhance / comparar cache vs ground truth" cuando la acción hace `PUT calculate-resource-usage`; A5.2: `getServiceInfo()` puebla `recoveryHint` incl. detección `plan_divergence` → `'reconcile'` con `status` sigue `active` DH-INV-6; A5.3: `list_dns_records` expone estado DNSSEC read-only en `result.data.zone.dnssec`). **E.1** BUG-15CII-I (`showReprovision = isDrift && info.recoveryHint === 'reprovision'`). **E.2** UI admin DNS nativa `/admin/services/[id]/dns` (GAP-15CII-L) reusando componentes shared — `DnsRecordsManager`/`DnsRecordForm`/`DnsExternallyBanner` **movidos** (`git mv`) a `_shared/services/dns/_components/` + prop `isAdmin` (back-links + paths) + server actions DNS con param `isAdmin` (helpers `dnsBasePath`/`dnsPagePath`, mismo patrón `refreshServiceInfoAction`) — gold standard: TTL presets dropdown + "Personalizado…" + validación client-side por kind (A=IPv4, AAAA=IPv6, CNAME/MX=FQDN, MX/SRV/CAA formato) + dedup (kind+name+value) + conflicto CNAME RFC 1034 §3.6.2 + Badge DNSSEC + nota propagación post-mutación + empty state pulido. **E.3** cancelar servicio (GAP-15CII-J): `CancelServiceModal` (AlertBanner advertencia irreversible + distinto de suspender; `<Select>` motivo 3 valores canónicos; `<Textarea>` nota interna no-cliente; checkbox "Notificar al cliente" default ON; **typing-confirm** del `serviceDisplayName` — botón danger deshabilitado hasta match exacto; tras OK toast + `router.refresh()` NO redirect) + `deprovisionServiceAction` + `DeprovisionDto.notify_client?` + evento `service.cancelled` lleva `notify_client` + **listener nuevo** `notifications-on-service-cancelled` (patrón L11+L12 Fase D, degradación elegante R7) + **templates seedeados** `service.cancelled` (email HTML + campana — genéricos, sin motivo interno ni nota del admin). **E.4** `recalculate_provider_metrics` → `INTERNAL_HELPER_SLUGS` del `ActionsBar` (ya no botón standalone en "Acciones rápidas"). **E.5** i18n: `plugin.enhance_cp.actions.recalculate_provider_metrics*` (reemplazan `force_resync*`) + `plugin.enhance_cp.status_reason.plan_divergence` + recovery messages `action.invalid_state.admin`/`sso.error.invalid_state.admin` apuntan ahora a "Reconciliar todos los servicios ahora" en settings del plugin (= cron L3, lo correcto). `AdminServiceOperationsCard` **reescrito** (always-render si `!isTerminal`; botones "Cambiar plan…" / "Recalcular métricas en el proveedor" / "Cancelar servicio…"; recibe `serviceDisplayName`). 4 gaps cerrados: BUG-15CII-I + GAP-15CII-J/K/L. **Handoff completo Fase F en [`sprint-15c-ii-hardening-enhance-dossier.md` §A.10](./sprint-15c-ii-hardening-enhance-dossier.md#apéndice-a10--cierre-fase-e--handoff-a-fase-f-2026-05-11)** con frase de arranque verbatim §A.10.1 + estado real §A.10.2 + **4 lecciones técnicas nuevas heredables §A.10.3** (L13 UI ramifica por contrato del plugin NUNCA por display strings — `recoveryHint` doctrine; L14 naming honesto sobre conveniencia — slugs de inline action son plugin-internos, renombrar es seguro; L15 "demote, don't delete" — progressive disclosure para power-user ops; L16 componentes compartidos cliente/admin `_shared/` + prop isAdmin NO duplicación; L17 checklist flujo destructivo grado profesional). Suite backend **591/596 unit verde + 5 skipped** (45 suites, +1 nueva `notifications-on-service-cancelled.listener.spec`; +10 tests netos vs Fase D: 4 recoveryHint + 1 DNSSEC + rename test + plugin-utils fallback + plugin-contract invariant + 8 listener + provisioning.service notify_client). **`pnpm ci:check:full` verde** (backend typecheck+lint+591/596 tests+`nest build`; frontend typecheck+lint+`next build` ✓) + **boot real backend verificado** (`Nest application successfully started`, DI sin errores incl. listener nuevo, 4 rutas admin DNS mapeadas) — bar del local-ci-playbook §4/§6. Frontend no tiene framework de unit tests (solo Playwright e2e en `tests/e2e/`) — cobertura de la lógica nueva frontend: tipos TS estrictos + extensión E2E `sprint-15c-enhance-flow.spec.ts` (10 tests, +3: admin DNS CRUD + cancelar con mailpit + recalcular) con label `ready-for-e2e` añadida al PR #60 (label creada en el repo en Fase E — no existía). **PR #60 mergeado a master** (squash-merge `1250a2e` 2026-05-11) vía **bypass policy §6** — CI GitHub estaba bloqueada por billing externo (mismo incidente §A.9.10), las 3 condiciones cumplidas (motivo externo + `ci:check:full` verde + boot real + doc formal en 2 comentarios del PR); la suite E2E correrá en el push a master cuando se resuelva el billing (red de seguridad post-merge). Rama temporal `sprint15c-ii-fase-e-admin-dns-operations` eliminada tras merge. **Sprint 15D RC sigue BLOQUEADO** hasta cierre Sprint 15C.II (Fases F + G). Earlier 2026-05-10 — **Sprint 15C.II Hardening — Fase C cerrada (PR #55 mergeado), próxima sesión arranca Fase D (audit-sanitizer.ts + email listener notifications-on-password-reset)**. Fases A (doc-only `714c94c`) + B (refresh metrics ↻ + reconcile dual entry point + endpoint manual cron + 6 rounds fix-up smoke real, PR #54 `01ad9a8`) + **C drift UX + i18n + a11y + service detail hardening (PR #55, 7 rounds round-by-round `5906165` → `f9b4b2f`, +1998/-337 LOC, 26 archivos)** cerradas en rama dedicada por fase. **Handoff completo para el próximo agente IA en [`sprint-15c-ii-hardening-enhance-dossier.md` §A.8](./sprint-15c-ii-hardening-enhance-dossier.md#apéndice-a8--handoff-fase-c--fase-d-próximo-agente-ia)** con: 7 commits Fase C round-by-round, **10 lecciones técnicas heredables** (cache wrapper invalidation canónica orquestador post-mutation, service terminal NO invocar plugin shortcircuit, backend retorna keys "base" frontend discrimina por rol, window.confirm() → Modal DS canonical UI_SPEC §4.2, capability flags has_metrics gating UI declarativo, refresh UX por rol cliente pasivo + admin cooldown 10s, IDs UUID secundarios con `<CopyableId>` info legible primaria, error codes backend canónicos INVALID_PAYLOAD/INVALID_STATE/CIRCUIT_OPEN/PROVIDER_INTERNAL_ERROR, reset status→provisioning antes de enqueue caso reprovision admin, bug crítico smoke producto sin enhance_plan_id → INVALID_PAYLOAD permanent), scope completo Fase D (D.1 audit-sanitizer + D.2 email listener + D.3 i18n + D.4 update success message), gaps audit estado actual (G2 ⏳ Fase D CRÍTICO / G3-G5 G8 → Fase F / G6 G6b G7 ✅ cerrados Fase C / G9 NUEVO Fase C round 7 — adoptar `<CopyableId>` cross admin pages futuras). 8 BUG-15CII-A..H descubiertos en smoke real cerrados rounds 2-7 (cache wrapper, reprovision idempotency, terminal UX, error codes, dedupe terminal, refresh discriminado). Suite **505/510 unit verde + 5 skipped + E2E 6/6 verde + 0 regresiones** (+8 tests nuevos vs Fase B baseline). 5/5 CI checks SUCCESS PR #55. Earlier 2026-05-10 — **Sprint 15C.II Hardening — Fase B cerrada (PR #54 pending merge), próxima sesión arranca Fase C (drift UX por rol)**. Fases A (doc-only `714c94c`) + B (refresh metrics ↻ + reconcile dual entry point + endpoint manual cron + 6 rounds fix-up smoke real) cerradas en rama dedicada por fase. **Handoff completo para el próximo agente IA en [`sprint-15c-ii-hardening-enhance-dossier.md` §A.7](./sprint-15c-ii-hardening-enhance-dossier.md#apéndice-a7--handoff-fase-b--fase-c-próximo-agente-ia)** con: 7 commits Fase B round-by-round, 8 lecciones técnicas heredables (DI Nest sin ciclos, rjsf v5 `props.label` inconsistente → FieldTemplate única fuente, statusReason → i18n keys, MetricsBar SIEMPRE visible, smoke real local setup, etc.), scope completo Fase C (6 sub-tareas + line numbers archivos a tocar), gaps audit estado actual (G1 ✅ cerrado / G2 G6 G6b G7 → Fase C / G3 G4 G5 G8 → Fase F). Suite **501/506 unit verde + 5 skipped + E2E 6/6 verde + 0 regresiones** (+13 tests nuevos Fase B: ReconcileRegistry + AdminPluginsService.reconcileAll + cron onModuleInit). Earlier 2026-05-10 — **Sprint 15C.II Hardening en curso (Fase A doc-only)** en rama `sprint15c-ii-enhance-hardening` desde master post merge PR [#52](https://github.com/yasmindanailov/dashboard/pull/52) (`ef7f488`, Fase 15C.I parcial). Las 4 decisiones doctrinales A1-A4 quedaron **CONGELADAS 2026-05-10** vía AskUserQuestion (todas con la opción Recommended industria Stripe/Vercel/WCAG seleccionada por Yasmin). Audit técnico paralelo (4 agentes) descubrió 8 gaps adicionales NO documentados originalmente (G1 vaporware endpoint manual cron + G2 password en audit + G3 capability flag `supports_suspend` + G4 cache TTL hardcoded + G5 breaker EnhanceApiClient + G6 PluginConfigForm useToast + G6b ChangePackageModal toast + G7 Modal a11y) — recogidos en [Apéndice A del dossier hardening](./sprint-15c-ii-hardening-enhance-dossier.md#apéndice-a--decisiones-congeladas--audit-técnico--plan-refinado-2026-05-10). Plan ampliado a **7 fases (A→G)** ~6-8 sesiones (vs 4-5 originales). Materializado en **3 ADRs Amendments**: [ADR-077 A4](../10-decisions/adr-077-contrato-provisioner-plugin-v2.md#amendment-a4-2026-05-10--capability-flag-supports_suspend) (capability `supports_suspend` heredable) + [ADR-083 A4](../10-decisions/adr-083-plugin-enhance-cp-specifics.md#amendment-a4-2026-05-10--hardening-ux-post-smoke-real-yasmin-sprint-15cii) (5 sub-secciones hardening UX) + [UI_SPEC §4.13](../UI_SPEC.md#413-estados-de-detección-externa-drift--patrón-discriminado-por-rol) nuevo (drift UX discriminado por rol heredable). Pregunta Yasmin "¿necesito Cloudflare tunnel / dashboard en internet?" → **NO** para Enhance CP (PURE PULL outbound, cero webhooks Enhance→Aelium, Stripe receiver no existe hoy). Suite **488/493 unit verde + 5 skipped + E2E 6/6**. **Sprint 15D RC sigue BLOQUEADO** hasta cierre Sprint 15C.II. Earlier 2026-05-10 — Sprint 15C cerrado al 90% — hardening sub-sprint requerido pre-producción (smoke real Yasmin reveló 18 issues + 4 decisiones doctrinales pendientes). Fase 15C.I aplicada parcialmente en rama `sprint15c-fase-i-cierre-sprint` con fixes valiosos sin commit (11+ correcciones a 488/493 verde + E2E 6/6). Compromiso doctrinal Yasmin literal: "no se da un paso más, hasta que el plugin esté al 100% operativo con los features básicos y necesarios perfectos para producción". → **Sprint 15D ResellerClub bloqueado** hasta cierre Sprint 15C.II hardening. Sub-sprint pre-research dossier en [`docs/60-roadmap/sprint-15c-ii-hardening-enhance-dossier.md`](./sprint-15c-ii-hardening-enhance-dossier.md). Detalle del estado actual + 18 issues categorizados + 4 decisiones doctrinales en [`docs/60-roadmap/completed/sprint-15c-plugin-enhance-cp.md`](./completed/sprint-15c-plugin-enhance-cp.md) §"Hardening Sprint 15C.II". Earlier 2026-05-10 — Fase 15C.I aplicada con 11 PRs encadenados A→J (#36..#51) sin desincronización. Earlier 2026-05-08 — **Sprint 15C Fases A + B + C + D cerradas y mergeadas a master**. Fase 15C.D (PR [#41](https://github.com/yasmindanailov/dashboard/pull/41), master `a319063`) cierra el bloque transversal **DNS-as-capability orchestration**: helper canónico `core/provisioning/dns-authority-resolver.ts` (PURO, R4 intacto) + extensiones `PluginRegistryService.getByCapability()` + `SettingsService.getJson<T>()` + servicio nuevo `EnhanceDnsDefaultsService` (`applyClusterNameservers` idempotente NS-sync C3→C2 + `reconcileZoneDefaults` defensivo). 3 listeners canónicos registrados en `ProvisioningModule`: `BootstrapEnhanceDefaults` (`@OnEvent('plugin.installed')`), `ReconcileDnsDefaults` (`@OnEvent('service.activated')`), `SyncDefaultNameservers` (`@OnEvent('provisioning.default_nameservers_changed')` aspiracional Sprint 12). 8 endpoints REST nuevos `/services/:id/dns/records` (cliente + admin) con error canónico `404 + { code: 'DNS_MANAGED_EXTERNALLY' \| 'DNS_NO_AUTHORITY_PLUGIN', ... }`. 2 settings nuevos seedeados (`provisioning.default_nameservers` JSON array + `provisioning.enhance_cp.reconciliation_alert_threshold=5`). Suite **445/450 unit verde + 5 skipped** (+50 vs base 395/400: 18 resolver + 3 registry + 10 dns-defaults + 13 listeners + 6 service pipeline). Smoke test cURL ✅ — los 4 casos de error canónicos del resolver verificados contra DB real con cuenta cliente demo. Earlier 2026-05-08 — Fase 15C.A doc-only (PR #36, master `0bb83b3`) congeló 3 ADRs (082 transversal Domain↔Hosting + 077 Amendment A1 `has_dns_management` + 083 Plugin Enhance specifics 35 decisiones). Fase 15C.B (PR #37, master `156ea35`) materializó `EnhanceApiClient` (28 métodos + 74 tests) + `MockEnhanceServer` Express stub + ADR-083 Amendment A1 (ubicación canónica del mock `backend/test/mocks/enhance-server/`). Fase 15C.C (PR #38, master `69fed47`) cerró el primer plugin SaaS real: `EnhanceProvisionerPlugin` (6 métodos contrato + manifest + 9 inlineActions + capabilities frozen) + `EnhanceCustomersService` (lazy create + 3-step idempotency con `pg_advisory_xact_lock`) + tabla nueva `enhance_customers` PK natural `user_id` (ADR-083 Amendment A2) + slug regex extendido `/^[a-z][a-z0-9_-]*$/` snake+kebab (ADR-077 Amendment A2, fix bug pre-existente registry kebab-only) + DI registration + 49 tests nuevos. Suite total: **395/400 unit verde + 5 skipped** (mode='static-only' del contract test para `enhance_cp`). Cierre doctrinal del PR #38: schema doc nueva [`enhance-customers.md`](../30-data/enhance-customers.md), `provisioning/contract.md` actualizado §2/§4/§6/§9, 10 términos nuevos al [glossary.md](../00-foundations/glossary.md) (cierre deuda histórica PR #36 — DH-INV-N + NS-sync 3 capas + DNS authority resolver + Default DNS records platform-level + Checkout flows F1-F4 + Enhance Customer + Master Org Aelium + Customer Org Enhance + OTP SSO URL + Reconcile drift detection). Smoke test browser ✅ — plugin renderiza correctamente en `/admin/settings/plugins/enhance-cp`. Earlier 2026-05-07 — **Sprint 15C arrancado** (rama `sprint15c-plugin-enhance-cp` desde master `80492ad`). Earlier 2026-05-07 — **Pre-sprint dossier 15C Enhance CP completado**. Cabeza de cola activa P2.3 (primer plugin real post Sprint 15A). Iteración con Yasmin produjo: spec `orchd v12.21.3` capturado literal en [`docs/_research/sprint-15c/orchd-oas3-api.yaml`](../_research/sprint-15c/orchd-oas3-api.yaml) (588 KB, ~280 paths, OpenAPI 3.0.3) + dossier formal 11 secciones en [`sprint-15c-enhance-cp-dossier.md`](./sprint-15c-enhance-cp-dossier.md) con catálogo exhaustivo Enhance API, scope frozen (28 in / 17+ out, supera WHMCS/Blesta/WiseCP/Upmind en DNS+métricas+audit+cross-plugin), 16 deudas explícitas (DC.NEW-15C-1..16), 3 ADRs futuros (082 transversal Domain↔Hosting con DH-INV-1..6 + 077 Amendment A1 `has_dns_management` flag + 083 plugin specifics). Decisiones canónicas frozen: provision 6-step idempotente con search-by-email, SSO 2-call OTP via `/orgs/{cust}/members/{owner}/sso`, default DNS records globales en cluster (reemplaza listener inline), reconcile 3 capas (60s/on-demand/6h), DH-INV-6 (Enhance gana en conflicto operacional). Earlier 2026-05-07 — **Inversión orden P2.3 ↔ P2.4** (Sprint 15C antes que Sprint 15D ResellerClub) por razón técnica DNS authority. Pre-sprint dossier 15D en [`sprint-15d-resellerclub-dossier.md`](./sprint-15d-resellerclub-dossier.md) ya en master (`542d589`). Anterior: 2026-05-06 — **Sprint 15A Plugin Framework cerrado al 100% y mergeado a master `bee90d8`**. ADR-080 (manifest declarativo + vault de secretos AES-256-GCM + loader desde DB + circuit breaker tras interface) materializado en 8 commits (6 originales + Amendment A1 con 2 fixes post-CI: `cad735b` ENCRYPTION_KEY 64 hex + `95659fb` audit_change_log entity_id UUID v5 derivado del slug). Cobertura final: **255/255 unit verde** (+57 nuevos), 7 E2E REST (`admin-plugins.spec.ts`), typecheck + lint verde backend & frontend, 5/5 CI checks verdes. Plugins reales 15B/C/D/E/G heredan TODO el framework — solo declaran 6 métodos del contrato + manifest. Detalle en [`completed/sprint-15a-plugin-framework.md`](./completed/sprint-15a-plugin-framework.md). PR [#31](https://github.com/yasmindanailov/dashboard/pull/31).
> **Cambios estructurales recientes:**
> - 📜 **[ADR-069 (2026-04-29)](../10-decisions/adr-069-estrategia-deploy-diferido.md)** reclasifica **Sprint 14 Deploy real** como **gate condicionado P-DEPLOY** (no está en cola activa). Se activa sólo con trigger de negocio explícito (cliente real, demo, captación, validación externa). La cola activa post-cierre Sprint 8 son features (Sprint 11 Provisioning como cabeza, Sprint 10 Infrastructure independiente, sub-sprint billing prorrateo cross-plan ADR-077 propuesto, Sprint 12 Settings+KB, Sprint 13 Hardening) según valor funcional.
> - **Sprint 11 Fases 11.A + 11.B mergeadas en master 2026-05-02** — ADR-077 (contrato canónico `ProvisionerPlugin` v2 congelado) + orquestador + cola BullMQ `provisioning-dispatch` + cache Redis dedicado (DB 2) + plugin registry. **183/183 unit verde** (157 base Sprint 8 + 26 nuevos). Plugins concretos pendientes (Fase 11.C). Plan canónico abajo.
> - **Sprint 8 (Tasks + Support Inside) cerrado 2026-05-01** — 5 ADRs nacieron en el sprint (072..076), 157/157 unit + 117/117 E2E verde, 5 migraciones. Detalle en [`completed/sprint-8-tasks-support-inside.md`](./completed/sprint-8-tasks-support-inside.md).
> - **Sprint 11.5 (MinIO Storage)** añadido como sprint independiente — antes estaba dentro del Sprint 14 Deploy. Desbloquea adjuntos chat/tickets sin obligar a desplegar a producción.
> - **Sprint 14 (Deploy)** limpiado — solo lo que realmente requiere producción real. **Hoy gate condicionado bajo ADR-069.**
> - **Sprint 15 (Plugins)** partido en sub-sprints 15A-15H independientes — cada plugin se aborda según necesidad real, no en cadena.

---

## 🔄 Sprint 7 — Billing Hardening + Support

**Estado:** ~95% completo, **bloqueado por dependencias externas** para los pasos restantes.
**Inicio:** Sprint 6 (continuación). **Cierre formal estimado:** cuando se desbloqueen Sprints 14, 15, 8.

### ✅ Lo cerrado (verificado contra código)

- **Billing hardening (5 pasos):** admin checkout selector, validar `targetUserId`, perfil de facturación contra cliente destino, IVA recálculo en edición, descuento anual aplicado.
- **Support core (8 pasos):** SupportService completo, WebSocket gateway con auth dual JWT+guest, chat tiempo real, arquitectura dual chat+ticket, escalación, panel agente 3 columnas, bandeja tickets, detalle conversación, plantillas de email, admin.md.
- **Support hardening (25 pasos H1-H25):** dedup WS+REST, escalación única, cleanup typing, post-escalación redirige al ticket, página `[id]` diferenciada, sorting waiting_agent, indicador asignación, unread separado por type, stats filtrados, sync notas, nota obligatoria al reabrir, coherencia acciones panel, sidebar contexto cliente, etc.
- **Chat anónimo (8 pasos):** guest token, endpoint guest, rate limit 3/h, gateway auth fallback, widget guest mode, vinculación por email, vinculación manual, cleanup cron 30d.
- **Refactorización R15 (9 pasos R15.1-R15.9):** chats/page (907→77), ChatWidget (671→155), support/page (557→102), support/[id] (733→88), checkout (570→233), layout (394→79), clients/[id] (683→243), products (323→282), products/new (347→296). **Backend support refactor:** support.service (1054→90 fachada + 4 sub-servicios), gateway (526→232).

### ⏳ Lo pendiente (todo bloqueado)

| Paso | Bloqueado por | Cuándo se desbloquea |
|------|---------------|----------------------|
| 7.6.1-3 Horario soporte | Nada — se puede hacer ya | Decisión de priorizar |
| 7.7 Adjuntos archivos | **Sprint 14 — MinIO** | Tras Sprint 14 |
| 7.6.1-4 Ticket UX (rich text + email-style + adjuntos + subject editable) | **Sprint 7.5 Fase 2 + Sprint 14 MinIO** | Cuando ambos cierren |
| 7.8/7.9 IA filtro + copilot | **Sprint 15 Plugins (Claude AI)** | Tras Sprint 15 |
| 7.SI.1/2 Support Inside (badge, página cliente) | **Sprint 8 Fase D** | Tras cierre Sprint 8 |

**Acción recomendada:** **NO cerrar Sprint 7 formalmente** todavía. Cuando todos los bloqueos se resuelvan en sus respectivos sprints, se cierra de una vez.

---

## 🔄 Sprint 7.5 — Design System Foundation

**Estado:** Fase 1 ✅ cerrada. Fase 2 parcial.

### ✅ Fase 1 — Tokens y componentes base (D1–D10f, D11)

Verificada completa contra código en `frontend/components/ui/`:

- D1 Tokens CSS, D2 Button, D3 Input/Select/SearchInput/Textarea, D4 Badge/StatusDot, D5 Card, D6 Modal, D7 Table, D8 Toast, D9 EmptyState/Skeleton, D10 Avatar/Tooltip/Dropdown, D10b Pagination/StatsCard/AlertBanner, D10c UI_SPEC.md, D10d StatusTabs, D10e Breadcrumb, D10f Tabs.
- D11 Dashboard shell migrado (Sidebar, Topbar, Layout) — CSS modules, eliminados inline styles.

### ⏳ Fase 2 — Migración de páginas existentes (parcial)

Algunas páginas migradas en Sprint 7 R15 (chats, support, checkout, layout, clients, products). Otras pendientes — el playbook no enumera el % exacto. Acción: **cuando se aborde una página por trabajo de feature, migrarla al DS en el mismo PR** (oportunismo) en lugar de un sprint dedicado de migración masiva.

---

## ✅ Sprint 8 — Tasks + Support Inside (cerrado 2026-05-01)

> Sprint cerrado al 100%. Movido a [`completed/sprint-8-tasks-support-inside.md`](./completed/sprint-8-tasks-support-inside.md) con retrospectiva completa, métricas, ADRs nacidos (072..076) y lecciones aprendidas. Cobertura final: 157/157 unit + 117/117 E2E verde, 5 migraciones aplicadas.

> Las páginas operativas del módulo viven en:
> - [`docs/features/tasks/admin.md`](../features/tasks/admin.md) — operativa diaria del módulo Tasks
> - [`docs/features/tasks/agent.md`](../features/tasks/agent.md) — guía del agente
> - [`docs/features/support-inside/admin.md`](../features/support-inside/admin.md) — operativa Support Inside (staff)
> - [`docs/features/support-inside/client.md`](../features/support-inside/client.md) — guía cliente Support Inside

---

## ✅ Sprint 9 — Audit + Notifications Full + BullMQ + DLQ (P1.1) (cerrado 2026-04-27)

> Sprint cerrado al 100% del alcance MVP. Movido a [`completed/sprint-9-audit-notifications-bullmq.md`](./completed/sprint-9-audit-notifications-bullmq.md) el 2026-05-01 (saneamiento documental post-Sprint 8 cierre). DoD verificado: typecheck + lint + build + 21/21 unit + 30/30 E2E + boot real con 3 colas BullMQ + 8 crons in-process. P1.1 desbloquea Sprint 14 Deploy sin bloqueos críticos.

---

## ✅ Sprint 9.5 — UX admin de notifications + cabos sueltos (P1.1.5) (cerrado 2026-04-27)

> Sprint cerrado en 1 sesión densa. Movido a [`completed/sprint-9-5-ux-admin-notifications.md`](./completed/sprint-9-5-ux-admin-notifications.md) el 2026-05-01.

---

## ✅ Sprint 11.5 — MinIO Storage local (P1.2) (cerrado 2026-04-26)

> Sub-sprint independiente que aisló storage local del Sprint 14 Deploy para desbloquear adjuntos chat/tickets. Movido a [`completed/sprint-11-5-minio-storage.md`](./completed/sprint-11-5-minio-storage.md) el 2026-05-01.

---

## ✅ Sprint 9.6 — Split admin/cliente retroactivo + 3 portales raíz + permisos granulares (P1.1.6 / DC.7) (cerrado 2026-04-28)

> Sprint cerrado en 1 sesión densa, 12 commits encadenados. ADR-066 + ADR-067 + ADR-068 nacieron aquí. Tres portales raíz formalizados (`/admin/*`, `/dashboard/*`, `/partner/*`). Retrospectiva ejecutiva + plan canónico completo en [`completed/sprint-9-6-split-admin-cliente.md`](./completed/sprint-9-6-split-admin-cliente.md).

---

## ✅ Sprint 11 — Provisioning (P2.1) (cerrado 2026-05-02)

> Sprint cerrado al 100%. Movido a [`completed/sprint-11-provisioning.md`](./completed/sprint-11-provisioning.md) con retrospectiva completa, métricas, 2 ADRs nacidos (077 contrato canónico `ProvisionerPlugin` v2 + 078 auth server-side cookies httpOnly) y lecciones aprendidas. Cobertura final: **241/241 unit + 129/129 E2E verde**, 1 migración aplicada, 7 PRs encadenados (#13 ADR-077 → #14 chasis → #15 cierre doc 11.B → #16 11.C plugins triviales → #17 ADR-078 → #18 11.D REST + frontend → #19 sync), 8 endpoints REST nuevos, 1 cola BullMQ nueva (`provisioning-dispatch`), 5 eventos `service.*` nuevos, 4 DCs nuevas registradas en `backlog.md` (DC.27/29/30/31).

> **Documentación canónica del módulo:**
> - [`docs/features/services/admin.md`](../features/services/admin.md) — operativa diaria del módulo Services para staff.
> - [`docs/features/services/client.md`](../features/services/client.md) — guía cliente.
> - [`docs/features/provisioning/admin.md`](../features/provisioning/admin.md) — vista interna del orquestador.
> - [`docs/20-modules/provisioning/contract.md`](../20-modules/provisioning/contract.md) — contrato canónico (12 secciones, marcado ✅ implementado).

---


## ✅ Sprint 13.5 — Hardening + Saneamiento de Deuda Continua (cerrado 2026-05-03)

> Sub-sprint dedicado a cerrar deuda continua acumulada antes de Sprint 15A Plugin Framework. Movido a [`completed/sprint-13-5-hardening-deuda-continua.md`](./completed/sprint-13-5-hardening-deuda-continua.md) con retrospectiva completa, métricas, lecciones aprendidas y plan de Sprint 13.5.5 CI Infra (sub-sprint nacido del aprendizaje). 8 DCs cerradas (DC.32/33/34 + DC.14/37/38 + DC.8/11/15 parciales) + 2 diferidas (DC.13 + DC.27 → Sprint 13.5.5). Cobertura final: **183/183 unit + 118/118 E2E verde** sin regresión.

---

## ✅ Sprint 13.5.5 — CI Infra (cerrado 2026-05-03)

> Sub-sprint cerrado al 100%. Movido a [`completed/sprint-13-5-5-ci-infra.md`](./completed/sprint-13-5-5-ci-infra.md) con retrospectiva completa, métricas, decisión arquitectónica + lecciones aprendidas. **DC.27 ✅** (imagen oficial Playwright `mcr.microsoft.com/playwright:v1.59.1-noble` + service names + MinIO `bitnamilegacy/minio:2025.7.23-debian-12-r5` como service container) + **DC.13 ✅ parcial-canónica** (sharding CI con `--shard=N/M` × 3 shards paralelos, wall-clock CI 25 min → ~10 min). Paralelización local con `workers > 1` **diferida a sub-sprint condicionado** Sprint 13.5.6 (trigger: suite local > 2 min) — el cuello real estaba en CI, no en local. Decisión arquitectónica completa en la retrospectiva §4.

---

## ✅ Sprint 16 — Tasks refactor + Notes consolidation (P2.1.5) (cerrado 2026-05-02)

> Sprint cerrado al 100%. Movido a [`completed/sprint-16-tasks-notes-refactor.md`](./completed/sprint-16-tasks-notes-refactor.md) con retrospectiva completa, métricas, ADR nacido (ADR-079 + Amendments A1/A2/A3) y lecciones aprendidas. Cobertura final: **183/183 unit + 118/118 E2E verde**, 1 migración aplicada (`sprint16_tasks_notes_refactor`), 4 PRs encadenados (#21 ADR-079 → #22 backend → #23 sync → #24 frontend + amendments + cierre documental).

> **Documentación canónica del módulo:**
> - [`docs/20-modules/tasks/contract.md`](../20-modules/tasks/contract.md) — Contract canónico tasks (post-ADR-079).
> - [`docs/30-data/tasks.md`](../30-data/tasks.md) — Schema canónico tasks.
> - [`docs/30-data/clients.md`](../30-data/clients.md) — Schema canónico `client_notes` (consolidación con source tracking).
> - [`docs/features/tasks/admin.md`](../features/tasks/admin.md) — Operativa admin.
> - [`docs/features/tasks/agent.md`](../features/tasks/agent.md) — Guía agente.
> - [`docs/features/notes/admin.md`](../features/notes/admin.md) — Operativa notas consolidadas (nuevo).
> - [`docs/features/support/lifecycle.md`](../features/support/lifecycle.md) — Lifecycle ticket vs chat (Amendments A1+A3, nuevo).

---

## ✅ Sprint 13 §13.AUTH — Auth server-side con cookies httpOnly + Server Components nativos (cerrado 2026-05-03)

> Sprint cerrado al 100%. Movido a [`completed/sprint-13-auth-cookies-httponly.md`](./completed/sprint-13-auth-cookies-httponly.md) con retrospectiva completa, métricas, ADR-078 Amendment A1 (Modelo A), 11 commits encadenados en rama `sprint13-auth-cookies-httponly`, lecciones aprendidas (smoke HTTP real desbloqueando bugs IPv6 + jti, decisión arquitectónica Opción B ESLint per-línea, modelo cross-origin cookies httpOnly Next.js + handshake WS via endpoint dedicado). Cobertura final: **198/198 unit backend verde + 3 specs E2E nuevos** (`auth-cookies-flow` + `auth-replay-detection` + `auth-no-localStorage`) + frontend `pnpm typecheck` + `pnpm lint:check --max-warnings=0` + `pnpm build` verdes. Cierra **DC.6 + DC.28**.

> **Documentación canónica del módulo (post-Sprint 13 §13.AUTH):**
> - [ADR-078 + Amendment A1](../10-decisions/adr-078-auth-server-side-cookies-httponly.md) — Modelo A (cookies httpOnly viven en dominio Next.js).
> - [`docs/00-foundations/rules.md` §R17](../00-foundations/rules.md#r17--jwt-en-cookies-httponly-de-nextjs-no-en-localstorage) — JWT en cookies httpOnly de Next.js, NO en localStorage.
> - [`docs/20-modules/auth/contract.md`](../20-modules/auth/contract.md) — §5 (`/auth/ws-token`), §7 (`auth.refresh_replay_detected`), §11 (env vars frontend `BACKEND_URL` + `NEXT_RUNTIME_SECRET`), §14 (AUTH-INV-8/9).
> - [`docs/50-operations/api-errors.md`](../50-operations/api-errors.md) — `AUTH_REPLAY_DETECTED`.

---

## ✅ Sprint 15A — Plugin Framework (P2.2) (cerrado 2026-05-06)

> Sprint cerrado al 100% y mergeado a master `bee90d8` (squash-merge PR #31). Movido a [`completed/sprint-15a-plugin-framework.md`](./completed/sprint-15a-plugin-framework.md) con retrospectiva completa, métricas, ADR-080 nacido (Plugin Framework: manifest declarativo + vault de secretos AES-256-GCM + loader desde DB + circuit breaker tras interface + 5 eventos `plugin.*`), 8 commits encadenados en rama `sprint15a-plugin-framework` (6 originales + Amendment A1 con 2 fixes CI post-cierre: ENCRYPTION_KEY 64 hex + audit_change_log entity_id UUID v5 derivado del slug), 9 lecciones aprendidas. Cobertura final: **255/255 unit verde** (+57 vs base post Sprint 13: 18 vault + 11 registry + 16 breaker + 15 admin-plugins + 2 manifest contract) + **7 E2E nuevos** (`admin-plugins.spec.ts`) + frontend `pnpm typecheck` + `pnpm lint:check --max-warnings=0` + `pnpm build` verdes. Plugins reales 15B/C/D/E/G heredan TODO el framework — solo declaran 6 métodos del contrato + manifest. PR [#31](https://github.com/yasmindanailov/dashboard/pull/31).

> **Documentación canónica del módulo (post-Sprint 15A):**
> - [ADR-080](../10-decisions/adr-080-plugin-framework.md) — Plugin Framework canónico (manifest declarativo JSON-Schema 7 + tabla `plugin_installs` + `SecretVaultService` AES-256-GCM + loader runtime desde DB + circuit breaker tras interface).
> - [`docs/30-data/plugin-installs.md`](../30-data/plugin-installs.md) — Schema canónico `plugin_installs` con justificación PK natural slug.
> - [`docs/features/provisioning/admin-plugins.md`](../features/provisioning/admin-plugins.md) — Operativa diaria del superadmin (4 flujos canónicos + auditoría + errores comunes).
> - [`docs/20-modules/_events.md` §🔌 plugin.*](../20-modules/_events.md) — 5 eventos `plugin.*` + 3 listeners nuevos.
> - [`docs/20-modules/provisioning/contract.md` §7 Admin Plugin Framework](../20-modules/provisioning/contract.md) — REST endpoints `/admin/plugins/*` + sección Pendientes actualizada.
> - [`docs/00-foundations/glossary.md`](../00-foundations/glossary.md) — 3 términos canónicos nuevos: Plugin Manifest, Secret Vault, Circuit Breaker.

---

## 🔄 Sprint 15C — Plugin Enhance CP (P2.3) — 90%, hardening Sprint 15C.II requerido

> Cabeza de cola activa P2.3 — primer plugin SaaS real post Sprint 15A. **11 fases ejecutadas** (15C.A → 15C.I) en 11 PRs encadenados (#36..#51 + cierre Fase I) sin desincronización a lo largo de 2026-05-08..2026-05-10. Reformulación 2026-05-09 (Fase 15C.E PR #44 review) añadió 2 fases nuevas (E.2 frontend acciones curadas + J cierre real con admin page) que cerraron gaps estructurales del alcance frontend originalmente implícito. Cierra el bloqueante operacional para Sprint 15D RC — el cluster Enhance opera autoridad DNS para los `ns1.aelium.net` / `ns2.aelium.net` que registrar dominios apunten al sistema correcto.
>
> **Estado real 2026-05-10:** Fase 15C.I aplicada en rama `sprint15c-fase-i-cierre-sprint` (E2E spec 6/6 + i18n + doc operativa + retrospectiva). **Smoke real Yasmin reveló 18 issues + 4 decisiones doctrinales** que requieren un **sub-sprint dedicado de hardening**. Fixes valiosos no commiteados en rama (11+ correcciones a `$queryRaw`, `AdminPluginUpdateDto`, `ActionsBar/SsoButton useToast`, mensajes engañosos, INTERNAL_HELPER_SLUGS DNS, `view_disk/bandwidth adminOnly`, `force_resync` description tooltip, `translateSchema()` rjsf, `ServiceHeader` i18n). Suite backend **488/493 unit verde + 5 skipped**. **Sprint 15D RC BLOQUEADO** hasta cierre Sprint 15C.II hardening.
>
> **Sub-sprint hardening (en ejecución):** [`docs/60-roadmap/sprint-15c-ii-hardening-enhance-dossier.md`](./sprint-15c-ii-hardening-enhance-dossier.md) — fases A→G; **A + B + C + D + E + F (F.1+F.2+F.3) ✅ mergeadas a master**. Continuación de la Fase F re-planificada 2026-05-12 (testing de F.1 destapó un agujero de robustez del status de suspensión + decisión Yasmin: traer al sprint los apuntados de backlog `DC.44`/`DC.45` + deep-links/notas/mini-badge/reenviar-notif/cross-link-billing + integrar las acciones críticas de servicio con el sistema transversal `ClientNote` + fase de layout canónico al final + "más fases, más pequeñas") — **10 fases**: **F.4** robustez del status de suspensión (PRÓXIMA) → **F.5** `DC.44` billing-suspend-unify → **F.6** notas operativas vía `ClientNote` → **F.7** SSL status read-only → **F.8** alertas de cuota → **F.9** reconcile per-servicio (`DC.45`) → **F.10** deep-links curados al panel → **F.11** conveniencias operativas (mini-badge salud + reenviar notif + cross-link billing) → **F.12** layout canónico (con freeze gate) → **G** cierre. Plan detallado por fase (scope + ADR amendments + valoración pre-código + DoD) en **dossier §A.11.10**.
>
> - ✅ **Fase A** (decisiones doctrinales A1-A4 frozen + 8 gaps audit + plan 7 fases) — PR #53.
> - ✅ **Fase B** (refresh metrics ↻ + reconcile dual) — PR #54.
> - ✅ **Fase C** (drift UX por rol + i18n + a11y + service detail hardening + 7 rounds smoke real → 8 bugs cerrados) — PR #55. Suite **505/510 unit verde**.
> - ✅ **Fase D** (audit-sanitizer.ts + listener email password reset + plantilla seedeada `service.password_reset` + i18n + E2E test 7) — PR [#57](https://github.com/yasmindanailov/dashboard/pull/57) mergeado 2026-05-10 (commit `c3b519e`) **con bypass CI documentado** por incidente billing GitHub Actions externo (validación local exhaustiva: 576/581 unit + lint + typecheck both + boot real). Suite **576/581 unit verde** (+21 tests nuevos). Smoke real Fase D descubrió **6 gaps nuevos** apuntados Fase E/F: BUG-15CII-I (heurística showReprovision), GAP-15CII-J (cancelar servicio UI), GAP-15CII-K (force_resync UI — re-evaluar antes implementar UI por redundancia con botón ↻), GAP-15CII-L (DNS records UI admin), GAP-15CII-M (audit timeline per-service), GAP-15CII-N (error_log módulo origen). Suspend doctrina transversal expandida §A.9.6.1 (heredable a billing/support/GDPR/maintenance).
> - ✅ **Fase E** (gold standard — Yasmin "cada punto al más alto estándar") — PR [#60](https://github.com/yasmindanailov/dashboard/pull/60) mergeado a master `1250a2e` 2026-05-11 (bypass policy §6 — CI GitHub bloqueada billing externo; rama temporal eliminada). **ADR-077 Amendment A5** (`recoveryHint` estructurado en `ServiceInfo` — la UI ramifica por contrato, NUNCA por `statusReason.endsWith`; cierra BUG-15CII-I por construcción) + **ADR-083 Amendment A5** (rename `force_resync` → `recalculate_provider_metrics` + reubicación a `AdminServiceOperationsCard` con etiquetado preciso — **corrige A4.2** inexacto; `getServiceInfo` puebla `recoveryHint` incl. detección `plan_divergence`; `list_dns_records` expone DNSSEC read-only). UI admin DNS nativa `/admin/services/[id]/dns` reusando componentes shared (`DnsRecordsManager`/`DnsRecordForm`/`DnsExternallyBanner` movidos a `_shared/services/dns/_components/` + prop `isAdmin`) — gold standard: TTL presets dropdown + validación client-side por kind (A/AAAA/CNAME/MX/SRV/CAA) + dedup + conflicto CNAME RFC 1034 + Badge DNSSEC + nota propagación + empty state. `CancelServiceModal` (typing-confirm del dominio + motivo dropdown + nota interna + toggle "notificar al cliente") + `deprovisionServiceAction` + DTO `notify_client` + listener nuevo `notifications-on-service-cancelled` + templates seedeados `service.cancelled` (email + campana). 4 gaps cerrados: BUG-15CII-I + GAP-15CII-J/K/L. Suite backend **591/596 unit verde** (45 suites, +1 nueva) + `pnpm ci:check:full` verde (typecheck+lint+tests+builds ambos) + boot real backend verificado. Handoff Fase F verbatim §A.10.1. 5 lecciones nuevas heredables §A.10.3 (L13 UI ramifica por contrato NUNCA por display strings; L14 naming honesto — slugs de inline action son plugin-internos, renombrar es seguro; L15 "demote, don't delete" progressive disclosure power-user ops; L16 componentes compartidos cliente/admin `_shared/` + prop isAdmin; L17 checklist flujo destructivo grado profesional).
> - **Fase F** (partida en F.1 / F.2 / F.3 — decisión Yasmin 2026-05-12; **F.1+F.2+F.3 ✅ mergeadas a master**; F.4 añadida 2026-05-12):
>   - ✅ **F.1** ([ADR-077 Amendment A4.5](../10-decisions/adr-077-contrato-provisioner-plugin-v2.md#a45-materialización-sprint-15cii-fase-f1-2026-05-12)) — **mergeada a master, PR [#63](https://github.com/yasmindanailov/dashboard/pull/63) squash-merge `b6675ed`** (bypass policy §6, CI bloqueada billing): suspend/unsuspend completo. `PluginCapabilities.supports_suspend` + tipo `SuspensionReason` + materialización **inline actions** `suspend_service`/`unsuspend_service` (NO métodos dedicados del contrato — reconcilia el apuntado §A.9.6.1 con A4.4 frozen) + `reason` enum canónico (mejora sobre string libre). Plugin Enhance + orquestador `suspendAsAdmin`/`unsuspendAsAdmin` + endpoints `POST /admin/services/:id/suspend|unsuspend` + listeners email (el de suspended tolera la forma legacy del worker de impago) + 4 plantillas + frontend `SuspendServiceModal` + banner amarillo + i18n + contract test + `getInfoForUser` summary `suspended_at`/`suspension_reason`. Suite **625/630 unit verde** (47 suites, +2 listener spec). `pnpm ci:check:full` verde + boot real. Diferido `DC.NEW-15CII-BILLING-SUSPEND-UNIFY`.
>   - ✅ **F.2** ([ADR-083 Amendment A6](../10-decisions/adr-083-plugin-enhance-cp-specifics.md#amendments)) — **mergeada a master, PR [#65](https://github.com/yasmindanailov/dashboard/pull/65) squash-merge `2a3cce8`** (bypass policy §6, CI bloqueada billing — `ci:check:full` verde + boot real + doc formal): admin overview operativo `/admin/settings/plugins/[slug]`. `<PluginOperationalOverview slug>` reusable en `frontend/app/_shared/plugins/` (ADR-083 A4.4 — ubicación que ya fijaba) + badge salud derivado (operational/degraded/down/disabled — de circuit breakers + secrets requeridos + última reconciliación) + stats grid 4 cards (activos / suspendidos / drifts 24h / breaker state in-process — etiquetado) + última/próxima reconciliación + tabla recent drifts (`entity_type='Service' AND action='reconciled_external_change'` filtrado por `_meta.plugin_slug`; cada fila enlaza a `/admin/services/[id]` — repunta a `…/audit` en F.3 vía helper `serviceDetailHref`) + reconcile-all/test conexión/form config (ya existen). **Evento rollup NUEVO `plugin.reconcile_completed`** (emitido por el cron del plugin tras cada pasada, `trigger='cron'|'manual'`) → `AuditOnPluginReconcileCompletedListener` → `audit_change_log` `entity_type='Plugin'` `action='reconcile_completed'` — el overview lo lee para "última reconciliación" como **estado observado, no inferido del schedule**. Backend: `core/provisioning/plugin-audit-id.util.ts` (extraído `deriveAuditEntityId`), `ReconcileRegistryService.register(slug, executor, { intervalSeconds })` opcional + `getScheduleMeta`, `AdminPluginsService.getOperationalOverview` + DTO `PluginOperationalOverview` plugin-agnóstico + endpoint `GET /admin/plugins/:slug/operational-overview`. **Corrige 3 apuntados del dossier** (ver §A.11.7): query de drifts es `action='reconciled_external_change'` (no `LIKE 'service.reconciled%'` — ese es el nombre del evento), componente en `_shared/plugins/` (no en `[slug]/_components/`), "última reconciliación" persistida (no inferida). Suite **637 passed + 5 skipped** (48 suites, +8: 6 `getOperationalOverview` + listener spec nuevo + 2 cron emit). `pnpm ci:check:full` verde + boot real (ruta nueva mapeada + listener registrado + cron `intervalSeconds` ok). Handoff F.3 + estado real F.2 en dossier §A.11.7/§A.11.8.
>   - ✅ **F.3** (cierre Fase F) — **mergeada a master, PR [#67](https://github.com/yasmindanailov/dashboard/pull/67) squash-merge `2a33850`** (bypass policy §6 — CI GitHub bloqueada billing externo §A.9.10; las 3 condiciones cumplidas: `pnpm ci:check:full` verde [49 suites, 664 passed + 5 skipped; `nest build` + `next build`] + boot real backend [rutas `GET|POST /api/v1/(admin/)services/:id/(audit|refresh)` mapeadas, DI sin errores] + sección formal en el cuerpo del PR; rama temporal eliminada; label `ready-for-e2e`) → post-merge doc-sync (patrón #61/#64/#66). **ADRs nuevos**: [ADR-083 Amendment A7](../10-decisions/adr-083-plugin-enhance-cp-specifics.md#amendments) (registro comprensivo de toda F.3 — A7.1 audit timeline · A7.2 error module · A7.3 testConnection · A7.4 cache TTL + timeout · A7.5 CTA · A7.6 cooldown · A7.7 catálogo · A7.8 backlog · A7.9 validación) + [ADR-077 Amendment A6](../10-decisions/adr-077-contrato-provisioner-plugin-v2.md#amendments) (`testConnection?()` 7º método opcional [obligatorio si `manifest.testConnectionMethod === 'custom'`] + `ProvisionerPluginError.module?` mutable) + [ADR-080 Amendment C](../10-decisions/adr-080-plugin-framework.md#amendments) (`PluginManifest.serviceInfoCacheTtlSeconds?` — patrón B.5 del propio ADR-080; + fila `plugin.reconcile_completed` en §6). **Contenido**: GAP-15CII-M audit timeline per-servicio (`/admin|/dashboard/services/[id]/audit` — `AuditService.getServiceTimeline` `UNION ALL` `audit_change_log`+`audit_access_log` + keyset cursor `(created_at,id)` + recorte GDPR cliente [whitelist `CLIENT_VISIBLE_TIMELINE_ACTIONS` incl. `admin_sso_impersonation` **con detalle**] + `ProvisioningService.getServiceTimelineForUser` ownership + endpoints `GET /api/v1/(admin/)services/:id/audit` + renderer reusable `ServiceAuditTimeline` + 2 páginas + i18n + `serviceDetailHref` overview F.2 repuntado a `…/audit`; **migración** índice de expresión parcial `audit_access_log ((metadata->>'resource_id')) WHERE metadata ? 'resource_id'` — no en `schema.prisma`) + GAP-15CII-N (`ProvisionerPluginError.module?` mutable + `GlobalExceptionFilter.resolveErrorModule` recorre `cause` duck-typed → `error_log.module='provisioning.<slug>'` no `'http'`) + G8 (test-connection `'custom'`: contrato `testConnection?()` + Enhance probe `GET /version`+`GET /orgs/{masterOrgId}` ADR-083 §1 dec.5 + `buildSyntheticService` `metadata:{}` defensivo) + G4 (`PluginManifest.serviceInfoCacheTtlSeconds?` precedencia manifest>setting>60s floor 5s) + G5 (`EnhanceHttpClient` 30s→15s fail-fast, NO breaker anidado — anti-patrón blanket protection; mover el breaker a envolver el client diferido v1.1) + CTA "Reconciliar contra el proveedor" en `AdminDriftBanner` cuando `recoveryHint==='reconcile'` → link a plugin settings + **B.1 cooldown server-side del force-refresh per-`serviceId`** (`ProvisioningCacheService.tryAcquireRefreshCooldown` `SET refresh_cooldown:<id> 1 EX 15 NX` fail-OPEN; `getInfoForUser` si `forceRevalidate` y ventana activa → degrada a lectura cacheada normal [coalescing — sin error, respuesta sigue siendo `ServiceDetailResponse` válido → sin cambios en el frontend]; cliente y admin comparten la ventana; ambos endpoints `refresh` ya pasan por `getInfoForUser` → cubiertos sin tocar controllers) + B.2 (`plugin.reconcile_completed` en el catálogo §6 de ADR-080). **Decisión clave B.1**: cooldown per-`serviceId` en Redis (coalescing a cache), NO `@Throttle` por IP — el throttle por IP no acota lo que cuesta (1 llamada a orchd por servicio), se rompe bajo NAT, `forceRevalidate` bypasea el cache → stampede concurrente que el `SET NX` resuelve gratis; en cooldown-hit no hay 429 ni `Retry-After`, solo cache → menos código + menos info-leak. Backlog nuevo `DC.NEW-15CII-RECONCILE-SINGLE` = DC.45. Suite **664 passed + 5 skipped** (49 suites). Dossier §A.11.9 (commit-by-commit + decisiones).
> - **Continuación Fase F — re-plan 2026-05-12 (refinado)** (el testing de F.1 destapó un agujero de robustez del status de suspensión + decisión Yasmin: traer al sprint los apuntados de backlog `DC.44`/`DC.45` + deep-links/notas/mini-badge/reenviar-notif/cross-link-billing + integrar las acciones críticas de servicio con el sistema transversal `ClientNote` [no un `service_notes` propio] + una fase de layout canónico al final + "más fases, más pequeñas" → cerrar el plugin Enhance + el módulo de servicios a estándar alto antes de desbloquear 15D RC). **10 fases ordenadas por prioridad = riesgo descendente** — detalle por fase en **dossier §A.11.10**:
>   - ⏳ **F.4 — Robustez del status de suspensión** (PRÓXIMA): `getInfoForUser` reconcilia el status administrativo (si `service.status==='suspended'` ⇒ `info.status='suspended'` override + `availableActions` re-filtrado [fuera `suspend_service`, dentro `unsuspend_service`] + flag `provider_state_desync` en el summary; `services.status` autoritativo para el lifecycle *administrativo*, distinto de DH-INV-6 *operacional*; capa orquestador, heredable; NO toca el contrato) + banner de suspensión para el cliente con CTA por motivo + aviso de desync en la UI admin. **Arregla el estado roto descubierto en testing** (banner "Servicio suspendido" sin botón "Reanudar"; desync inverso → `409 SERVICE_NOT_SUSPENDED` en un botón visible). Rama `sprint15c-ii-fase-f4-lifecycle-robustness`. Sin ADR del contrato (`provider_state_desync` es summary); nota en ADR-082 DH-INV-6 / ADR-070.
>   - ⏳ **F.5 — `DC.44` billing-suspend-unify**: `ServiceLifecycleWorker.autoSuspendServices` (cron impago) → `ProvisioningService.suspendAsAdmin(serviceId,{reason:'overdue_payment'},<actor sistema>,...)` en vez de su propio `prisma.update` + forma reducida; convención "actor sistema" (`actor_user_id:null` + `metadata.actor:'system:billing-overdue-cron'`); auto-reactivación al pagar (`billing-on-invoice-paid` → `unsuspendAsAdmin`). Va **antes** de F.6 para que la suspensión por impago ya pase por `suspendAsAdmin` y la `ClientNote` de F.6 la cubra. Toca el módulo billing. Rama `sprint15c-ii-fase-f5-billing-suspend-unify`. (`DC.44` promovido del backlog.)
>   - ⏳ **F.6 — Notas operativas vía `ClientNote`** (sistema transversal Sprint 16 / ADR-079): cancelar/suspender/reactivar manual → nota **obligatoria** en el modal → `ClientNote` `source_system='service'` + `source_id=serviceId` + `triggered_by_action='service.cancelled'|'service.suspended'|'service.unsuspended'`; auto-suspensión por impago → `ClientNote` auto (`triggered_by_action='service.auto_suspended_overdue'`); el motivo-enum sigue como **campo** del banner (categórico, desacoplado de la nota); el servicio renderiza sus notas inline + salen en `/admin/clients/[id]` → "Notas" como las de los demás módulos (staff-internal — el cliente NO la ve); "añadir nota" del admin = `ClientNote` `manual_entry`. **Supersede el `service_notes` table** (no se crea tabla nueva). Migración: split del `"<motivo>: <nota>"` actual → enum en el campo + `ClientNote` retroactivo. Rama `sprint15c-ii-fase-f6-service-client-notes`. **ADR-079 amendment** (`NoteSourceSystem.service` + `triggered_by_action`s + render del link).
>   - ⏳ **F.7 — SSL/TLS status read-only**: `EnhanceApiClient` lee el estado del cert que Enhance gestiona; `getServiceInfo` gana `ssl?: { status: 'valid'|'expiring_soon'|'expired'|'none'; expiresAt?; autoRenew?; issuer? }` (additivo opcional, patrón `metrics`/`recoveryHint` — **ADR-077 amendment**) + card cliente/admin (`_shared/services/SslStatusCard.tsx`, prop `isAdmin`) con badge verde/ámbar/rojo. Read-only — Aelium NO gestiona el cert (DH-INV-6). Rama `sprint15c-ii-fase-f7-ssl-status`. **ADR-077 amendment** (`ServiceInfo.ssl?`) + **ADR-083 amendment** (probe SSL Enhance).
>   - ⏳ **F.8 — Alertas de cuota** (disco / ancho-banda): setting `quota_alert_threshold_pct` (default 85) + aviso visual en `MetricsBar` al cruzarlo (barra ámbar/roja) + notificación al cliente al cruzar (el reconcile L3 detecta el cruce; persiste "última notif por recurso" para no spamear → emite `service.quota_threshold_crossed` → listener + plantilla seedeada). Heredable a cualquier plugin con `has_metrics`. Rama `sprint15c-ii-fase-f8-quota-alerts`. (Posible tabla `service_quota_alerts`.)
>   - ⏳ **F.9 — Reconciliación per-servicio (`DC.45`)**: `ProvisionerPlugin.reconcileOne?(service)` opcional (**ADR-077 amendment**, patrón A6 `testConnection?()`) + `ReconcileRegistryService.reconcileOne` + `ProvisioningService.reconcileServiceAsAdmin` (con shortcircuit terminal) + endpoint `POST /admin/services/:id/reconcile` + Enhance implementa `reconcileOne` + wire del CTA "Reconciliar contra el proveedor" del `AdminDriftBanner` y de cada fila de drift del `<PluginOperationalOverview>` → endpoint single-shot in-place (cierra el cabo de F.3: hoy el CTA linka a settings). Rama `sprint15c-ii-fase-f9-reconcile-single`. **ADR-077 amendment** (`reconcileOne?()`). (`DC.45` promovido del backlog.)
>   - ⏳ **F.10 — Deep-links curados al panel del proveedor**: "Gestionar email / bases de datos / archivos / logs…" como atajos SSO (las secciones que orchd exponga) en vez del único "Abrir panel" genérico — materialización a decidir pre-código (inline actions plugin-internas sin ADR, o `getSsoUrl({section})` con ADR-077 amendment). Cliente + admin (`_shared/`); capability-driven (ADR-070). Rama `sprint15c-ii-fase-f10-curated-deeplinks`.
>   - ⏳ **F.11 — Conveniencias operativas del detalle de servicio + plugins**: mini-badge de salud del proveedor en `/admin/services/[id]` (del `CircuitBreakerRegistry.getState()`, link al overview F.2) + reenviar notificación al cliente (modal con whitelist de plantillas de service-lifecycle → `POST /admin/services/:id/notifications/resend` + audit) + cross-link a billing en la página del servicio ("Próxima renovación: X · €Y · [Ver factura]"). Rama `sprint15c-ii-fase-f11-service-conveniences`.
>   - ⏳ **F.12 — Layout canónico** (última fase de features, antes de cerrar): diseño — secciones nuevas en `UI_SPEC.md` para `/services/[id]` (admin+cliente discriminado por rol §4.13), lista de plugins, detalle de plugin — jerarquía de componentes + orden/prioridad + responsive + estados empty/error/loading + qué es admin-only — con **wireframes ASCII**; iterar con Yasmin; **FREEZE**; luego refactor de las 3 familias de páginas a la composición congelada (pura composición, cero cambio de comportamiento; reutiliza DS + cards `_shared/`; slots con nombre para lo de F.4-F.11). Rama `sprint15c-ii-fase-f12-canonical-layout`. **Fase con freeze gate** (diseño → iteración → freeze → implementación). Nuevas secciones `UI_SPEC.md` (el diseño es el deliverable).
>   - ⏳ **Fase G — Cierre Sprint 15C.II**: tests críticos faltantes (8 áreas audit Fase A) + E2E extension Fases E + F.1-F.12 (label `ready-for-e2e`) + smoke real Yasmin + retrospectiva (L19+) + mover dossier a `completed/sprint-15c-ii-hardening-enhance.md` (header retrospectiva, dossier original como anexo) + doc-sync de cierre (15C.II ✅ CERRADO, **Sprint 15D RC DESBLOQUEADO**). DoD: todos los DoD de fase (A→F.12) ✓ + `ci:check:full` + `ci:e2e` verdes + smoke OK.
>
> **Detalle completo Sprint 15C original (resumen ejecutivo + métricas + 8 lecciones + commit refs + 18 issues smoke + 4 decisiones doctrinales pendientes):** [`docs/60-roadmap/completed/sprint-15c-plugin-enhance-cp.md`](./completed/sprint-15c-plugin-enhance-cp.md).
>
> **Doc operativa diaria del plugin** (con §8 inventario funcionalidades + 18 issues smoke 2026-05-10): [`docs/features/provisioning/admin-plugins-enhance.md`](../features/provisioning/admin-plugins-enhance.md).

---

## Convenciones de este documento

- **Estado real ≠ estado declarado.** Los símbolos aquí reflejan lo verificado en código a fecha 2026-04-26.
- **No mover sprints a `completed/` hasta que estén realmente cerrados** según [Definition of Done](../90-meta/definition-of-done.md).
- **Cuando un sprint cierra:** mover su sección a `completed/sprint-N-titulo.md` con resumen ejecutivo + commit de cierre + retrospectiva breve.
