# Sprints en curso — Aelium Dashboard

> **Estado real verificado** contra código en auditoría 2026-04-26 + closures Sprint 8 / 9 / 9.5 / 9.6 / 11.5 (2026-04-26 → 2026-05-01) + Sprint 11 Fases A+B (2026-05-01/02). Cualquier sprint listado aquí está parcialmente avanzado (no es backlog puro — para eso ver [`backlog.md`](./backlog.md)). Los sprints ✅ que aparecen abajo son punteros a `completed/`; viven aquí solo para trazabilidad cronológica de la ola P1.1.

> **Última actualización:** 2026-05-20 — **Sprint 15C.II Hardening — Fase F.12 (Layout canónico del detalle de servicio + densidad profesional) cerrada y mergeada a master — PR [#94](https://github.com/yasmindanailov/dashboard/pull/94) squash-merge [`c381e68`](https://github.com/yasmindanailov/dashboard/commit/c381e68)** (bypass policy §6, **15ª aplicación** — patrón heredado #57/#60/#63/#65/#67/#70/#72/#74/#75/#77/#79/#82/#85/#90; CI GitHub billing-bloqueada §A.9.10; las 3 condiciones cumplidas: motivo externo + `pnpm ci:check:full` verde [backend **55 suites · 798 passed + 6 skipped** sin cambios — F.12 es **frontend-only** + frontend `tsc --noEmit` + `eslint --max-warnings=0` + `next build` 32 páginas] + smoke visual Yasmin "todo ok") → post-merge doc-sync (este PR). **Tema**: refactor de la composición del detalle de servicio (`/dashboard/services/[id]` + `/admin/services/[id]`) a layout canónico + densidad profesional según estándar del sector (Hostinger/OVH/cPanel/Stripe/Vercel/GitHub) — última fase de features de Fase F. **Contenido**: F.12.1-2 registry declarativo de secciones (`SERVICE_DETAIL_SECTIONS` + extensión admin vía `extraSections`) + `<ServiceDetailLayout>` único discriminado por ruta (`forceAdminRoute`), wrappers de página finos; F.12.3 tabs adaptativas; F.12.4 `<DetailPage>` + `<ServiceHeaderCard>` (identidad + metadata inline + clúster de acciones); **F.12.5 densidad profesional (Amendments V–VIII)**: 4 primitivas DS nuevas (`<Meter>`/`<SectionCard>`/`<DescriptionList>`/`<DangerZone>` en `components/ui/` + `DESIGN_SYSTEM.md`) + overview en layout **main+aside** (campo `column` en el descriptor) + **menú "Más acciones" único** en el header (cambiar plan · reenviar notif · suspender/reanudar · cancelar + quick-actions, cada ítem con descripción de contexto; `<Dropdown triggerAsChild>` vía `cloneElement` para evitar `<button>` anidado) → **elimina la tab "Gestión"** (Regla D5 destructivas en menú contextual); tabs cliente = Resumen·Auditoría, admin = Resumen·Notas·Auditoría (la antigua "Actividad" dividida); recalcular junto a refrescar (cada uno con `<HelpTip>` ⓘ); salud del plugin reubicada a "Datos técnicos"; dedup de badges (sin "Estado" duplicado — Regla D4); card "Información del servicio" para servicios mínimos (`support_inside`/`internal`/`manual`) → garantiza 2 columnas; tab Auditoría = preview (~15 entradas) + enlace a página completa; coherencia "Renueva/Próxima renovación" oculta en servicios terminales (cancelado no renueva); fixes post-smoke (scrollbar vertical de la barra de tabs `overflow-y:hidden` + margen de la zona de banners + CTA de Notas homogéneo con Auditoría + **endpoint del tab Auditoría cliente** `/services/:id/audit` ≠ ruta de Next `/dashboard/...`). **8 Amendments doctrinales (I–VIII)** en dossier §A.11.10.9.2/.4/.5/.6. **Cero cambios de contrato** (ADR-077/079/080/082/083 SIN amendments — UI_SPEC §5.14 + DESIGN_SYSTEM son el deliverable). **Tras este merge**: 15C.II A→F **COMPLETA** (F.1→F.12 en master); **siguiente y única fase restante: Fase G — cierre Sprint 15C.II** (tests críticos 8 áreas + E2E + smoke + retrospectiva + dossier→`completed/`). **Sprint 15D RC sigue BLOQUEADO** hasta cierre de G.
>
> **Anterior:** 2026-05-19 — **Sprint 15C.II Hardening — Fase F.11 (Conveniencias operativas del detalle de servicio + plugins — mini-badge salud del plugin in-process admin + reenviar notificación al cliente con whitelist defense-in-depth + Amendment II P1 rate limiting + cross-link Service↔billing) cerrada y mergeada a master — PR [#90](https://github.com/yasmindanailov/dashboard/pull/90) squash-merge [`b4b2941`](https://github.com/yasmindanailov/dashboard/commit/b4b2941)** (bypass policy §6, **14ª aplicación** — patrón heredado #57/#60/#63/#65/#67/#70/#72/#74/#75/#77/#79/#82/#85; CI GitHub billing-bloqueada §A.9.10; las 3 condiciones cumplidas: motivo externo + `pnpm ci:check:full` verde [backend **55 suites · 798 passed + 6 skipped** = +44 tests netos vs F.10 master 767+5 + `nest build`; frontend `tsc --noEmit` + `eslint --max-warnings=0` + `next build` 32 static pages + dynamic routes] + boot real backend verificado [hot-fix DI clash `ProvisioningCacheModule` leaf canónico detectado por boot smoke y arreglado pre-merge — `ci:check:full` NO atrapa errores de DI graph runtime de NestJS; lección operativa heredable anotada]) → post-merge doc-sync (este PR). **Tema**: 3 conveniencias profesionales que cierran cabos heredados del audit "qué falta a estándar alto en el plugin Enhance + módulo de servicios cliente/admin" (sesión re-plan 2026-05-12 §A.11.11) — sin tocar el contrato `ProvisionerPlugin` (composición sobre framework existente). **Contenido**: F.11.1 mini-badge salud del plugin in-process en `/admin/services/[id]` — función pura `derivePluginHealth(slug, registry)` worst-case `open > half-open > closed` sobre los breakers del `CircuitBreakerRegistry` Sprint 15A ADR-080 §5 (NO crea breakers — read-only); "Proveedor: ● operativo/degradado/caído → Ver detalle del plugin"; **admin-puro** R1 frozen ADR-070 separación admin/cliente — `<ProviderHealthBadge>` SC en `_components/` admin-only NO en `_shared/`; **doctrina heredable nueva L16 NO universal** (cliente NO ve indicador técnico de breaker; banner drift + recoveryHint + statusReason ya cubren su lado funcional). F.11.2 reenviar notificación al cliente desde panel admin — whitelist canónica defense-in-depth `NOTIFICATION_TEMPLATE_WHITELIST_SERVICE_LIFECYCLE` con `@IsIn` validator en DTO + re-check defensivo en service (curl con `template_key='task.assigned'` → 400 antes de tocar el service); **3 plantillas V1 frozen** tras Amendment I (descubrimiento durante implementación L18): `service.suspended`/`service.unsuspended`/`service.cancelled`; `service.password_reset` excluida (flow propio con OTP fresh — action `reset_account_password` Sprint 15C.II Fase D ya cubre); `service.quota_threshold_crossed` excluida (payload requiere snapshot in-flight no derivable del Service entity — apuntado `DC.NEW-55` con propuesta `ServiceQuotaAlert` como snapshot persistido); R2 frozen re-render fresh contra estado actual del Service (NO re-encola render histórico del `notification_log`; NO se re-emite el evento original — eso dispararía otros listeners); R5 audit `audit_access_log.metadata.{template_key, target_user_id}` enriquecido SIN `rendered_subject`/`rendered_body` (cero PII en audit log); **extensión ADR-077 A9.7 doctrina** heredada F.10 R6 — JSON path admite múltiples sub-recursos identificables en el mismo audit row. **Amendment II 2026-05-19** (self-review profesional post-PR original — cierra gap real de seguridad: admin con curl/script podía disparar N reenvíos al cliente sin protección server-side, spam vector real): P1 rate limiting sobre `POST /admin/services/:id/notifications/resend` — cooldown server-side per **3-tupla `(actor_user_id, service_id, template_key)`** TTL 60s Redis (`SET NX EX` fail-OPEN, patrón canónico heredado F.3 B.1 + F.9); granularidad doctrinal — el actor responsable del spam es quien dispara, otros admins pueden reenviar otra plantilla; defense-in-depth orden de checks **frozen** (INVALID_TEMPLATE_KEY → NotFound anti-enumeration → cooldown → dispatch → audit); HTTP 429 `RESEND_TOO_FREQUENT` con shape `{code, message, retry_after_seconds}` + header HTTP estándar `Retry-After: <segundos>` re-mapeado en controller via `@Res({passthrough:true})` (mismo patrón canónico F.9 `RECONCILE_IN_PROGRESS`); frontend toast accionable con cuenta atrás "Esta misma plantilla se reenvió hace pocos segundos. Reintenta en N s." vs error genérico. F.11.3 cross-link Service↔billing — `BillingInvoiceService.getServiceBillingCrossLink(serviceId, userId, isAdmin)` con owner check espejo `getInfoForUser` (!isAdmin && service.user_id !== userId → 403) + lookup última `Invoice` via `items.some({service_id})` ordered by `created_at DESC` (Decimal serializado como string patrón Prisma canónico); endpoint **unificado** `GET /billing/services/:id/cross-link` con `isAdmin` derivado del role del JWT (patrón canónico `BillingController.findAll`/`findOne` — evita duplicar admin twin); `<BillingCrossLinkCard>` SC en `_shared/services/` con prop `isAdmin` (**L16 SÍ aplica aquí** vs F.11.1 admin-puro — cliente y admin necesitan la MISMA información, solo `href` ramifica `/dashboard/billing/[id]` vs `/admin/billing/[id]`; **decisión funcional por feature**, no por convención ciega); capability-driven por presencia: si `nextDueDate===null && lastInvoice===null` → `null`. **Hot-fix DI clash post-Amendment II** (commit [`5d72e89`](https://github.com/yasmindanailov/dashboard/commit/5d72e89) — detectado por boot smoke real, NO por `ci:check:full`): nuevo módulo leaf canónico `core/provisioning/provisioning-cache.module.ts` aloja `ProvisioningCacheService` con cero deps propias (mismo patrón `ReconcileRegistryModule` introducido F.9 Amendment II DI clash precedente); ambos `ProvisioningModule` + `NotificationsModule` (Global) lo importan. **Lección operativa heredable nueva (anotada inline)**: `ci:check:full` cubre TS + lint + unit tests + builds, **NO el DI graph runtime de NestJS** — el único detector canónico es el boot real del backend dev (`pnpm run dev` + verificar `Aelium API running` sin `UnknownDependenciesException`). Boot smoke OBLIGATORIO antes de mergear PRs que tocan `@Module`/imports/exports. Heredable a 15D RC / 15E Docker / 15G Plesk. **Smoke real Yasmin (commit [`00ff811`](https://github.com/yasmindanailov/dashboard/commit/00ff811))** — primer testing manual completo detectó 4 bugs UX latentes (NO F.11-específicos, heredados): (#1) "Abrir admin" en `<ActionsBar>` genérico lanzaba `INVALID_PAYLOAD` porque la action `open_app_admin` (F.10) requiere `{appId}` pero el ActionsBar lo invocaba con `payload: {}` ("acciones triviales sin payload") — fix añadiendo `open_app_admin` a `INTERNAL_HELPER_SLUGS` blacklist (patrón canónico ya usado por `add_dns_record`/`suspend_service`/etc.); (#2)+(#3) "Restablecer contraseña" + "Abrir panel Enhance" devolvían INVALID_STATE — **NO bug código, gap arquitectónico**: la tabla `enhance_customers` (mapping User → enhance_owner_login_id + enhance_owner_member_id) persiste IDs creados durante el provisioning original; mock Enhance in-memory reiniciado pierde state → IDs en Aelium quedan stale; `reconcile-all` F.9 reconcilia services/subscriptions pero NO valida `enhance_customers` (vive a nivel de User, no de Service) — apuntado `DC.NEW-58` con propuesta arquitectónica rigurosa (NO auto-healing automático por riesgo false-positive en producción si Enhance da false 404 → invalidaría customer correcto; en su lugar comando admin dedicado de reconcile enhance_customers); path manual: `DELETE FROM enhance_customers WHERE user_id=...` + Reaprovisionar el servicio (re-ejecuta flow 6-step que recrea customer en Enhance + repuebla la tabla con IDs frescos); (#4) "Probar conexión" en `/admin/settings/plugins/[slug]` sin feedback — anti-pattern React 18 `startTransition(async () => ...)` (NO marca setState como transition; según versión React/Next puede perderse setState en race conditions del unmount); fix refactor a `async function` plain + onClick wrapper sync `() => { void handle(); }` + try/catch/finally defensivo. **Doctrina heredable nueva (anotada inline)**: cualquier handler `await + setState` en client components sigue el patrón canónico `async function handle()` + `<Button onClick={() => { void handle(); }} />`; NO usar `startTransition(async)`; cualquier slug en `inlineActions[]` que requiera payload no-trivial debe entrar a `INTERNAL_HELPER_SLUGS`. **+30 tests netos F.11** (vs 754+6 master post-F.10 → **+44 tests** post-merge contando ajustes de specs existentes): `circuit-breaker.spec.ts` +5 `derivePluginHealth` + `notification-resend.service.spec.ts` +15 (10 base + 5 Amendment II rate limiting incluyendo granularidad 3-tupla + anti timing-attack NotFound + anti behavior-mapping INVALID_TEMPLATE_KEY) + `billing-invoice.service.cross-link.spec.ts` +6 + `provisioning.service.spec.ts` +4 `getPluginHealthForService`. Cobertura post-F.11: **55 suites / 798 passed + 6 skipped**. **ADRs**: NINGUNO (F.11 es composición sobre framework existente — ADR-077/079/080/083 sin cambios). Doctrina heredable nueva inline en §A.11.10.8.2: L16 NO universal + audit metadata JSON path multi-key (extensión ADR-077 A9.7) + DI runtime detection via boot smoke + async handlers canonical pattern + slug payload no-trivial → INTERNAL_HELPER_SLUGS. **Backlog DC.NEW-55..58 apuntados rigurosamente** ([`backlog.md`](../60-roadmap/backlog.md)): DC.NEW-55 (quota threshold reenvío con snapshot persistido — extiende whitelist V2 a 4 plantillas), DC.NEW-56 (tests integración HTTP supertest del endpoint resend — verificar `Retry-After` header en wire real + `AdminOnlyGuard` rechaza agent_support + `@IsIn` desde `ValidationPipe` global), DC.NEW-57 (Idempotency-Key header Stripe-style para reintentos seguros — requiere persistencia `(idempotency_key, response)` TTL), DC.NEW-58 (reconcile enhance_customers mapping — gap arquitectónico detectado en smoke real F.11 — propuesta NO auto-healing automático). **Total rama**: 10 commits (`a2b6993` doc-only refinamiento R1..R5 frozen pre-código + `a3153c8` F.11.1 mini-badge salud admin + `94ecca0` F.11.2 reenviar notificación + Amendment I whitelist 3 plantillas + `cab6336` F.11.3 cross-link billing + `6f13df4` tests backend +25 + `fbd31a9` style lint fix + `1236e55` docs Amendment I formal en dossier + `70d3027` F.11.2 Amendment II P1 rate limiting + `5d72e89` hot-fix DI clash ProvisioningCacheModule leaf + `00ff811` smoke real Yasmin fixes #1 + #4). **Tras este merge**: 15C.II A→F (F.1+F.2+F.3+F.4+F.5+F.6+F.7+F.8+F.9+F.10+F.11) en master; **siguiente fase F.12 — layout canónico** (última fase de features, refactoriza la composición de todo F.4-F.11; freeze gate diseño → iteración con Yasmin → freeze → implementación pura composición). 15C.II G **CIERRE Sprint** sigue diferido (tests críticos faltantes + E2E spec extension + smoke real cierre + retrospectiva + dossier→`completed/`). **Sprint 15D RC sigue BLOQUEADO** hasta cierre 15C.II completo (F.12 + G).
>
> **Anterior:** 2026-05-18 — Fase F.10 (Capa base de App Management — deep-links a apps CMS instaladas WordPress SSO contractual + Joomla URL canónica + audit per-app) cerrada y mergeada a master — PR [#85](https://github.com/yasmindanailov/dashboard/pull/85) squash-merge `f1f75d5`** (bypass policy §6, **13ª aplicación** — patrón heredado #57/#60/#63/#65/#67/#70/#72/#74/#75/#77/#79/#82; CI GitHub billing-bloqueada §A.9.10; las 3 condiciones cumplidas: motivo externo + `pnpm ci:check:full` verde [backend 53 suites, **767 passed + 6 skipped** = +13 vs F.9 + `nest build`; frontend `tsc --noEmit` + `eslint --max-warnings=0` + `next build` 32 static pages + dynamic routes] + boot real backend verificado [backend :3001 healthy + `MockEnhanceServer` :3099 healthy + ruta `GET /orgs/.../websites/.../apps` mapeada con semántica orchd canónica] + sección formal en el PR body; rama temporal eliminada; label `ready-for-e2e`) → post-merge doc-sync (este PR). **Pivot pre-código 2026-05-18 documentado** en §A.11.10.7.2 R1..R6 frozen: el plan original "deep-links curados al panel del proveedor" se redefine tras la investigación rigurosa OAS de orchd — los endpoints SSO del panel (`getOrgMemberLogin` + `createOtpSession`) son **agnósticos a sección** (cero query params declarados), por lo que construir sobre `?next=`/`?goto=` no-documentado violaría la doctrina de robustez heredable (ADR-070 + ADR-077). El OAS SÍ documenta endpoints contractuales para **apps CMS instaladas dentro de un website** ([`GET /orgs/{org}/websites/{w}/apps`](../_research/sprint-15c/orchd-oas3-api.yaml#L9408) + [`GET /apps/{appId}/wordpress/users/{userId}/sso`](../_research/sprint-15c/orchd-oas3-api.yaml#L9945) SSO contractual + [`GET /apps/{appId}/joomla/info`](../_research/sprint-15c/orchd-oas3-api.yaml#L10255) para URL canónica `${site_url}/administrator`). **Tema**: primera capa del módulo App Management — capability-driven por presencia, contractual, heredable a fases futuras F.10.x (stats UI per-app — `DC.NEW-51`), F.10.y (install/uninstall desde dashboard — `DC.NEW-52`), F.10.z (ops mutación per-app — `DC.NEW-53`) sin breaking changes contractuales. **Contenido**: F.10.1 `ServiceInfo.apps?: AppPresence[]` capability-driven (ADR-077 Amendment A9, mismo molde A5/A6/A7/A8 — NO bumpea `contractVersion`). Shape mínimo contractual genérico: `AppPresence = { appId: string, kind: string libre, label: i18n key, path?: string, version?: string, actions: readonly ServiceAction[] }`. Multi-instancia (N WP en mismo website con diferentes paths) por `appId+path`. F.10.2 action canónica `open_app_admin` con slug fijo + payload `{ appId }` (D5 frozen — heredable a futuras actions per-app `update_app_version`/`install_app_plugin`/`uninstall_app` `DC.NEW-53` sin diversificar shape). Dispatcher interno por kind: WordPress → SSO contractual via `getDefaultWpSsoUser` (404 defensive → null) + `getWordpressUserSsoUrl(defaultUserId)` → `ActionResult.data = { url, appKind: 'wordpress', urlKind: 'sso', opensIn: 'new_tab' }`; Joomla → URL canónica `${site_url}/administrator` (estándar CMS Joomla desde 2005 — estable a nivel del CMS, NO del panel) → `{ url, appKind: 'joomla', urlKind: 'canonical', opensIn: 'new_tab' }`; kind desconocido defensive → `NOT_IMPLEMENTED`. F.10.3 `<AppShortcutsCard>` SC `_shared/services/` paralelo a `<SslStatusCard>` F.7 + `<AppShortcutButton>` Client Component (security best-practice `window.open(url, '_blank', 'noopener,noreferrer')` + `useTransition` loading state `aria-busy` + `errorMessage` `role="alert"`+`aria-live="polite"` a11y mínima + estado disabled si `hasOpenAction=false` con tooltip + CTA fallback). Wire en `/dashboard/services/[id]` cliente y `/admin/services/[id]` admin (L16 prop `isAdmin` pass-through). F.10.4 **audit per-app (R6 frozen + ADR-077 Amendment A9.7 doctrina nueva)**: `ProvisioningService.executeActionForUser` añade `audit_access_log.metadata.app_id` + `metadata.app_kind` + `metadata.url_kind` JSON path **cero schema change** cuando admin actúa sobre service ajeno con `result.success=true` — coherente con `target_user_id` que ya vive como JSON path desde Sprint 9 Fase E. Entry ADITIVO al `audit_change_log` del wrapper — distinta dimensión (read vs change), NO duplicación. Heredable a F.10.x: cuando emerjan actions admin sobre apps (`DC.NEW-53`), el predicado canónico se generaliza a "action que opera sobre sub-recurso identificado por `payload.appId`" — mismo patrón sin refactor. F.10.5 `MockEnhanceServer` extendido con `state.websiteApps: Map<websiteId, EnhanceWebsiteApp[]>` + `state.wordpressInfoByAppId`/`joomlaInfoByAppId` overrides + 5 endpoints `registerWebsiteAppsRoutes` (GET `/apps` lista vacía si sin apps + GET `/wordpress/info` defaults sintéticos derivados del primary domain + GET `/wordpress/users/default` 404 si `defaultWpUserId === undefined` permite testing del path "WP sin default user" canónico + GET `/wordpress/users/:userId/sso` text/plain string JSON-encoded mismo shape que `/members/.../sso` existente + GET `/joomla/info` defaults sintéticos); seed opt-in `MockEnhanceSeed.websiteApps` (NO se auto-siembran apps al `POST /websites` — las apps las instala el cliente explícitamente o via F.10.y futuro install desde dashboard `DC.NEW-52`); cleanup canónico en `DELETE /websites/:id` (coherente con cleanup SSL F.7). **Amendment doctrinal I descubierto durante implementación**: rename `ActionResult.data.kind` → `data.appKind` + nuevo `data.urlKind` ([ADR-083 A9.10](../10-decisions/adr-083-plugin-enhance-cp-specifics.md#amendments)) — separación rigurosa QUÉ app vs CÓMO se generó URL. Compatible hacia atrás porque F.10 es la primera implementación que consume el shape. Heredable a futuras actions: `{ url, <subject>Kind, urlKind?, opensIn }` — separar QUÉ del CÓMO. **+13 tests F.10**: `describe('getServiceInfo() — apps F.10')` 7 tests (sin apps null → undefined / array vacío → undefined / WP con default → action / WP sin default → actions [] / Joomla → action / multi-instancia 3 entries por path / fail-soft getWebsiteApps throws → undefined preserving status active) + `describe('executeAction open_app_admin')` 6 tests (WP SSO real con default user / WP 404 defensive INVALID_STATE / Joomla URL canónica con trailing slash normaliza / appId no existe INVALID_STATE / payload sin appId INVALID_PAYLOAD) + 3 tests existentes ajustados al sumar la 11ª action `open_app_admin` (conteo `inlineActions` 10→11, `clientSlugs` 5→6 cliente self-service NO adminOnly, `availableActions` active 9→10 post `filterActionsByStatus`). **ADRs F.10**: [ADR-077 Amendment A9](../10-decisions/adr-077-contrato-provisioner-plugin-v2.md#amendments) genérico al contrato (additivo NO bumpea `contractVersion` capability-driven por presencia coherente A5/A6/A7/A8) + A9.2 shape `AppPresence` + A9.3 `ServiceInfo.apps?` + A9.4 action canónica `open_app_admin` slug fijo + payload discriminator + A9.5 doctrina "detalles per-kind FUERA del contrato genérico" (`WordPressInfo`/`JoomlaInfo` viven en endpoints/actions plugin-internos invocados on-demand cuando F.10.x stats UI lo requiera) + A9.6 extensibilidad futura additiva (actions/status sin breaking changes) + A9.7 doctrina audit per-sub-recurso JSON path; [ADR-083 Amendment A9](../10-decisions/adr-083-plugin-enhance-cp-specifics.md#amendments) Enhance specifics (4 endpoints orchd consumidos + flow WP SSO contractual `getDefaultWpSsoUser → getWordpressUserSsoUrl(userId)` + flow Joomla URL canónica + manejo 404 default user defensive + mock extension + UI card + A9.10 naming clarity I + A9.11 implementación audit per-app + A9.9 heredabilidad 15D/15E/15G). ADR-080 sin cambios (no eventos nuevos del framework). **Heredable a 15D RC / 15E Docker / 15G Plesk** (ADR-077 A9.9 doctrina): (1) plugin enumera apps en `getServiceInfo()` con sus kinds plugin-internos cuando upstream las expone; (2) action canónica `open_app_admin` declarada con payload `{ appId }`; (3) URLs fresh on-demand en `ActionResult.data` NO cacheadas; (4) mock extendido análogamente con seed opt-in + cleanup en delete del recurso padre; (5) capability gating UI vía presencia de `info.apps`; (6) audit per-app via `metadata.app_id` cero schema change. 15D RC no aplica (registro de dominios — no hay apps). 15E Docker SÍ (containers con apps web). 15G Plesk SÍ (Application Vault con catálogo extensivo — kinds adicionales sin amendment del contrato). **Smoke real automatizado contra MockEnhanceServer 11/11 OK** (2026-05-18 pre-F.11): script `backend/scripts/smoke-f10-mock.ts` arranca mock standalone con seed inline (1 customer + 2 websites + 3 apps WP root+default user / WP /blog sin default / Joomla) y verifica los 5 endpoints F.10 cubriendo los 4 escenarios canónicos (E1 sin apps → `{items:[]}` 200 + E5 multi-instancia 3 entries diferenciadas por `appId+path` + E2 WP con default → SSO URL string + E3 WP sin default → 404 NotFound + E4 Joomla → `site_url` para URL canónica `${site_url}/administrator`) + bonus `getWordpressInfo` shape completo (heredable F.10.x stats UI DC.NEW-51) + defensive GET `/apps` website inexistente → 404. **Cierra parcialmente DC.49** (extension de `MockEnhanceSeed` con `websites?` + env vars JSON `E2E_MOCK_ENHANCE_SEED_WEBSITES_JSON` + `E2E_MOCK_ENHANCE_SEED_WEBSITE_APPS_JSON` en el runner). **Smoke real visual frontend end-to-end** (browser navegando service + click → window.open) sigue diferido a Fase G.2 E2E spec extension contra Playwright (patrón heredado F.9 que difirió E5/E6/E7 a Fase G). Justificación: la suite test rigurosa +13 con todas las ramas dispatcher + capability gating + audit per-app + fail-soft + payload defensive + `ci:check:full` verde + boot smoke real verde + **smoke real automatizado mock 11/11** dan cobertura profesional robusta de la lógica; el smoke visual cubre regresiones UI que `tsc`+`eslint`+tests unit no pueden capturar — su lugar canónico es el E2E suite Playwright. **Tras este merge**: 15C.II A→F (F.1+F.2+F.3+F.4+F.5+F.6+F.7+F.8+F.9+F.10) en master; **siguiente fase F.11 — conveniencias operativas** (mini-badge salud `CircuitBreakerRegistry.getState()` + reenviar notif whitelist plantillas service-lifecycle + cross-link billing subscription/invoice). 15C.II G **CIERRE Sprint** sigue diferido (tests críticos faltantes + E2E spec extension + smoke real cierre + retrospectiva + dossier→`completed/`). **Sprint 15D RC sigue BLOQUEADO** hasta cierre 15C.II completo (F.11+F.12 + G).
>
> **Anterior:** 2026-05-16 — **Sprint 15C.II Hardening — Fase F.9 (Reconciliación per-servicio `DC.45` — endpoint `POST /admin/services/:id/reconcile` + ADR-077 Amendment A8 `reconcileOne?()` opcional + cierre del cabo del CTA reconcile) cerrada y mergeada a master — PR [#82](https://github.com/yasmindanailov/dashboard/pull/82) squash-merge `55b3f86`** (bypass policy §6, **12ª aplicación** — patrón heredado #57/#60/#63/#65/#67/#70/#72/#74/#75/#77/#79; CI GitHub billing-bloqueada §A.9.10; las 3 condiciones cumplidas: motivo externo + `pnpm ci:check:full` verde [backend 53 suites, **754 passed + 6 skipped** = +23 vs F.8 + `nest build`; frontend `tsc --noEmit` + `eslint --max-warnings=0` + `next build` 32 static pages + 13 dynamic routes] + boot real backend verificado [PID 35364 post `nest build`; ruta `POST /admin/services/:id/reconcile` mapeada + executor `reconcileOne` registrado para `enhance_cp` en `onModuleInit`] + **smoke real automatizado Yasmin 7/10 escenarios** verificado end-to-end via curl + Mailpit auth flow + DB queries (E1 reconcile sin drift HTTP 200 shape correcto ✓ + E2 cooldown R6 coalesced flag ✓ + E3 race no reproducible mock fast — path 429 cubierto unit ✓ + E4 shortcircuit terminal 409 SERVICE_TERMINAL_NOT_RECONCILABLE ✓ + E8 audit timeline F.3 con 3 entries `service_reconcile_admin` access_log + `service.reconciled_single` change_log + `reconciled_external_change` evento ✓ + E9 ClientNotes vacío `driftsApplied=0` R3 condicional frozen ✓ + E10 admin overview `supports_reconcile_one=true` derivado server-side R9 ✓; E5/E6/E7 3 drift types runtime diferidos a Fase G E2E spec con `MockEnhanceServer` seed dinámico DC.49) + sección formal en el PR body; label `ready-for-e2e`) → post-merge doc-sync (este PR). **Tema**: la reconciliación contra el proveedor pasa de ser granular SOLO al nivel del cron L3 (reconcile-all del `ReconcileRegistryService`) a tener un endpoint admin **single-shot per-servicio** — el admin puede pulsar "Reconciliar contra el proveedor" en `<AdminDriftBanner>` (cuando `info.recoveryHint === 'reconcile'`) o en cada fila de drift del `<PluginOperationalOverview>` F.2 sin disparar la pasada completa del plugin. **Cierra el cabo de F.3** (hoy el CTA linkaba a la página de settings del plugin como placeholder — reconcile-all). **Capability-driven por presencia** (mismo patrón canónico A6 `testConnection?()` / A7 `ServiceInfo.ssl?`): plugins que implementen `reconcileOne?()` lo exponen + se auto-registran via `ReconcileRegistryService.registerReconcileOne(slug, executor)` desde `onModuleInit` del cron del plugin; plugins que NO lo expongan, el frontend oculta el CTA leyendo `supports_reconcile_one: false` derivado del admin overview F.2. Heredable a 15D RC / 15E Docker / 15G Plesk **sin tocar el `PluginManifest` declarativo**. **Contenido**: F.9.1 schema — migración `prisma/migrations/20260516130000_sprint15c_ii_f9_note_category_reconciliation` añade `reconciliation` al enum `NoteCategory` (9º valor) + `service.reconciled_single` al enum `triggered_by_action` (6º valor canónico VARCHAR(100), validación app-level). F.9.2 backend types — nuevos shapes en `core/provisioning/types.ts`: `ServiceDriftType` (3 valores: `subscription_missing` | `status_divergence` | `plan_divergence`) + `ServiceDrift` + `ServiceReconcileResult { driftsDetected: ServiceDrift[]; driftsApplied: ServiceDrift[]; reconciledAt: Date }` (separación explícita — `driftsApplied ⊆ driftsDetected`; el `ReconcileResult` agregado del reconcile-all NO se renombra — sufijo `Service*` heredable resuelve Amendment I naming clash) + `ProvisionerErrorCode.RECONCILE_ONE_NOT_SUPPORTED` + `ProvisionerPlugin.reconcileOne?()` interface (additivo, NO bumpea `contractVersion`). F.9.5-F.9.6 registry + cache — `ReconcileRegistryService.reconcileOne(slug, service)` con executor map paralelo `reconcileOneExecutors` (Amendment II DI clash — `ReconcileRegistryModule` leaf-importable preserva sin ciclo); cada plugin se auto-registra via `registerReconcileOne(slug, (service) => plugin.reconcileOne!(service))` desde `onModuleInit` del cron, capturando la instancia en closure (patrón heredado del `ReconcileExecutor` reconcile-all). `ProvisioningCacheService.tryAcquireReconcileSingleCooldown` + `cacheServiceReconcileResult` + `getCachedServiceReconcileResult` (30s TTL Redis, `SET NX EX` fail-OPEN, coalescing al cache — patrón paralelo a F.3 B.1 force-refresh 15s; R6 más generoso porque `reconcileOne` implica más calls al proveedor). F.9.7 orquestador — `ProvisioningService.reconcileServiceAsAdmin(serviceId, actorUserId)` constructor +1 arg (10 total): carga service via `loadServiceForView` (NotFound si no existe) + shortcircuit terminal (`cancelled`/`terminated` → 409 `SERVICE_TERMINAL_NOT_RECONCILABLE`) + guard defensivo `SERVICE_HAS_NO_PROVISIONER` (servicios legacy pre-Sprint 15A sin slug ni product.provisioner — polish review B3) + cooldown R6 (deniega → coalesced si hay cached, 429 sino) + delega a `reconcileRegistry.reconcileOne` (plugin aplica drifts safe-adopt FUERA de `$transaction` del orquestador — Sub-amendment III A8.5: plugin aplica, orquestador maneja transversales) + `ClientNote` automática solo si `driftsApplied > 0` (R3 frozen, vía `createFromServiceLifecycleAction` con `NoteCategory.reconciliation` + `triggered_by_action: 'service.reconciled_single'`; el `noteBody` usa `humanizeServiceDriftType()` helper localizado para mostrar "estado del servicio"/"plan del producto"/"suscripción del proveedor ausente" en lugar de enums técnicos crudos — polish review B1) + cache invalidation `service_info:<id>` + emit evento `service.reconciled_external_change` con discriminator `trigger: 'manual_single'` (R2 — reusa evento existente; ADR-080 sin cambios) + audit `change_log` (`service.reconciled_single` con `changes_after` detalle drift types) + `access_log` (`service_reconcile_admin` con `target_user_id` GAP-M GDPR portal). F.9.8 endpoint — `POST /admin/services/:id/reconcile` en `admin-provisioning.controller.ts` con triple guard (JwtAuthGuard + AdminOnlyGuard + PoliciesGuard) + `@CheckPolicies(Update Service)` + `@AuditAccess('Service')` + `ParseUUIDPipe`; **R7 re-mapeo HTTP** capa REST de presentación: try/catch sobre `ConflictException(RECONCILE_IN_PROGRESS)` del orquestador → `HttpException(payload, HttpStatus.TOO_MANY_REQUESTS)` con header `Retry-After: 30` (constante exportada `RECONCILE_SINGLE_COOLDOWN_SECONDS` — polish review B2 elimina magic number duplicado); el `ConflictException` original del service queda intacto (tests del service sin modificación). F.9.10b admin overview R9 — `AdminPluginsService.getOperationalOverview` suma `reconciliation.supports_reconcile_one: boolean` derivado server-side vía `reconcileRegistry.hasReconcileOneExecutor(slug)` — NO toca `PluginManifest` declarativo (capability-driven por presencia coherente A6/A7). F.9.10c plugin Enhance R8 — refactor `enhance-reconciliation.cron.ts`: `reconcileService` privado renombrado a `reconcileOneInternal` público con return `ServiceReconcileResult` (3 drift types mutuamente excluyentes, `driftsDetected.length ∈ {0,1}`); `runOnce` adapta conteo del summary leyendo `result.driftsDetected[].type` en lugar del antiguo `ReconcileChangeType | null`; `onModuleInit` añade `reconcileRegistry.registerReconcileOne('enhance_cp', (service) => this.reconcileOneInternal(service))` paralelo al `register()` reconcile-all existente. DRY: cron L3 (`runFor`) y endpoint admin invocan exactamente la misma lógica per-service. `plan_divergence` retorna `applied: false` (Amendment IV R4.1 — emit-only por billing implication; cambiar `services.metadata.plan_id` sin sincronizar `product.provisioner_config` rompe coherencia billing-provisioning). Plugin Enhance NO declara `reconcileOne` en `enhance.plugin.ts` — el executor se registra desde el cron directamente vía registry (preserva arquitectura plugin↔cron sin `forwardRef` bidireccional). F.9.11 frontend wire — `<AdminDriftBanner>` nueva prop `supportsReconcileOne?: boolean` + handler `executeReconcileSingle` (Toast UX R5 3 ramas success/warning/info + redirect timeline F.3 1.5s delay condicional + coalesced prefix R6 + 429 handler R7) en lugar del redirect placeholder F.3 a settings; `<DriftRowReconcileButton>` Client Component nuevo en `_shared/plugins/` (paralelo al banner, ~60 LOC, duplicación intencional vs hook compartido — 2 callers); `<PluginOperationalOverview>` añade columna "Acción" condicional a `overview.reconciliation.supports_reconcile_one`; Server Action federada `reconcileServiceAction(serviceId)` en `_shared/services/_actions.ts` (re-hidrata `reconciledAt` ISO→Date, propaga `coalesced?` + `retry_after_seconds`); `frontend/app/lib/api.ts` shapes espejo. **Polish post-review** (commit `9f6f455`, 10 fixes MAJOR del 3-agent review canónico sin bugs funcionales ni violaciones doctrinales R1..R9 frozen): B1 helper `humanizeServiceDriftType()` localizado + B2 export constante cooldown + B3 guard `SERVICE_HAS_NO_PROVISIONER` + F1+F3 helper compartido `_reconcile-toast.ts` con parámetro `withTimelineCta` explícito (banner=true, row button=false documentando divergencia intencional) + 14 keys i18n nuevas en `translations-es.ts` + F2 elimina flash UX `setReconciling(false)` durante 1.5s redirect + F4 comentario engañoso reemplazado + M6 `aria-busy` accesibilidad + T1 tests usan `ProvisionerPluginError` canónico + T2 test coalesced verifica TODOS los campos + A1 ADR-077 A8.3 aclara doctrina capability-driven por presencia (NO flag manifest). **+23 tests F.9**: `reconcile-registry.service.spec.ts` 13 nuevos + `provisioning.service.spec.ts` +8 (NotFound + shortcircuit terminal + cooldown 429 vs coalesced + happy path con `ClientNote` + R3 condicional) + `enhance-reconciliation.cron.spec.ts` +6 (5 drift types + onModuleInit registra ambos executors) + `plugin-contract.spec.ts` +1 invariante capability-driven A8 + `admin-plugins.service.spec.ts` +1 supports_reconcile_one. **4 Amendments doctrinales** descubiertos durante implementación + frozen en §A.11.10.6.2: I naming clash → `ServiceReconcileResult` sufijo `Service*` heredable + II DI clash leaf module → executor map paralelo + III R7..R9 post-handoff + IV R4.1 `plan_divergence` emit-only Enhance specifics. **ADRs**: [ADR-077 Amendment A8](../10-decisions/adr-077-contrato-provisioner-plugin-v2.md#amendments) (`reconcileOne?(service): Promise<ServiceReconcileResult>` opcional capability-driven por presencia + shapes `ServiceReconcileResult`/`ServiceDrift`/`ServiceDriftType` + `RECONCILE_ONE_NOT_SUPPORTED` + Sub-amendment A8.5 plugin aplica drifts vs orquestador maneja transversales — additivo, NO bumpea `contractVersion`) + [ADR-079 Amendment A5](../10-decisions/adr-079-tasks-bridge-unidireccional-y-notas-source-tracking.md#amendments) (`NoteCategory.reconciliation` 9º + `triggered_by_action.service.reconciled_single` 6º + reutilización `createFromServiceLifecycleAction(tx?)` canónico F.6). ADR-080 sin cambios (evento `service.reconciled_external_change` reusado con discriminator payload-level). ADR-083 sin Amendment A9 explícito (specifics documentados inline en docstring `reconcileOneInternal` con refs a R4/R4.1/R8 frozen). **Heredable a 15D RC / 15E Docker / 15G Plesk**: (a) patrón "plugin aplica drifts safe-adopt + orquestador maneja transversales" para cualquier futuro método opcional del contrato que mute estado (`detectAbuse?()`, `reapplyDnsZone?()`...); (b) sufijo `Service*` para shapes per-servicio (vs agregado sin prefijo del reconcile-all); (c) executor map paralelo en `ReconcileRegistryService` cuando el módulo leaf preserva ausencia de ciclo DI; (d) capability gating UI vía flag derivado en admin overview F.2 (NO en manifest declarativo) — coherente A6/A7. **Tras este merge**: 15C.II A→F (F.1+F.2+F.3+F.4+F.5+F.6+F.7+F.8+F.9) en master; **siguiente fase F.10 — deep-links curados al panel del proveedor** (en vez del único "Abrir panel del proveedor" genérico, atajos curados a las secciones más usadas — "Gestionar email", "Gestionar bases de datos", "Administrador de archivos", "Logs del sitio"... — capability-driven con cada plugin/instancia declarando qué secciones expone; materialización a decidir pre-código: inline actions plugin-internas tipo `sso_email`/`sso_databases`/... [slugs plugin-internos sin ADR, L14] o `getSsoUrl` gana parámetro `section?` [ADR-077 amendment + posible ADR-083 amendment]; cliente + admin `_shared/` capability-driven [ADR-070 cero `if (provisioner === 'X')`]). 15C.II G **CIERRE Sprint** sigue diferido (tests críticos faltantes + E2E spec extension + smoke real cierre + retrospectiva + dossier→`completed/`). **Sprint 15D RC sigue BLOQUEADO** hasta cierre 15C.II completo (F.10→F.12 + G).
>
> **Anterior:** 2026-05-16 — Fase F.8 (Alertas de cuota de disco — edge-triggered) PR [#79](https://github.com/yasmindanailov/dashboard/pull/79) squash-merge `46d2888` (bypass policy §6, **11ª aplicación** — CI GitHub billing-bloqueada §A.9.10; las 3 condiciones cumplidas: motivo externo + `pnpm ci:check:full` verde [backend 53 suites, **731 passed + 5 skipped** = +19 vs F.7 + `nest build`; frontend `tsc --noEmit` + `eslint --max-warnings=0` + `next build`] + boot real backend verificado [`QuotaThresholdDetectorService` en `ReconcileRegistryModule`, listener `NotificationsOnServiceQuotaThresholdCrossedListener` en `NotificationsModule`] + **smoke real Yasmin 4/4 escenarios** verificado contra `MockEnhanceServer` (MetricsBar ámbar 87% + barra `role="progressbar"` + advisory + 2 emails Mailpit + 2 notifs campana + **DoD anti-spam confirmado** — 4ª pasada NO duplica) + sección formal en el PR; label `ready-for-e2e`) → post-merge doc-sync (este PR). **Tema**: edge-triggered alerting (patrón Prometheus/AlertManager) para cuota de disco — el cliente recibe **un solo email** cuando cruza el umbral, MetricsBar colorea ámbar/rojo con accesibilidad ARIA. Heredable a cualquier plugin con `has_metrics` (15E Docker / 15G Plesk) **sin tocar el contrato `ProvisionerPlugin`**. **Contenido**: F.8.1 schema — enums Prisma `QuotaAlertResource.disk` + `QuotaAlertKind.{crossed_up,crossed_down}` + tabla `service_quota_alerts` (FK `ON DELETE Cascade`, índice composite, `DECIMAL(5,2)`); manifest Enhance gana `quota_alert_threshold_pct` en `configSchema` (`default:85, minimum:50, maximum:95`; 95% hardcoded crítico). F.8.2 backend — `QuotaThresholdDetectorService` transversal en `core/provisioning/` (provee `ReconcileRegistryModule` leaf-importable para evitar ciclo) con lógica edge-trigger en `$transaction` con `isolationLevel: Serializable` (M2 idempotency operativa explícita); emit del evento FUERA de la tx (R7). `EnhanceReconciliationCron.runAsExecutor()` extendido para llamar `detectQuotaThresholds()` tras `runOnce()` (lee threshold del install + itera services active + `api.calculateResourceUsage` lean). Listener `NotificationsOnServiceQuotaThresholdCrossedListener` consume el evento + dispatch BullMQ; plantilla seedeada `service.quota_threshold_crossed` (email + internal, locale ES, guard EC-T8-17). F.8.3 orquestador — `getInfoForUser` summary gana `quota_alert_threshold_pct: number | null` (paralelo a `provider_state_desync` F.4.1 — vive en summary, NO en `ServiceInfo` → ADR-077 intacto); helper `loadQuotaAlertThresholdFromInstall` con rango defensivo + R7. F.8.4 frontend — `MetricsBar.tsx` SC puro gana prop `quotaAlertThresholdPct?: number | null` (capability-driven); `<QuotaIndicatorBlock>` SC nuevo con barra `role="progressbar"` + aria-label + advisory ámbar/rojo (`urgentemente` en font-weight 600); colores DS coherentes con `invoice.paid/failed/overdue` (#10B981/#F59E0B-D97706/#DC2626); SOLO la fila de disco recibe coloreo. **Mejora profesional sobre dossier R7 literal** (L18): threshold viaja por summary del orquestador (no endpoint nuevo). **Hotfix UX descubierto en smoke** (commit `c9dfa6f`): `formatAjvDetailsMessage` + `humanizeAjvPath` + `translateAjvMessage` en `frontend/app/admin/settings/plugins/_actions.ts` traducen `details[]` Ajv del backend a ES — antes toast "Bad Request", ahora "Umbral de alerta de cuota de disco (%): debe ser mayor o igual que 50". **+20 tests F.8**: detector ×12 (M8 sin total / M4 boundary inclusivo / DoD anti-spam dos pasadas / crossed_down sin emit / re-cross genera 2º email / M2 tx_failed / M2 isolationLevel Serializable / R7 emit fail tras persist) + listener ×8 (dispatch variables / formato MB / fallback domain / R7 Prisma+dispatch fail / ConfigService fallback). **ADRs F.8**: NINGUNO (todo additivo a capa orquestador). **Lecciones operativas heredables**: (a) backend dist desfasado post `nest build` requiere reinicio manual antes del smoke real; (b) `pnpm seed` post-merge para nuevas plantillas; (c) edge-trigger DoD requiere state reset (DELETE filas smoke previas). **Diferidos apuntados (L18)**: DC.48 (bandwidth como F.8.x cuando se resuelva semántica del reset mensual) + DC.49 (`MockEnhanceServer` seed dinámico de usedResources per-subscriptionId — housekeeping pre-G.2 E2E spec). **Tras este merge**: 15C.II A→F (F.1+F.2+F.3+F.4+F.5+F.6+F.7+F.8) en master; **siguiente fase F.9 — reconcile per-servicio (DC.45)** (`ProvisionerPlugin.reconcileOne?(service)` ADR-077 amendment + endpoint `POST /admin/services/:id/reconcile` + wire del CTA en `AdminDriftBanner`). 15C.II G **CIERRE Sprint** sigue diferido. **Sprint 15D RC sigue BLOQUEADO** hasta cierre 15C.II completo (F.9→F.12 + G).
> **Cambios estructurales recientes:**
> - 📜 **[ADR-069 (2026-04-29)](../10-decisions/adr-069-estrategia-deploy-diferido.md)** reclasifica **Sprint 14 Deploy real** como **gate condicionado P-DEPLOY** (no está en cola activa). Se activa sólo con trigger de negocio explícito (cliente real, demo, captación, validación externa). La cola activa post-cierre Sprint 8 son features (Sprint 11 Provisioning como cabeza, Sprint 10 Infrastructure independiente, sub-sprint billing prorrateo cross-plan ADR-077 propuesto, Sprint 12 Settings+KB, Sprint 13 Hardening) según valor funcional.
> - **Sprint 11 Fases 11.A + 11.B mergeadas en master 2026-05-02** — ADR-077 (contrato canónico `ProvisionerPlugin` v2 congelado) + orquestador + cola BullMQ `provisioning-dispatch` + cache Redis dedicado (DB 2) + plugin registry. **183/183 unit verde** (157 base Sprint 8 + 26 nuevos). Plugins concretos pendientes (Fase 11.C). Plan canónico abajo.
> - **Sprint 8 (Tasks + Support Inside) cerrado 2026-05-01** — 5 ADRs nacieron en el sprint (072..076), 157/157 unit + 117/117 E2E verde, 5 migraciones. Detalle en [`completed/sprint-8-tasks-support-inside.md`](./completed/sprint-8-tasks-support-inside.md).
> - **Sprint 11.5 (MinIO Storage)** añadido como sprint independiente — antes estaba dentro del Sprint 14 Deploy. Desbloquea adjuntos chat/tickets sin obligar a desplegar a producción.
> - **Sprint 14 (Deploy)** limpiado — solo lo que realmente requiere producción real. **Hoy gate condicionado bajo ADR-069.**
> - **Sprint 15 (Plugins)** partido en sub-sprints 15A-15H independientes — cada plugin se aborda según necesidad real, no en cadena.

---

## 🔄 Sprint 7 — Billing Hardening + Support

**Estado:** ~95% completo, **bloqueado por dependencias externas** para los pasos restantes.
**Inicio:** Sprint 6 (continuación). **Cierre formal estimado:** cuando se desbloqueen Sprints 14, 15, 8.

### ✅ Lo cerrado (verificado contra código)

- **Billing hardening (5 pasos):** admin checkout selector, validar `targetUserId`, perfil de facturación contra cliente destino, IVA recálculo en edición, descuento anual aplicado.
- **Support core (8 pasos):** SupportService completo, WebSocket gateway con auth dual JWT+guest, chat tiempo real, arquitectura dual chat+ticket, escalación, panel agente 3 columnas, bandeja tickets, detalle conversación, plantillas de email, admin.md.
- **Support hardening (25 pasos H1-H25):** dedup WS+REST, escalación única, cleanup typing, post-escalación redirige al ticket, página `[id]` diferenciada, sorting waiting_agent, indicador asignación, unread separado por type, stats filtrados, sync notas, nota obligatoria al reabrir, coherencia acciones panel, sidebar contexto cliente, etc.
- **Chat anónimo (8 pasos):** guest token, endpoint guest, rate limit 3/h, gateway auth fallback, widget guest mode, vinculación por email, vinculación manual, cleanup cron 30d.
- **Refactorización R15 (9 pasos R15.1-R15.9):** chats/page (907→77), ChatWidget (671→155), support/page (557→102), support/[id] (733→88), checkout (570→233), layout (394→79), clients/[id] (683→243), products (323→282), products/new (347→296). **Backend support refactor:** support.service (1054→90 fachada + 4 sub-servicios), gateway (526→232).

### ⏳ Lo pendiente (todo bloqueado)

| Paso | Bloqueado por | Cuándo se desbloquea |
|------|---------------|----------------------|
| 7.6.1-3 Horario soporte | Nada — se puede hacer ya | Decisión de priorizar |
| 7.7 Adjuntos archivos | **Sprint 14 — MinIO** | Tras Sprint 14 |
| 7.6.1-4 Ticket UX (rich text + email-style + adjuntos + subject editable) | **Sprint 7.5 Fase 2 + Sprint 14 MinIO** | Cuando ambos cierren |
| 7.8/7.9 IA filtro + copilot | **Sprint 15 Plugins (Claude AI)** | Tras Sprint 15 |
| 7.SI.1/2 Support Inside (badge, página cliente) | **Sprint 8 Fase D** | Tras cierre Sprint 8 |

**Acción recomendada:** **NO cerrar Sprint 7 formalmente** todavía. Cuando todos los bloqueos se resuelvan en sus respectivos sprints, se cierra de una vez.

---

## 🔄 Sprint 7.5 — Design System Foundation

**Estado:** Fase 1 ✅ cerrada. Fase 2 parcial.

### ✅ Fase 1 — Tokens y componentes base (D1–D10f, D11)

Verificada completa contra código en `frontend/components/ui/`:

- D1 Tokens CSS, D2 Button, D3 Input/Select/SearchInput/Textarea, D4 Badge/StatusDot, D5 Card, D6 Modal, D7 Table, D8 Toast, D9 EmptyState/Skeleton, D10 Avatar/Tooltip/Dropdown, D10b Pagination/StatsCard/AlertBanner, D10c UI_SPEC.md, D10d StatusTabs, D10e Breadcrumb, D10f Tabs.
- D11 Dashboard shell migrado (Sidebar, Topbar, Layout) — CSS modules, eliminados inline styles.

### ⏳ Fase 2 — Migración de páginas existentes (parcial)

Algunas páginas migradas en Sprint 7 R15 (chats, support, checkout, layout, clients, products). Otras pendientes — el playbook no enumera el % exacto. Acción: **cuando se aborde una página por trabajo de feature, migrarla al DS en el mismo PR** (oportunismo) en lugar de un sprint dedicado de migración masiva.

---

## ✅ Sprint 8 — Tasks + Support Inside (cerrado 2026-05-01)

> Sprint cerrado al 100%. Movido a [`completed/sprint-8-tasks-support-inside.md`](./completed/sprint-8-tasks-support-inside.md) con retrospectiva completa, métricas, ADRs nacidos (072..076) y lecciones aprendidas. Cobertura final: 157/157 unit + 117/117 E2E verde, 5 migraciones aplicadas.

> Las páginas operativas del módulo viven en:
> - [`docs/features/tasks/admin.md`](../features/tasks/admin.md) — operativa diaria del módulo Tasks
> - [`docs/features/tasks/agent.md`](../features/tasks/agent.md) — guía del agente
> - [`docs/features/support-inside/admin.md`](../features/support-inside/admin.md) — operativa Support Inside (staff)
> - [`docs/features/support-inside/client.md`](../features/support-inside/client.md) — guía cliente Support Inside

---

## ✅ Sprint 9 — Audit + Notifications Full + BullMQ + DLQ (P1.1) (cerrado 2026-04-27)

> Sprint cerrado al 100% del alcance MVP. Movido a [`completed/sprint-9-audit-notifications-bullmq.md`](./completed/sprint-9-audit-notifications-bullmq.md) el 2026-05-01 (saneamiento documental post-Sprint 8 cierre). DoD verificado: typecheck + lint + build + 21/21 unit + 30/30 E2E + boot real con 3 colas BullMQ + 8 crons in-process. P1.1 desbloquea Sprint 14 Deploy sin bloqueos críticos.

---

## ✅ Sprint 9.5 — UX admin de notifications + cabos sueltos (P1.1.5) (cerrado 2026-04-27)

> Sprint cerrado en 1 sesión densa. Movido a [`completed/sprint-9-5-ux-admin-notifications.md`](./completed/sprint-9-5-ux-admin-notifications.md) el 2026-05-01.

---

## ✅ Sprint 11.5 — MinIO Storage local (P1.2) (cerrado 2026-04-26)

> Sub-sprint independiente que aisló storage local del Sprint 14 Deploy para desbloquear adjuntos chat/tickets. Movido a [`completed/sprint-11-5-minio-storage.md`](./completed/sprint-11-5-minio-storage.md) el 2026-05-01.

---

## ✅ Sprint 9.6 — Split admin/cliente retroactivo + 3 portales raíz + permisos granulares (P1.1.6 / DC.7) (cerrado 2026-04-28)

> Sprint cerrado en 1 sesión densa, 12 commits encadenados. ADR-066 + ADR-067 + ADR-068 nacieron aquí. Tres portales raíz formalizados (`/admin/*`, `/dashboard/*`, `/partner/*`). Retrospectiva ejecutiva + plan canónico completo en [`completed/sprint-9-6-split-admin-cliente.md`](./completed/sprint-9-6-split-admin-cliente.md).

---

## ✅ Sprint 11 — Provisioning (P2.1) (cerrado 2026-05-02)

> Sprint cerrado al 100%. Movido a [`completed/sprint-11-provisioning.md`](./completed/sprint-11-provisioning.md) con retrospectiva completa, métricas, 2 ADRs nacidos (077 contrato canónico `ProvisionerPlugin` v2 + 078 auth server-side cookies httpOnly) y lecciones aprendidas. Cobertura final: **241/241 unit + 129/129 E2E verde**, 1 migración aplicada, 7 PRs encadenados (#13 ADR-077 → #14 chasis → #15 cierre doc 11.B → #16 11.C plugins triviales → #17 ADR-078 → #18 11.D REST + frontend → #19 sync), 8 endpoints REST nuevos, 1 cola BullMQ nueva (`provisioning-dispatch`), 5 eventos `service.*` nuevos, 4 DCs nuevas registradas en `backlog.md` (DC.27/29/30/31).

> **Documentación canónica del módulo:**
> - [`docs/features/services/admin.md`](../features/services/admin.md) — operativa diaria del módulo Services para staff.
> - [`docs/features/services/client.md`](../features/services/client.md) — guía cliente.
> - [`docs/features/provisioning/admin.md`](../features/provisioning/admin.md) — vista interna del orquestador.
> - [`docs/20-modules/provisioning/contract.md`](../20-modules/provisioning/contract.md) — contrato canónico (12 secciones, marcado ✅ implementado).

---


## ✅ Sprint 13.5 — Hardening + Saneamiento de Deuda Continua (cerrado 2026-05-03)

> Sub-sprint dedicado a cerrar deuda continua acumulada antes de Sprint 15A Plugin Framework. Movido a [`completed/sprint-13-5-hardening-deuda-continua.md`](./completed/sprint-13-5-hardening-deuda-continua.md) con retrospectiva completa, métricas, lecciones aprendidas y plan de Sprint 13.5.5 CI Infra (sub-sprint nacido del aprendizaje). 8 DCs cerradas (DC.32/33/34 + DC.14/37/38 + DC.8/11/15 parciales) + 2 diferidas (DC.13 + DC.27 → Sprint 13.5.5). Cobertura final: **183/183 unit + 118/118 E2E verde** sin regresión.

---

## ✅ Sprint 13.5.5 — CI Infra (cerrado 2026-05-03)

> Sub-sprint cerrado al 100%. Movido a [`completed/sprint-13-5-5-ci-infra.md`](./completed/sprint-13-5-5-ci-infra.md) con retrospectiva completa, métricas, decisión arquitectónica + lecciones aprendidas. **DC.27 ✅** (imagen oficial Playwright `mcr.microsoft.com/playwright:v1.59.1-noble` + service names + MinIO `bitnamilegacy/minio:2025.7.23-debian-12-r5` como service container) + **DC.13 ✅ parcial-canónica** (sharding CI con `--shard=N/M` × 3 shards paralelos, wall-clock CI 25 min → ~10 min). Paralelización local con `workers > 1` **diferida a sub-sprint condicionado** Sprint 13.5.6 (trigger: suite local > 2 min) — el cuello real estaba en CI, no en local. Decisión arquitectónica completa en la retrospectiva §4.

---

## ✅ Sprint 16 — Tasks refactor + Notes consolidation (P2.1.5) (cerrado 2026-05-02)

> Sprint cerrado al 100%. Movido a [`completed/sprint-16-tasks-notes-refactor.md`](./completed/sprint-16-tasks-notes-refactor.md) con retrospectiva completa, métricas, ADR nacido (ADR-079 + Amendments A1/A2/A3) y lecciones aprendidas. Cobertura final: **183/183 unit + 118/118 E2E verde**, 1 migración aplicada (`sprint16_tasks_notes_refactor`), 4 PRs encadenados (#21 ADR-079 → #22 backend → #23 sync → #24 frontend + amendments + cierre documental).

> **Documentación canónica del módulo:**
> - [`docs/20-modules/tasks/contract.md`](../20-modules/tasks/contract.md) — Contract canónico tasks (post-ADR-079).
> - [`docs/30-data/tasks.md`](../30-data/tasks.md) — Schema canónico tasks.
> - [`docs/30-data/clients.md`](../30-data/clients.md) — Schema canónico `client_notes` (consolidación con source tracking).
> - [`docs/features/tasks/admin.md`](../features/tasks/admin.md) — Operativa admin.
> - [`docs/features/tasks/agent.md`](../features/tasks/agent.md) — Guía agente.
> - [`docs/features/notes/admin.md`](../features/notes/admin.md) — Operativa notas consolidadas (nuevo).
> - [`docs/features/support/lifecycle.md`](../features/support/lifecycle.md) — Lifecycle ticket vs chat (Amendments A1+A3, nuevo).

---

## ✅ Sprint 13 §13.AUTH — Auth server-side con cookies httpOnly + Server Components nativos (cerrado 2026-05-03)

> Sprint cerrado al 100%. Movido a [`completed/sprint-13-auth-cookies-httponly.md`](./completed/sprint-13-auth-cookies-httponly.md) con retrospectiva completa, métricas, ADR-078 Amendment A1 (Modelo A), 11 commits encadenados en rama `sprint13-auth-cookies-httponly`, lecciones aprendidas (smoke HTTP real desbloqueando bugs IPv6 + jti, decisión arquitectónica Opción B ESLint per-línea, modelo cross-origin cookies httpOnly Next.js + handshake WS via endpoint dedicado). Cobertura final: **198/198 unit backend verde + 3 specs E2E nuevos** (`auth-cookies-flow` + `auth-replay-detection` + `auth-no-localStorage`) + frontend `pnpm typecheck` + `pnpm lint:check --max-warnings=0` + `pnpm build` verdes. Cierra **DC.6 + DC.28**.

> **Documentación canónica del módulo (post-Sprint 13 §13.AUTH):**
> - [ADR-078 + Amendment A1](../10-decisions/adr-078-auth-server-side-cookies-httponly.md) — Modelo A (cookies httpOnly viven en dominio Next.js).
> - [`docs/00-foundations/rules.md` §R17](../00-foundations/rules.md#r17--jwt-en-cookies-httponly-de-nextjs-no-en-localstorage) — JWT en cookies httpOnly de Next.js, NO en localStorage.
> - [`docs/20-modules/auth/contract.md`](../20-modules/auth/contract.md) — §5 (`/auth/ws-token`), §7 (`auth.refresh_replay_detected`), §11 (env vars frontend `BACKEND_URL` + `NEXT_RUNTIME_SECRET`), §14 (AUTH-INV-8/9).
> - [`docs/50-operations/api-errors.md`](../50-operations/api-errors.md) — `AUTH_REPLAY_DETECTED`.

---

## ✅ Sprint 15A — Plugin Framework (P2.2) (cerrado 2026-05-06)

> Sprint cerrado al 100% y mergeado a master `bee90d8` (squash-merge PR #31). Movido a [`completed/sprint-15a-plugin-framework.md`](./completed/sprint-15a-plugin-framework.md) con retrospectiva completa, métricas, ADR-080 nacido (Plugin Framework: manifest declarativo + vault de secretos AES-256-GCM + loader desde DB + circuit breaker tras interface + 5 eventos `plugin.*`), 8 commits encadenados en rama `sprint15a-plugin-framework` (6 originales + Amendment A1 con 2 fixes CI post-cierre: ENCRYPTION_KEY 64 hex + audit_change_log entity_id UUID v5 derivado del slug), 9 lecciones aprendidas. Cobertura final: **255/255 unit verde** (+57 vs base post Sprint 13: 18 vault + 11 registry + 16 breaker + 15 admin-plugins + 2 manifest contract) + **7 E2E nuevos** (`admin-plugins.spec.ts`) + frontend `pnpm typecheck` + `pnpm lint:check --max-warnings=0` + `pnpm build` verdes. Plugins reales 15B/C/D/E/G heredan TODO el framework — solo declaran 6 métodos del contrato + manifest. PR [#31](https://github.com/yasmindanailov/dashboard/pull/31).

> **Documentación canónica del módulo (post-Sprint 15A):**
> - [ADR-080](../10-decisions/adr-080-plugin-framework.md) — Plugin Framework canónico (manifest declarativo JSON-Schema 7 + tabla `plugin_installs` + `SecretVaultService` AES-256-GCM + loader runtime desde DB + circuit breaker tras interface).
> - [`docs/30-data/plugin-installs.md`](../30-data/plugin-installs.md) — Schema canónico `plugin_installs` con justificación PK natural slug.
> - [`docs/features/provisioning/admin-plugins.md`](../features/provisioning/admin-plugins.md) — Operativa diaria del superadmin (4 flujos canónicos + auditoría + errores comunes).
> - [`docs/20-modules/_events.md` §🔌 plugin.*](../20-modules/_events.md) — 5 eventos `plugin.*` + 3 listeners nuevos.
> - [`docs/20-modules/provisioning/contract.md` §7 Admin Plugin Framework](../20-modules/provisioning/contract.md) — REST endpoints `/admin/plugins/*` + sección Pendientes actualizada.
> - [`docs/00-foundations/glossary.md`](../00-foundations/glossary.md) — 3 términos canónicos nuevos: Plugin Manifest, Secret Vault, Circuit Breaker.

---

## 🔄 Sprint 15C — Plugin Enhance CP (P2.3) — 90%, hardening Sprint 15C.II requerido

> Cabeza de cola activa P2.3 — primer plugin SaaS real post Sprint 15A. **11 fases ejecutadas** (15C.A → 15C.I) en 11 PRs encadenados (#36..#51 + cierre Fase I) sin desincronización a lo largo de 2026-05-08..2026-05-10. Reformulación 2026-05-09 (Fase 15C.E PR #44 review) añadió 2 fases nuevas (E.2 frontend acciones curadas + J cierre real con admin page) que cerraron gaps estructurales del alcance frontend originalmente implícito. Cierra el bloqueante operacional para Sprint 15D RC — el cluster Enhance opera autoridad DNS para los `ns1.aelium.net` / `ns2.aelium.net` que registrar dominios apunten al sistema correcto.
>
> **Estado real 2026-05-10:** Fase 15C.I aplicada en rama `sprint15c-fase-i-cierre-sprint` (E2E spec 6/6 + i18n + doc operativa + retrospectiva). **Smoke real Yasmin reveló 18 issues + 4 decisiones doctrinales** que requieren un **sub-sprint dedicado de hardening**. Fixes valiosos no commiteados en rama (11+ correcciones a `$queryRaw`, `AdminPluginUpdateDto`, `ActionsBar/SsoButton useToast`, mensajes engañosos, INTERNAL_HELPER_SLUGS DNS, `view_disk/bandwidth adminOnly`, `force_resync` description tooltip, `translateSchema()` rjsf, `ServiceHeader` i18n). Suite backend **488/493 unit verde + 5 skipped**. **Sprint 15D RC BLOQUEADO** hasta cierre Sprint 15C.II hardening.
>
> **Sub-sprint hardening (en ejecución):** [`docs/60-roadmap/sprint-15c-ii-hardening-enhance-dossier.md`](./sprint-15c-ii-hardening-enhance-dossier.md) — fases A→G; **A + B + C + D + E + F (F.1→F.12) ✅ TODAS mergeadas a master — Fase F COMPLETA (2026-05-20); G (cierre) es la única fase restante**. Continuación de la Fase F re-planificada 2026-05-12 (testing de F.1 destapó un agujero de robustez del status de suspensión + decisión Yasmin: traer al sprint los apuntados de backlog `DC.44`/`DC.45` + deep-links/notas/mini-badge/reenviar-notif/cross-link-billing + integrar las acciones críticas de servicio con el sistema transversal `ClientNote` + fase de layout canónico al final + "más fases, más pequeñas") — **10 fases**: **F.4** robustez del status de suspensión (PRÓXIMA) → **F.5** `DC.44` billing-suspend-unify → **F.6** notas operativas vía `ClientNote` → **F.7** SSL status read-only → **F.8** alertas de cuota → **F.9** reconcile per-servicio (`DC.45`) → **F.10** deep-links curados al panel → **F.11** conveniencias operativas (mini-badge salud + reenviar notif + cross-link billing) → **F.12** layout canónico (con freeze gate) → **G** cierre. Plan detallado por fase (scope + ADR amendments + valoración pre-código + DoD) en **dossier §A.11.10**.
>
> - ✅ **Fase A** (decisiones doctrinales A1-A4 frozen + 8 gaps audit + plan 7 fases) — PR #53.
> - ✅ **Fase B** (refresh metrics ↻ + reconcile dual) — PR #54.
> - ✅ **Fase C** (drift UX por rol + i18n + a11y + service detail hardening + 7 rounds smoke real → 8 bugs cerrados) — PR #55. Suite **505/510 unit verde**.
> - ✅ **Fase D** (audit-sanitizer.ts + listener email password reset + plantilla seedeada `service.password_reset` + i18n + E2E test 7) — PR [#57](https://github.com/yasmindanailov/dashboard/pull/57) mergeado 2026-05-10 (commit `c3b519e`) **con bypass CI documentado** por incidente billing GitHub Actions externo (validación local exhaustiva: 576/581 unit + lint + typecheck both + boot real). Suite **576/581 unit verde** (+21 tests nuevos). Smoke real Fase D descubrió **6 gaps nuevos** apuntados Fase E/F: BUG-15CII-I (heurística showReprovision), GAP-15CII-J (cancelar servicio UI), GAP-15CII-K (force_resync UI — re-evaluar antes implementar UI por redundancia con botón ↻), GAP-15CII-L (DNS records UI admin), GAP-15CII-M (audit timeline per-service), GAP-15CII-N (error_log módulo origen). Suspend doctrina transversal expandida §A.9.6.1 (heredable a billing/support/GDPR/maintenance).
> - ✅ **Fase E** (gold standard — Yasmin "cada punto al más alto estándar") — PR [#60](https://github.com/yasmindanailov/dashboard/pull/60) mergeado a master `1250a2e` 2026-05-11 (bypass policy §6 — CI GitHub bloqueada billing externo; rama temporal eliminada). **ADR-077 Amendment A5** (`recoveryHint` estructurado en `ServiceInfo` — la UI ramifica por contrato, NUNCA por `statusReason.endsWith`; cierra BUG-15CII-I por construcción) + **ADR-083 Amendment A5** (rename `force_resync` → `recalculate_provider_metrics` + reubicación a `AdminServiceOperationsCard` con etiquetado preciso — **corrige A4.2** inexacto; `getServiceInfo` puebla `recoveryHint` incl. detección `plan_divergence`; `list_dns_records` expone DNSSEC read-only). UI admin DNS nativa `/admin/services/[id]/dns` reusando componentes shared (`DnsRecordsManager`/`DnsRecordForm`/`DnsExternallyBanner` movidos a `_shared/services/dns/_components/` + prop `isAdmin`) — gold standard: TTL presets dropdown + validación client-side por kind (A/AAAA/CNAME/MX/SRV/CAA) + dedup + conflicto CNAME RFC 1034 + Badge DNSSEC + nota propagación + empty state. `CancelServiceModal` (typing-confirm del dominio + motivo dropdown + nota interna + toggle "notificar al cliente") + `deprovisionServiceAction` + DTO `notify_client` + listener nuevo `notifications-on-service-cancelled` + templates seedeados `service.cancelled` (email + campana). 4 gaps cerrados: BUG-15CII-I + GAP-15CII-J/K/L. Suite backend **591/596 unit verde** (45 suites, +1 nueva) + `pnpm ci:check:full` verde (typecheck+lint+tests+builds ambos) + boot real backend verificado. Handoff Fase F verbatim §A.10.1. 5 lecciones nuevas heredables §A.10.3 (L13 UI ramifica por contrato NUNCA por display strings; L14 naming honesto — slugs de inline action son plugin-internos, renombrar es seguro; L15 "demote, don't delete" progressive disclosure power-user ops; L16 componentes compartidos cliente/admin `_shared/` + prop isAdmin; L17 checklist flujo destructivo grado profesional).
> - **Fase F** (partida en F.1 / F.2 / F.3 — decisión Yasmin 2026-05-12; **F.1+F.2+F.3 ✅ mergeadas a master**; F.4 añadida 2026-05-12):
>   - ✅ **F.1** ([ADR-077 Amendment A4.5](../10-decisions/adr-077-contrato-provisioner-plugin-v2.md#a45-materialización-sprint-15cii-fase-f1-2026-05-12)) — **mergeada a master, PR [#63](https://github.com/yasmindanailov/dashboard/pull/63) squash-merge `b6675ed`** (bypass policy §6, CI bloqueada billing): suspend/unsuspend completo. `PluginCapabilities.supports_suspend` + tipo `SuspensionReason` + materialización **inline actions** `suspend_service`/`unsuspend_service` (NO métodos dedicados del contrato — reconcilia el apuntado §A.9.6.1 con A4.4 frozen) + `reason` enum canónico (mejora sobre string libre). Plugin Enhance + orquestador `suspendAsAdmin`/`unsuspendAsAdmin` + endpoints `POST /admin/services/:id/suspend|unsuspend` + listeners email (el de suspended tolera la forma legacy del worker de impago) + 4 plantillas + frontend `SuspendServiceModal` + banner amarillo + i18n + contract test + `getInfoForUser` summary `suspended_at`/`suspension_reason`. Suite **625/630 unit verde** (47 suites, +2 listener spec). `pnpm ci:check:full` verde + boot real. Diferido `DC.NEW-15CII-BILLING-SUSPEND-UNIFY`.
>   - ✅ **F.2** ([ADR-083 Amendment A6](../10-decisions/adr-083-plugin-enhance-cp-specifics.md#amendments)) — **mergeada a master, PR [#65](https://github.com/yasmindanailov/dashboard/pull/65) squash-merge `2a3cce8`** (bypass policy §6, CI bloqueada billing — `ci:check:full` verde + boot real + doc formal): admin overview operativo `/admin/settings/plugins/[slug]`. `<PluginOperationalOverview slug>` reusable en `frontend/app/_shared/plugins/` (ADR-083 A4.4 — ubicación que ya fijaba) + badge salud derivado (operational/degraded/down/disabled — de circuit breakers + secrets requeridos + última reconciliación) + stats grid 4 cards (activos / suspendidos / drifts 24h / breaker state in-process — etiquetado) + última/próxima reconciliación + tabla recent drifts (`entity_type='Service' AND action='reconciled_external_change'` filtrado por `_meta.plugin_slug`; cada fila enlaza a `/admin/services/[id]` — repunta a `…/audit` en F.3 vía helper `serviceDetailHref`) + reconcile-all/test conexión/form config (ya existen). **Evento rollup NUEVO `plugin.reconcile_completed`** (emitido por el cron del plugin tras cada pasada, `trigger='cron'|'manual'`) → `AuditOnPluginReconcileCompletedListener` → `audit_change_log` `entity_type='Plugin'` `action='reconcile_completed'` — el overview lo lee para "última reconciliación" como **estado observado, no inferido del schedule**. Backend: `core/provisioning/plugin-audit-id.util.ts` (extraído `deriveAuditEntityId`), `ReconcileRegistryService.register(slug, executor, { intervalSeconds })` opcional + `getScheduleMeta`, `AdminPluginsService.getOperationalOverview` + DTO `PluginOperationalOverview` plugin-agnóstico + endpoint `GET /admin/plugins/:slug/operational-overview`. **Corrige 3 apuntados del dossier** (ver §A.11.7): query de drifts es `action='reconciled_external_change'` (no `LIKE 'service.reconciled%'` — ese es el nombre del evento), componente en `_shared/plugins/` (no en `[slug]/_components/`), "última reconciliación" persistida (no inferida). Suite **637 passed + 5 skipped** (48 suites, +8: 6 `getOperationalOverview` + listener spec nuevo + 2 cron emit). `pnpm ci:check:full` verde + boot real (ruta nueva mapeada + listener registrado + cron `intervalSeconds` ok). Handoff F.3 + estado real F.2 en dossier §A.11.7/§A.11.8.
>   - ✅ **F.3** (cierre Fase F) — **mergeada a master, PR [#67](https://github.com/yasmindanailov/dashboard/pull/67) squash-merge `2a33850`** (bypass policy §6 — CI GitHub bloqueada billing externo §A.9.10; las 3 condiciones cumplidas: `pnpm ci:check:full` verde [49 suites, 664 passed + 5 skipped; `nest build` + `next build`] + boot real backend [rutas `GET|POST /api/v1/(admin/)services/:id/(audit|refresh)` mapeadas, DI sin errores] + sección formal en el cuerpo del PR; rama temporal eliminada; label `ready-for-e2e`) → post-merge doc-sync (patrón #61/#64/#66). **ADRs nuevos**: [ADR-083 Amendment A7](../10-decisions/adr-083-plugin-enhance-cp-specifics.md#amendments) (registro comprensivo de toda F.3 — A7.1 audit timeline · A7.2 error module · A7.3 testConnection · A7.4 cache TTL + timeout · A7.5 CTA · A7.6 cooldown · A7.7 catálogo · A7.8 backlog · A7.9 validación) + [ADR-077 Amendment A6](../10-decisions/adr-077-contrato-provisioner-plugin-v2.md#amendments) (`testConnection?()` 7º método opcional [obligatorio si `manifest.testConnectionMethod === 'custom'`] + `ProvisionerPluginError.module?` mutable) + [ADR-080 Amendment C](../10-decisions/adr-080-plugin-framework.md#amendments) (`PluginManifest.serviceInfoCacheTtlSeconds?` — patrón B.5 del propio ADR-080; + fila `plugin.reconcile_completed` en §6). **Contenido**: GAP-15CII-M audit timeline per-servicio (`/admin|/dashboard/services/[id]/audit` — `AuditService.getServiceTimeline` `UNION ALL` `audit_change_log`+`audit_access_log` + keyset cursor `(created_at,id)` + recorte GDPR cliente [whitelist `CLIENT_VISIBLE_TIMELINE_ACTIONS` incl. `admin_sso_impersonation` **con detalle**] + `ProvisioningService.getServiceTimelineForUser` ownership + endpoints `GET /api/v1/(admin/)services/:id/audit` + renderer reusable `ServiceAuditTimeline` + 2 páginas + i18n + `serviceDetailHref` overview F.2 repuntado a `…/audit`; **migración** índice de expresión parcial `audit_access_log ((metadata->>'resource_id')) WHERE metadata ? 'resource_id'` — no en `schema.prisma`) + GAP-15CII-N (`ProvisionerPluginError.module?` mutable + `GlobalExceptionFilter.resolveErrorModule` recorre `cause` duck-typed → `error_log.module='provisioning.<slug>'` no `'http'`) + G8 (test-connection `'custom'`: contrato `testConnection?()` + Enhance probe `GET /version`+`GET /orgs/{masterOrgId}` ADR-083 §1 dec.5 + `buildSyntheticService` `metadata:{}` defensivo) + G4 (`PluginManifest.serviceInfoCacheTtlSeconds?` precedencia manifest>setting>60s floor 5s) + G5 (`EnhanceHttpClient` 30s→15s fail-fast, NO breaker anidado — anti-patrón blanket protection; mover el breaker a envolver el client diferido v1.1) + CTA "Reconciliar contra el proveedor" en `AdminDriftBanner` cuando `recoveryHint==='reconcile'` → link a plugin settings + **B.1 cooldown server-side del force-refresh per-`serviceId`** (`ProvisioningCacheService.tryAcquireRefreshCooldown` `SET refresh_cooldown:<id> 1 EX 15 NX` fail-OPEN; `getInfoForUser` si `forceRevalidate` y ventana activa → degrada a lectura cacheada normal [coalescing — sin error, respuesta sigue siendo `ServiceDetailResponse` válido → sin cambios en el frontend]; cliente y admin comparten la ventana; ambos endpoints `refresh` ya pasan por `getInfoForUser` → cubiertos sin tocar controllers) + B.2 (`plugin.reconcile_completed` en el catálogo §6 de ADR-080). **Decisión clave B.1**: cooldown per-`serviceId` en Redis (coalescing a cache), NO `@Throttle` por IP — el throttle por IP no acota lo que cuesta (1 llamada a orchd por servicio), se rompe bajo NAT, `forceRevalidate` bypasea el cache → stampede concurrente que el `SET NX` resuelve gratis; en cooldown-hit no hay 429 ni `Retry-After`, solo cache → menos código + menos info-leak. Backlog nuevo `DC.NEW-15CII-RECONCILE-SINGLE` = DC.45. Suite **664 passed + 5 skipped** (49 suites). Dossier §A.11.9 (commit-by-commit + decisiones).
> - **Continuación Fase F — re-plan 2026-05-12 (refinado)** (el testing de F.1 destapó un agujero de robustez del status de suspensión + decisión Yasmin: traer al sprint los apuntados de backlog `DC.44`/`DC.45` + deep-links/notas/mini-badge/reenviar-notif/cross-link-billing + integrar las acciones críticas de servicio con el sistema transversal `ClientNote` [no un `service_notes` propio] + una fase de layout canónico al final + "más fases, más pequeñas" → cerrar el plugin Enhance + el módulo de servicios a estándar alto antes de desbloquear 15D RC). **10 fases ordenadas por prioridad = riesgo descendente** — detalle por fase en **dossier §A.11.10**:
>   - ⏳ **F.4 — Robustez del status de suspensión** (PRÓXIMA): `getInfoForUser` reconcilia el status administrativo (si `service.status==='suspended'` ⇒ `info.status='suspended'` override + `availableActions` re-filtrado [fuera `suspend_service`, dentro `unsuspend_service`] + flag `provider_state_desync` en el summary; `services.status` autoritativo para el lifecycle *administrativo*, distinto de DH-INV-6 *operacional*; capa orquestador, heredable; NO toca el contrato) + banner de suspensión para el cliente con CTA por motivo + aviso de desync en la UI admin. **Arregla el estado roto descubierto en testing** (banner "Servicio suspendido" sin botón "Reanudar"; desync inverso → `409 SERVICE_NOT_SUSPENDED` en un botón visible). Rama `sprint15c-ii-fase-f4-lifecycle-robustness`. Sin ADR del contrato (`provider_state_desync` es summary); nota en ADR-082 DH-INV-6 / ADR-070.
>   - ⏳ **F.5 — `DC.44` billing-suspend-unify**: `ServiceLifecycleWorker.autoSuspendServices` (cron impago) → `ProvisioningService.suspendAsAdmin(serviceId,{reason:'overdue_payment'},<actor sistema>,...)` en vez de su propio `prisma.update` + forma reducida; convención "actor sistema" (`actor_user_id:null` + `metadata.actor:'system:billing-overdue-cron'`); auto-reactivación al pagar (`billing-on-invoice-paid` → `unsuspendAsAdmin`). Va **antes** de F.6 para que la suspensión por impago ya pase por `suspendAsAdmin` y la `ClientNote` de F.6 la cubra. Toca el módulo billing. Rama `sprint15c-ii-fase-f5-billing-suspend-unify`. (`DC.44` promovido del backlog.)
>   - ⏳ **F.6 — Notas operativas vía `ClientNote`** (sistema transversal Sprint 16 / ADR-079): cancelar/suspender/reactivar manual → nota **obligatoria** en el modal → `ClientNote` `source_system='service'` + `source_id=serviceId` + `triggered_by_action='service.cancelled'|'service.suspended'|'service.unsuspended'`; auto-suspensión por impago → `ClientNote` auto (`triggered_by_action='service.auto_suspended_overdue'`); el motivo-enum sigue como **campo** del banner (categórico, desacoplado de la nota); el servicio renderiza sus notas inline + salen en `/admin/clients/[id]` → "Notas" como las de los demás módulos (staff-internal — el cliente NO la ve); "añadir nota" del admin = `ClientNote` `manual_entry`. **Supersede el `service_notes` table** (no se crea tabla nueva). Migración: split del `"<motivo>: <nota>"` actual → enum en el campo + `ClientNote` retroactivo. Rama `sprint15c-ii-fase-f6-service-client-notes`. **ADR-079 amendment** (`NoteSourceSystem.service` + `triggered_by_action`s + render del link).
>   - ⏳ **F.7 — SSL/TLS status read-only**: `EnhanceApiClient` lee el estado del cert que Enhance gestiona; `getServiceInfo` gana `ssl?: { status: 'valid'|'expiring_soon'|'expired'|'none'; expiresAt?; autoRenew?; issuer? }` (additivo opcional, patrón `metrics`/`recoveryHint` — **ADR-077 amendment**) + card cliente/admin (`_shared/services/SslStatusCard.tsx`, prop `isAdmin`) con badge verde/ámbar/rojo. Read-only — Aelium NO gestiona el cert (DH-INV-6). Rama `sprint15c-ii-fase-f7-ssl-status`. **ADR-077 amendment** (`ServiceInfo.ssl?`) + **ADR-083 amendment** (probe SSL Enhance).
>   - ⏳ **F.8 — Alertas de cuota** (disco / ancho-banda): setting `quota_alert_threshold_pct` (default 85) + aviso visual en `MetricsBar` al cruzarlo (barra ámbar/roja) + notificación al cliente al cruzar (el reconcile L3 detecta el cruce; persiste "última notif por recurso" para no spamear → emite `service.quota_threshold_crossed` → listener + plantilla seedeada). Heredable a cualquier plugin con `has_metrics`. Rama `sprint15c-ii-fase-f8-quota-alerts`. (Posible tabla `service_quota_alerts`.)
>   - ⏳ **F.9 — Reconciliación per-servicio (`DC.45`)**: `ProvisionerPlugin.reconcileOne?(service)` opcional (**ADR-077 amendment**, patrón A6 `testConnection?()`) + `ReconcileRegistryService.reconcileOne` + `ProvisioningService.reconcileServiceAsAdmin` (con shortcircuit terminal) + endpoint `POST /admin/services/:id/reconcile` + Enhance implementa `reconcileOne` + wire del CTA "Reconciliar contra el proveedor" del `AdminDriftBanner` y de cada fila de drift del `<PluginOperationalOverview>` → endpoint single-shot in-place (cierra el cabo de F.3: hoy el CTA linka a settings). Rama `sprint15c-ii-fase-f9-reconcile-single`. **ADR-077 amendment** (`reconcileOne?()`). (`DC.45` promovido del backlog.)
>   - ⏳ **F.10 — Deep-links curados al panel del proveedor**: "Gestionar email / bases de datos / archivos / logs…" como atajos SSO (las secciones que orchd exponga) en vez del único "Abrir panel" genérico — materialización a decidir pre-código (inline actions plugin-internas sin ADR, o `getSsoUrl({section})` con ADR-077 amendment). Cliente + admin (`_shared/`); capability-driven (ADR-070). Rama `sprint15c-ii-fase-f10-curated-deeplinks`.
>   - ⏳ **F.11 — Conveniencias operativas del detalle de servicio + plugins**: mini-badge de salud del proveedor en `/admin/services/[id]` (del `CircuitBreakerRegistry.getState()`, link al overview F.2) + reenviar notificación al cliente (modal con whitelist de plantillas de service-lifecycle → `POST /admin/services/:id/notifications/resend` + audit) + cross-link a billing en la página del servicio ("Próxima renovación: X · €Y · [Ver factura]"). Rama `sprint15c-ii-fase-f11-service-conveniences`.
>   - ✅ **F.12 — Layout canónico + densidad profesional** — **mergeada a master, PR [#94](https://github.com/yasmindanailov/dashboard/pull/94) squash-merge `c381e68`** (bypass §6, 15ª; `ci:check:full` verde + smoke visual; **8 Amendments I–VIII**: registry declarativo + `<DetailPage>`/`<ServiceHeaderCard>` + 4 primitivas DS [`Meter`/`SectionCard`/`DescriptionList`/`DangerZone`] + overview main+aside + menú "Más acciones" único [elimina tab Gestión] + tabs Resumen/Notas/Auditoría + card "Información del servicio" para servicios mínimos + coherencia terminal + fixes post-smoke). Plan original: diseño — secciones nuevas en `UI_SPEC.md` para `/services/[id]` (admin+cliente discriminado por rol §4.13), lista de plugins, detalle de plugin — jerarquía de componentes + orden/prioridad + responsive + estados empty/error/loading + qué es admin-only — con **wireframes ASCII**; iterar con Yasmin; **FREEZE**; luego refactor de las 3 familias de páginas a la composición congelada (pura composición, cero cambio de comportamiento; reutiliza DS + cards `_shared/`; slots con nombre para lo de F.4-F.11). Rama `sprint15c-ii-fase-f12-canonical-layout`. **Fase con freeze gate** (diseño → iteración → freeze → implementación). Nuevas secciones `UI_SPEC.md` (el diseño es el deliverable).
>   - ⏳ **Fase G — Cierre Sprint 15C.II (PRÓXIMA — única fase restante tras F.12)**: tests críticos faltantes (8 áreas audit Fase A) + E2E extension Fases E + F.1-F.12 (label `ready-for-e2e`) + smoke real Yasmin + retrospectiva (L19+) + mover dossier a `completed/sprint-15c-ii-hardening-enhance.md` (header retrospectiva, dossier original como anexo) + doc-sync de cierre (15C.II ✅ CERRADO, **Sprint 15D RC DESBLOQUEADO**). DoD: todos los DoD de fase (A→F.12) ✓ + `ci:check:full` + `ci:e2e` verdes + smoke OK.
>
> **Detalle completo Sprint 15C original (resumen ejecutivo + métricas + 8 lecciones + commit refs + 18 issues smoke + 4 decisiones doctrinales pendientes):** [`docs/60-roadmap/completed/sprint-15c-plugin-enhance-cp.md`](./completed/sprint-15c-plugin-enhance-cp.md).
>
> **Doc operativa diaria del plugin** (con §8 inventario funcionalidades + 18 issues smoke 2026-05-10): [`docs/features/provisioning/admin-plugins-enhance.md`](../features/provisioning/admin-plugins-enhance.md).

---

## Convenciones de este documento

- **Estado real ≠ estado declarado.** Los símbolos aquí reflejan lo verificado en código a fecha 2026-04-26.
- **No mover sprints a `completed/` hasta que estén realmente cerrados** según [Definition of Done](../90-meta/definition-of-done.md).
- **Cuando un sprint cierra:** mover su sección a `completed/sprint-N-titulo.md` con resumen ejecutivo + commit de cierre + retrospectiva breve.
